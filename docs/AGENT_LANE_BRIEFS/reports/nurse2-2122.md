# Nurse2 sweep 7 — 2026-08-29 21:15–21:22 UTC (lane/nurse2)

Telemetry gate: `active_nodes=0 avg_gpu_pct=0.0 input_tok_s=0.0
reachable=16`; t8 within bring-up window (see below).

## ESCALATE (carried, unchanged)

- **wave3 residentd survivors** on the fleet (verified spark9 rank9
  pid 2317937, sparkc rank12 pid 4188297; still alive this sweep).
  Relevant RIGHT NOW: r3flash-t8 just took spark8-f — spark8, spark9,
  sparkc are IN its node set, so the survivors can collide with t8
  bring-up (ports/memory). Coordinator TERM before the next 16-node
  window (k3finish is waiting for exactly that).
- **"pid gone without exit file" x2** (q27b-build-1 21:00Z, t2 19:48Z)
  — dispatch-layer bug, coordinator/queue-infra owned.

## Fleet / queue

- **r3flash-glm52-exact-cell-t8 RUNNING** on spark8-f (8 leases,
  dispatched ~21:14Z; log on spark8 created 21:14Z, 0 B — bring-up
  window). Ghost-check at sweep 8 (~21:25Z): expect non-zero GPU /
  tok_s or a FAIL line. Context: base task rc=0 at 19:24Z, then
  t3-t7 ALL exit 1 — five consecutive failures, lane iterating every
  ~2-7 min. 6c: receipt still missing, owner r3-flashdecode.
- **bf16-replace-verify-true FINISHED**: `REVALIDATE-DONE rc=0,
  errors: 0`. Final gate UNCHANGED: `PLACE-PASS2-DONE rc=0` + all 16
  ranks exactly 98,019,368,448 B (revalidation may have raced pass2's
  later re-pushes; pass2's own per-rank byte check is the authority).
- **bf16 pass2 accelerated**: rank 3 at ~21:05Z -> rank 6 placed by
  ~21:15Z (~3min/rank); ranks 7-15 remain, ETA `PLACE-PASS2-DONE`
  ~21:40-21:50Z. place_packs alive (3 procs on spark5).
- Queue: 1 running (t8) + 3 notes; spark0-7 idle but no executable
  queued -> no idle-node alarm. Dispatcher ALIVE, all passes clean.

## Healthy / verified

- Nodes 16/16 reporting, error_streak=0. spark8 healthy post-reboot
  (answered nvidia-smi + accepted task log instantly).
- Lanes all active per newest-dirty-file method; qwen27b-serve needs
  to re-queue its build (build-1 died pid-gone — coordinator routing).

## Actions

Read-only + this report.
