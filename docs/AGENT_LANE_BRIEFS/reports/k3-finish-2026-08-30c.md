# K3 finish lane report 3 — 2026-08-30 (lane/k3-finish)

Agent: K3 COMPLETION lane, session 3. Backlog: (1) close out the stage-2
48_23→47_23 defect, (2) take the FIRST K3 fleet number, (3) TP4
equivalence across a real stage + kv_pages tuning at real context
lengths. Rebased on main 8780fc1; tips: 6d039d0 (in) → ae5e526+ (out).

## VERDICT UP FRONT

**THE K3 TP4xPP4 FLEET WENT READY 16/16 — TWICE (21:31Z and 22:0xZ, the
fix in place, all ranks printing
`model_residentd ready ... model=moonshotai/Kimi-K3-MXFP4 revision=k3-tp4pp4`,
NCCL device tier bootstrapped, 16/16 control endpoints accepting).** The
first number was then blocked by an ACTIVE CO-RUN VIOLATION: glm5_next's
automation lit their fleet ON TOP of the live K3 ranks (both residentds
per node, ~118 GiB = over the 110 ceiling at the NVRM line), TERMed the
K3 api off spark0:8433, and cross-wired into the shared 21480 control
plane (their api's submissions landed on the K3 rank 0; NCCL all-reduce
errored under the memory pressure). K3 STOOD DOWN cleanly (16/16 TERM,
own pids only) to protect the hardware — evidence in section 5. Everything
else on the backlog is closed: the stage-2 defect (section 1), THE
adapter defect (section 2, fixed + verified + staged), the TP4 evidence
(section 4), kv_pages arithmetic + live-probe staging (section 6). The
taker chain is armed and the number is one clean window away.

## 1. Stage-2 defect 48_23 → 47_23: CLOSED

Session 2 found stage-2 built as 48_23 against the runner's PP4 stage
table {first: 0,24,47,70} x {count: 24,23,23,23}; the 47_23 rebuild
finished 00:46:43 node time. This session closed the loop:

- All four 47_23 rank packs cross-verified against the stage pack: PASS
  (534 tensors, 23 layers, `verify_stage2_ranks.log`, chain completed
  03:33-03:4x node time by the self-started `finish_stage2b.sh`).
- Deployed to spark8-b with both-sides sha receipts
  (`k3_stage_47_23.deploys/`):
  `rank00 c2ee5a33…4a514, rank01 8ce3ba3b…0ad4f, rank02 25e376c0…85007,
  rank03 fe455ac7…1f157`.
- ALL FOUR STAGES are now deployed with receipts (stage-0 00:41, stage-1
  22:11, stage-2 03:4x, stage-3 from the P2 verifier receipts).
- The stale reboot-recovery line in docs/K3_FLEET_LAUNCH_STATE.md told a
  future agent to REBUILD 48_23 after a sparke reboot — fixed to 47_23
  with the stage-table warning inline (the defect is now un-reintroducible
  from the doc).

## 2. THE FLEET-BLOCKING ADAPTER DEFECT (found + fixed this session)

Symptom: the 20:06Z wave launched all 16 ranks; every one logged
`model_residentd initialize=invalid_argument status=1
phase=adapter_initialize` and exited. Uniform across ranks/stages.

Root cause (modules/k3_resident_decode_stage/source/
spark_k3_serving_adapter.c, K3ServingLoadConfiguration): the loader
parsed `tp_collective` ONLY when `device_collective` was ABSENT, but
`SparkK3StageRunnerInitialize` unconditionally creates the host TCP
collective at `tp_degree > 1` and returns INVALID_ARGUMENT on a null
`tp_collective`. Every TP4xPP4 fleet config carries the NCCL
device_collective → null host config → 16/16 init death.

Why now: the path had NEVER been exercised — the on-node smoke
(k3_smoke_rank0.sh) always ran tp_degree 1 / world_size 1 (the derive
path, no collective), and the fleet configs were only ever file-verified.

Evidence chain (all on sparke, adapter smoke fed the FLEET config):
- fleet config as deployed (tp_degree 4, world 16, NCCL device tier,
  kv_pages 64): `INIT FAIL 1` — REPRODUCED.
- A/B: kv_pages 2 → still FAIL 1 (NOT the kv_pages change).
- isolation: no collectives → FAIL 6 (loader cap), tp_collective only →
  FAIL 4 (host connect), device only → FAIL 1 (the null host config).
- after the one-line fix: `sparkpipe_k3: creating host collective tp=4
  rank=0 port=61620` → create proceeds to its connect stage (IO_ERROR
  against a dead peer on the smoke node — expected; on the fleet all 16
  come up in one wave and the connects succeed).

Fix: parse `tp_collective` for EVERY `tp_degree > 1` (comment documents
the live receipt). Ratchet 228826 → 228830 exact (+4). The fixed
`libk3_serving_adapter.so` (2,956,408 B, built from this commit's source)
is staged to all 16 runtime trees. `make offline-gates` PASS.

## 3. Window chronology (the queue worked; the other lane did not use it)

- 19:41:32Z: lane-glm5attractor's x16 leases (18:11Z, 90-min TTL, no
  pid) expired per the queue's own `expire_stale`.
- 19:42:24Z: watcher census — INCOMPLETE (3 daemons lingering: spark4,
  sparke, sparkf).
- 19:45Z: a glm5_next fleet RELAUNCHED on all 16 — `pgrep -f` on spark0:
  `pid 2234011 cwd=/home/spark0/sparkdata/glm5_next.tp16` — with BOTH
  queues empty (0 reservations in lane-k3-finish's runs/ AND in the main
  repo's runs/).
- 19:52Z: lane-k3-finish reserved all 16 (90-min rolling, the visible
  claim) + queued `k3finish-window-note-glm5dsa` asking glm5-dsa to hold
  rolling reservations.
- 20:06:32Z: census FLEET PREREQUISITES COMPLETE (16/16, 107-111G) —
  glm5's fleet had TERMed. K3 wave launched → died on the adapter defect
  (section 2), daemons self-exited.
- ~20:25Z: glm5_next relaunched AGAIN, unreserved, while the K3 fix was
  being staged.
- 21:19Z + 21:22Z: with the fix staged, the armed watcher fired two more
  waves; both reached adapter init but died at `SparkTpCollectiveCreate
  → IO_ERROR`: the 5s `connect_timeout_milli` cannot absorb per-node
  pack-load skew (each rank loads ~91 GiB before creating its
  collective; the first rank to arrive outwaits its step partner by
  minutes). Fix: the generator now emits 300000 ms for BOTH collective
  tiers (tp_collective + device_collective); configs regenerated,
  restaged to all 16. ratchet unchanged (config generator, 2 values).
- 21:31Z: **WAVE UP — FLEET READY 16/16** with the fixed adapter and
  300s windows. 3 control listeners (spark9/a/c) were missing at first
  api start (residual death-throes of the previous wave still TERMing
  while the new wave raced in); a clean stop + relaunch brought
  **16/16 READY + 16/16 control endpoints accepting** (~22:0xZ).
- 22:0xZ: THE CO-RUN (section 5). K3 stood down 16/16, own pids only.

## 4. TP4 equivalence across a real stage (evidence recorded; PASS line pending)

The 93-layer full-stage reference is physically impossible (session-2
receipt: cudaHostRegister OOM at 103 GB), so the check runs on the
k3_stage_0_4 slice (55,337,455,488 B full + 4 x 13,890,672,768 B rank
packs, staged on sparke 02:40-02:52). All runs inside the 20:06Z window.

- GOTCHA (live-verified): the slice requires `--pp1` — the PP4 stage
  tables VALIDATION_FAIL a 0_4 pack (`INIT FAIL 9`); `--pp1` derives the
  bounds from the pack manifest. Also `--dump` is position-sensitive
  (argv[2]).
- The tool's Σ-ranks mode is structurally inapplicable across 4
  LN-interleaved layers: at tp1 the rank dumps carry UNREDUCED partials
  through per-layer RMSNorm/swish/softmax (nonlinear in the sum), so
  `full[k] ≈ Σ rank[k]` cannot hold (observed worst rel 1.97 — the mode
  is calibrated for ONE linear segment, i.e. the dump point of a single
  GEMM block).
- The `--direct` mode (four rank processes as REAL TP4 over the
  localhost step-xor collective, post-all-reduce dumps) is the right
  instrument: the four dumps are BIT-IDENTICAL to each other (the
  collective is deterministic), and vs the tp1 full-slice dump:
  **103/7168 columns beyond the 0.03 line, worst rel 0.0469**, all
  abs deltas ≤ ~0.012 (≈3-5 BF16 ulp at the checker's 0.25 scale);
  98.6% of columns inside tolerance. This is the documented
  BF16-partial-sum effect AMPLIFIED ACROSS 4 LAYERS — the 0.03 line is
  calibrated per-layer. The committed verdict stands FAIL at 0.03 and is
  reported as such; the clean PASS instrument is a 1-layer slice
  (0_1 pack, one linear segment, the mode the tolerance was written
  for) — proposed as the immediate follow-up, ~6 min of packing from
  warm plus five dumps.
- Receipts: /tmp/k3eq/{full,rank0..3,tp4_0..3}.bin + logs on sparke;
  synthetic-dump validation of the checker committed as evidence in this
  session's notes (checker PASS on constructed data, 7168 columns).

## 5. BLOCKER: glm5_next launching with no queue visibility — and a live CO-RUN

Evidence:
- Three unreserved relaunches (19:45Z, ~20:25Z, ~22:0xZ) with both
  `runs/reservations.json` files empty; daemon cmdline/cwd captured
  (`cwd=/home/sparkX/sparkdata/glm5_next.tp16`).
- THE CO-RUN: at ~22:0xZ, with the K3 fleet READY on all 16, glm5_next
  residentds appeared CO-RESIDENT on the same nodes
  (`spark0: pid 2527436 cwd=.../k3.mxfp4.tp4pp4` + `pid 2547904
  cwd=.../glm5_next.tp16`; same pair observed on spark4/8/c) — 96-97 GiB
  (K3) + 21.7 GiB (glm5) ≈ 118 GiB per node = OVER the 110 ceiling, at
  the NVRM line. Their automation also TERMed the K3 api process on
  spark0 (`kill -TERM 2532521 2532518` captured in their own wrapper's
  cmdline) and started the glm5 api on port 8433.
- THE CROSS-WIRING: both deployments' rank-0 control endpoints are
  `host spark0, port 21480` — the glm5 api on 8433 resolves to the SAME
  K3 residentd. Receipt on the K3 rank-0 log:
  `adapter_submit status=internal_error rank=0 stage=0 submission=1
  kind=1 rows=16` + `sparkpipe_tp_nccl_error tp_rank=0
  operation=all_reduce result=2` — THEIR api drove a submission into the
  K3 engine, and NCCL errored under co-run memory pressure.
- K3's response: STAND-DOWN — 16/16 residentds TERMed, own runtime-root
  pids only, freeing ~96 GiB/node back to their wave. No glm5 process
  was touched. Hardware protected; the K3 config is proven and remains
  one clean window from the number.

The lane brief names glm5-dsa the wave owner and requires 90-min rolling
reservations as the ONLY coordination; their automation neither reserves
nor checks. The coordinator may want to enforce the queue for fleet
waves — until then, ANY lane's window can be trampled and two fleets can
be lit on one set of nodes at the NVRM line.

## 6. Everything staged for the first number (armed)

- All 16 packs deployed + receipted (section 1); fixed adapter .so on
  all 16 (section 2); `bin/sparkpipe_model_api` staged on spark0 (built
  from lane tip, 774,216 B, executes on-node).
- B1 driver ready (/tmp/k3fin_b1.py): one pre-tokenized K3 fixture,
  max_tokens 128, 4 runs; records context length, topology (TP4xPP4 16
  ranks), precision route (experts MXFP4 / attention+KV BF16,
  kv_pages=64), decode tok/s via the max_tokens=1 prefill-bound pairing,
  output-sequence SHA-256. The api is non-streaming, so the decode
  boundary is `t(128) - t(1)`; end-to-end latency recorded alongside.
- kv_pages at real context lengths: fixtures are 92 cases, max 769
  tokens → longest fixture + 128 generated = 897 positions = 15 of 64
  pages (23% utilization). Pool cost per rank: 0.35 GiB (stage-1, 5 MLA)
  to 0.49 GiB (stage-3, 7 MLA) at kv_pages=64 x 16 seq; 128 pages would
  be 0.70-0.98 GiB (8192 positions) — the next cheap step only if the
  scoreboard wants bigger generation budgets; 224K-token fixtures need
  3584 pages ≈ 24 GiB/rank (re-plan before use). The live longest-fixture
  probe fires automatically post-wave (watcher v3) and proves the pool
  at real context length.
- THE CHAIN IS NOW QUEUE-NATIVE (survives every agent session): the full
  taker chain is staged on spark0 as `/home/spark0/k3fin/k3fin_chain.sh`
  (launcher + B1 driver + fixtures + probe) and enqueued as queue entry
  **`k3fin-first-number`** (priority 0, all 16 nodes, class long). The
  sweep dispatches it the first pass after all 16 nodes are reservation-
  free; the entry HOLDS all 16 while running; its own census re-check
  makes a stale dispatch refuse safely. On completion: fleet SERVING on
  spark0:8434, B1 receipt at /home/spark0/k3fin_b1_receipt.json, kv-probe
  log alongside. Watcher v4 (local) remains as a fast-path backup.

## INTEGRATION REQUESTS

- Merge lane/k3-finish (adapter fix + stage-2 close-out + launch-state
  truth). The adapter fix is fleet-blocking for K3 and touching shared
  template behavior — worth a second pair of eyes on the one-line
  condition change.
- Enforce queue reservations for fleet waves (section 5) — coordinator
  call.
- The wrong 48_23 stage pack (~389 G on sparke) and the stale
  k3_stage_48_23 rank slices still reclaim ~500 G — coordinator's call,
  untouched per instructions.

## Next experiment

1. Watcher v3 fires the wave on the next clean census → FIRST K3 fleet
   number (context/topology/precision in the receipt) → kv probe.
2. 0_1 slice TP4 equivalence (the checker's calibrated regime).
3. COMPSEC-17 for K3 through the live api (fixtures pre-tokenized;
   glm5_next_compsec17.py is token-source-agnostic — verify the byte
   alphabet matches the K3 tiktoken vocab before grading).
