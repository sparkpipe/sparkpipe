# wave adopt-qf identity receipt, 2026-09-13

Baseline: origin/lane/mod-infra-qwen 5096079 (INFRA-Q's common GDN stage kernels +
common KV frame + qwen38max llm_defines.h). Family: qwen3flash (qwen4_flash).
Code-only wave: no inference, no execution, no weightd contact.

## Module 1 adoption: common/common_gdn_stage_kernels.cu -> qwen4_flash

Deleted from modules/qwen4_flash_resident_decode_stage/source/spark_qwen4_flash_resident_decode_stage_cuda.cu
(2,587 -> 1,706 lines): the shared 22-kernel GDN/attention/MoE/router suite
(RopeFrequency, ConvUpdate, DecayBeta, GdnStep, GatedNorm, EmbeddingGather,
ChunkPrepare/Solve/Transform/QkDecay/Step/Conv, ResidualAdd, EmbeddingGather,
GateScores, GateSelect, SwiGlu, SharedGate, TpCombineAdd, WarpReduceMax,
AttnTokenBase, ChunkHeadOffset + their launchers + ConfigureCudaKernels).
Kept as flash arms: AttnPrepare/AttnDecode (flash's speculative token-mask
parameter the common pair lacks), HC x5, Indexer x2, PLE x4, HeadMaxLoc,
HeadOrderKey, TpCombineU64Max, EmbeddingGatherSharded, GroupedScalarE8m0,
LaunchMoePairReduce, the flash GroupedExpertLinear/TileLinear launchers
(superset weight-format dispatch: E8M0B128/BF16/NVFP4 + frame_error).

Receipt method: deleted flash bodies token-renamed
(SparkQwen4Flash->LmGdnStage, SPARK_QWEN4_FLASH_MODEL_*->SPARK_LLM_*,
SPARK_QWEN38_ROUTER_SORT_CAPACITY->SPARK_LLM_ROUTER_SORT_CAPACITY,
SPARK_QWEN4_FLASH_CUDA_*->SPARK_GDN_STAGE_CUDA_*) and diffed against the
common bodies: 11 of 12 pure-shared kernels at 0 changed lines (RopeFrequency's
2 changed lines are the rope key names ATTN_ROPE_*->ROPE_* carrying identical
values 64u / 1e7f). GateSelect: 0 changed lines beyond the token table plus two
static_assert message strings ("qwen38 expert count..." -> "expert count...");
the misnamed SPARK_QWEN38_ROUTER_SORT_CAPACITY definition is gone from the
flash tree (grep count 0).

TP parameterization added to the common module (behavior-preserving for max):
the GDN-side kernels took the FULL head count at compile time in max's copy
while flash's copies took tp_degree and computed local counts — a silent
behavior fork between family copies. The common kernels now take
`uint32_t tp_degree` and compute locals through new SPARK_LLM_GDN_LOCAL_*
shard-algebra keys (LOCAL_KEY_HEAD_COUNT, LOCAL_VALUE_HEAD_COUNT,
LOCAL_QK_DIMENSION, LOCAL_VALUE_DIMENSION, LOCAL_CONV_CHANNELS). Max's
delegating launchers pass 1u, at which every local expression reduces to the
previous compile-time constant and every grid dimension is unchanged
(GDN_VALUE_HEAD_COUNT/1 == GDN_VALUE_HEAD_COUNT). Changed-line counts for the
tp insertion: ConvUpdate 8, DecayBeta 6, GdnStep 19, GatedNorm 0 (bounds-only),
ChunkPrepare 6, ChunkTransform 6, ChunkStep 2, ChunkConv 2.

## Module 5 creation + adoption: common/common_stagepack_format_ext.h

New module (catalog #5, seed = the deleted spark_qwen4_flash_stagepack_format.h,
548 lines): family-neutral SparkStagePackEntry (56-byte wire entry,
_Static_assert-pinned), tensor-role table (32 roles), SparkStagePackFamilySpec +
SparkStagePackKindTable descriptors, and the family-parameterized validator set
(ExpectedTensorCount, ExpectedGeometry, TensorShapeOf, ResolvedShape,
NarrowShape, PayloadBytes, ScaleBytes, SpecCheck). Consuming the missing
weight-format codes U32/MXFP4/E8M0B128 as family-neutral codes, and the
GLOBAL/MTP layer sentinels + 256-byte payload alignment.

Adoption deletes: the format header itself (548 lines). module.c and
qwen4_flash_pack_synthesize.c call the common functions with the spec+table
from model-families/qwen4_flash/include/sparkpipe/llm_defines.h.

Receipt: ground-truth vectors were generated by compiling the DELETED format.h
from main and printing its outputs (counts 101/111/1236/1246/100/1200,
27 shape vectors, 12 resolved-shape statuses, 6 narrow-shape numbers, 15
payload/scale byte numbers). tests/test_llm_stagepack_format.c asserts all of
them against the common implementation — all pass. Negative controls
(tests/test_llm_stagepack_format_negative.c) flip named params
(attention_period 5, full_attention_phase 4, gdn_value_head_count 50,
attn_query_head_count 25, attn_rope_dimension 63, mxfp4_group_size 48,
fp8_block 100, ple_embed_dimension 2559, mtp_layer_count 0) and require the
exact validator failure code / exact count delta (1236 -> 1205).

## Module 7 adoption: common/common_kv_frame.h -> qwen4_flash

Deleted from spark_qwen4_flash_resident_decode_stage_module.c
(2,531 -> 2,262 lines): KvWaitBatch (25), KvEvictSlot (39), KvPrepareFrame
(249), KvMarkWritten (12). The 249-line KvPrepareFrame was measured identical
to qwen38max's copy before either adoption: 249 = 249 lines, difflib ratio 1.0,
zero unified diff lines after family-token + whitespace normalization.
Adopted the LmKvFrameOps vtable (ops wired to flash's WorkControl functions
with sizeof _Static_asserts, mirroring max's adoption) and the LmKvFrameState
embedding; family delegates keep the SparkQwen4FlashModuleKvPrepareFrame /
KvMarkWritten signatures so callers and the validator are unchanged.

## Module 3 (parameter carrier): llm_defines.h

model-families/qwen4_flash/include/sparkpipe/llm_defines.h now holds every
value the three modules plus the kernels consume: the model.h 66-key geometry,
the firmware.h ABI/capacity keys, the router sort capacity (family-neutral
SPARK_LLM_ROUTER_SORT_CAPACITY 512u replacing the misnamed constant), the KV
frame keys, the GDN shard algebra, and the 36-row stagepack kind table +
family spec. spark_qwen4_flash_model.h (100 -> 65 lines) and the firmware.h
define block are pure aliases; tests/test_qwen4_flash_model_header.py now
parses llm_defines.h and still binds all 36 contract keys + 8 composed values
to model_contracts/qwen4_flash_authoritative.json.

## Test evidence (host, this workstation, no GPU)

- build/test_llm_stagepack_format: PASS (47 vectors + 10 negative controls).
- build/test_llm_module_contract (INFRA-Q's suite, proves max side unaffected
  by the tp extension): PASS.
- python3 tests/test_qwen4_flash_model_header.py: PASS (36 + 8 bindings).
- clang -std=c11 -Wall -Wextra -Werror -fsyntax-only (CUDA stub):
  module.c, serving_adapter.c (with Makefile -D flags), work_control.c,
  qwen4_flash_pack_synthesize.c, llm_defines.h, common_stagepack_format_ext.h:
  clean.
- clang++ -std=c++17 -fsyntax-only (CUDA stub): cuda_validation.cu clean.
- NOT verifiable on this host: nvcc compile of the two .cu files and the
  sm_121a gate (tools/cuda13_sm121a_compile_gate.sh) — GPU/nvcc nodes only.
  The flash validator only consumes the family launcher ABI, which is
  unchanged.

## Complexity ceiling

tests/test_complexity_ceiling.py fails on this branch exactly as it fails on
origin/lane/mod-infra-qwen and on main: production max CCN 88 (ceiling 75) at
spark_qwen38_27b_serving_adapter.c:SparkQwen38_27bServingSubmitSpeculativeDecode
— pre-existing, untouched by this wave. The wave strictly reduces production
function count (3,725 on main -> 3,674 here).
