# Nurse2 sweep 8 — 2026-08-29 21:25–21:33 UTC (lane/nurse2)

Telemetry gate: `active_nodes=0 avg_gpu_pct=0.0 input_tok_s=0.0
reachable=16`; t8 holds 8 leases — discrepancy escalated below.

## ESCALATE

1. **GHOST WORK #2 — r3flash-glm52-exact-cell-t8: 0% GPU on ALL 8
   held nodes, ~13 min silent in its compute phase** (owner
   r3-flashdecode; dispatched 21:14:27Z, TTL from queue). Evidence:
   `/tmp/r3flash-cell.log` on spark8 — last lines
   `21:14:27 r3cell phase 0: distribute / 21:14:40 r3cell phase 0
   done / 21:14:40 r3cell phase A: threshold-0 O128` and NOTHING
   since (log mtime 21:14Z; checked 21:26 + 21:30). Node sweep of all
   8 held nodes (spark8,9,a,b,c,d,e,f): `nvidia-smi utilization` = 0%
   on every one; telemetry 21:26Z = 0 busy / 0 tok_s. Phase A is
   6x longer than t5/t6/t7's whole fast-fail lives — bring-up is
   stuck, not running. Likely cause (one read): t8's per-host
   residentd bring-up blocked by **wave3's surviving residentds** (my
   standing escalation) — sparka currently runs TWO residentds at
   once (direct stacking evidence); the t8 node set includes
   spark8/9/c where survivors were verified. Coordinator owns
   fix/cancel; the survivor TERM (escalated sweeps 6-7) likely
   unblocks this. 6c: sixth consecutive r3flash cell failure class
   (base rc=0 19:24Z, t3-t7 exit 1, t8 hung).
2. **Carried**: wave3 residentd survivors fleet-wide (now implicated
   as t8's blocker — see above). **Carried**: "pid gone without exit
   file" x2 dispatch-layer bug.

## Verified this sweep

- **bf16 pass2**: rank 8 -> spark8 and rank 9 -> spark9 placed by
  ~21:20Z; ranks 10-15 remain; 3 place_packs procs alive; pace
  ~4-5min/rank -> `PLACE-PASS2-DONE` ETA ~21:50-22:00Z. THE only
  fleet compute-adjacent activity all cycle.
- t8's 0 B queue log on spark8 is BY DESIGN (cmd redirects to
  /tmp/r3flash-cell.log) — not a stuck launch in the pid=None class;
  the ghost call is based on phase silence + all-node GPU zero.
- Dispatcher ALIVE, passes clean; queue: 1 running (t8) + 3 notes.
- Nodes 16/16 reporting, error_streak=0.

## Actions

Read-only + this report. Sweep 9 (final): pass2 completion + t8 fate.
