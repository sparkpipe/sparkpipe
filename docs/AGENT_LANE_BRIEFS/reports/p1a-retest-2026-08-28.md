# P1a retest — deactivate-route fix (dcd3824) — 2026-08-28

Worktree /tmp/lane-p1a2, branch `lane/p1a-retest2` (= main 0d09034; fix
dcd3824 verified ancestor). Node: spark8. Deployment:
`/home/spark8/sparkdata/qwen38.fp8.tp1` (rsync'd from the spark9 staging
tree, 29G pack, hash-chain below).

Mission: prove or refute the P1a fix for the intermittent client-fatal
`FAILURE_DEACTIVATE_ROUTE` (status=17 reason=7) at B>=24 on the 27B TP1
(knee-sweep receipts). Old failing points retested at the 128-lane config;
B=16/32 regression check; verdict below.

## Contention context (read this first)

spark8 is NOT exclusively free: the glm53 lane's all-16 TP16 fleet brings
rank 8 up on spark8 in cycling waves (~5-10 min up, ~5 min gaps observed
16:21-16:31 UTC; their wave launcher double-launched rank 8 twice — two
residentd pids 1126834/1142786, then 1151490/1151492). All 27B points here
ran in those wave gaps. The 128-lane 27B config needs 80.6 GiB device
(knee K2), the glm53 pair holds ~43-52 GiB — co-residency is impossible
under the 110 GiB operator ceiling, so gap timing was mandatory. The
glm53 reservation on spark8 (TTL 480m from 09:07 UTC) blocked
`spark_queue.py reserve` until its TTL expiry; see INTEGRATION REQUEST.

## R1 — provenance: rebuilt from main with the fix

```
$ git merge-base --is-ancestor dcd3824b1a27b375564affbd17dba6cd53f1caeb HEAD && echo HAS_FIX
HAS_FIX   (HEAD = 0d09034 "rdma: debug_enabled was dead code...")
$ ssh spark8 'cd /tmp/lane-p1a-build && git log --oneline -1 && git merge-base --is-ancestor dcd3824b... HEAD && echo HAS_FIX_AT_BUILD_HEAD'
0d09034a rdma: debug_enabled was dead code (never set) — env-gate SPARK_RDMA_DEBUG=1 ...
HAS_FIX_AT_BUILD_HEAD
$ ssh spark8 'cd /tmp/lane-p1a-build && make -j8 build/sparkpipe_model_residentd build/sparkpipe_model_batch build/sparkpipe_model_api'
cc ... node/model_residentd.c ... -o build/sparkpipe_model_residentd        [exit 0]
cc ... node/model_batch.c ... -o build/sparkpipe_model_batch                [exit 0, forced fresh rebuild]
cc ... node/model_api.c ... -o build/sparkpipe_model_api                    [exit 0]
```

Install into the deployment (pre-p1a2 binaries kept as `*.before-p1a2`;
the first p1a agent's Aug-27 staging kept as `*.before-p1a-20260828`):

```
bbb22e8ec56cea9a2a3df7deea63083368dd653dccadb688875f77118c64493c  sparkpipe_model_residentd   (= build worktree, hash-verified)
2818d9ac6f600acbc94375b2a159e86f3521232b03f4686a28715a9e94efc81f  sparkpipe_model_batch
3616c7183d9147d864f8ecdc08865c53b96684aa5ab8b4f5ae7fa47ad8474574  sparkpipe_model_api
```

## R1b — deployment staging + config (exact)

Tree: `rsync -a spark9:/home/spark9/sparkdata/qwen38.fp8.tp1
spark8:/home/spark8/sparkdata/` — 29G both ends, 24 files, no errors.
Portability (spark9 -> spark8) + the knee-sweep 128-lane envelope applied
with `cp` backups (`*.before-p1a2`); live-vs-backup diff is exactly:

```
 "runtime_limits": {
-  "max_inflight_submissions": 2,     +  "max_inflight_submissions": 4,
-  "max_active_sequences": 64,        +  "max_active_sequences": 128,
    "max_input_rows": 128,               "max_input_rows": 128,
-  "resident_sequence_capacity": 64,  +  "resident_sequence_capacity": 128,
-  "kv_logical_page_capacity": 256,   +  "kv_logical_page_capacity": 2048,
-  "kv_physical_page_capacity": 64    +  "kv_physical_page_capacity": 1024
 }
 nodes[0].runtime_root: /home/spark9/... -> /home/spark8/sparkdata/qwen38.fp8.tp1
 nodes[0].control_endpoint.host: spark9 -> spark8
 config/qwen38_27b_tp1_rank0.json: max_sequence_positions 8192 -> 4096
```

This is byte-for-byte the knee-sweep 128-lane diff (report K2). Protocol
identical to the sweep: budgets 128/256, 64-token prompts, one
`tests/test_batch_knee_sweep.sh` invocation per B (own residentd, TERM at
exit, TERM-first/no-KILL, spawn-captured pid inside the script), 45s
pre-launch sleep between my own invocations, `drop_caches` before every
residentd start (page-cache starvation finding), sweep tool UNITS-FIXED
on main (rates = tokens/wall).

Runner: `/home/spark8/sparkdata/p1a/run-point.sh` (clean-name launcher —
see Operational notes for the pgrep -f self-match that cost one window).

## R2 — the old failing points (B=24, B=48 @ 128-lane)

<!-- RESULTS_R2 -->

## R3 — regression points (B=16, B=32 @ 128-lane)

<!-- RESULTS_R3 -->

## Fix-path evidence

<!-- FENCING_LINES -->

## Verdict

<!-- VERDICT -->

## Operational notes

- pgrep -f self-match cost one full window: the sweep tool's own guard
  (`pgrep -f sparkpipe_model_residentd`) matched the LAUNCHING shell's
  cmdline (the guard text itself), refusing a start on a FREE node at
  16:22 UTC. Fix: launch through a clean-name runner script whose cmdline
  carries no daemon names (run-point.sh). Same hazard class as the knee
  lane's "TERM killed the ssh wrapper" note.
- The first p1a agent got further than "nothing started": its residentd
  reached ready at the 128-lane config (device_gib=80.6, spark8, 03:44
  UTC) and published 48 prefix slots (B=48-shaped run) before dying with
  the agent — no route_failed, no fencing line in 98 log lines. Receipt
  kept: p1a/agent1-residentd-log-stale.bak.
- glm53 wave observations on spark8 (all times UTC): TERMs ~16:21-16:26
  (node 109 GiB free), relaunch 16:26 x2, relaunch 16:31 x2. Two copies
  of rank 8 up simultaneously twice (their launcher's liveness check
  misses live daemons; they fixed pgrep -x truncation in c9cbfdb but the
  double-launch persists).

## INTEGRATION REQUEST

1. Node assignment collision: my brief assigned spark8 (reserved), but
   glm53's all-16 fleet claim includes spark8 with a live reservation
   that `spark_queue.py reserve` refuses to override, and their wave
   launcher does not check the queue before relaunching. GPU lanes
   sharing fleet nodes need either (a) the launcher to respect
   reservations, or (b) an explicit coordinator arbitration entry.
2. (suggestion) sweep tool: the guard pgrep patterns should use
   `pgrep -x sparkpipe_model` (15-char comm) instead of `pgrep -f` full
   cmdline matching to kill the self-match refusal class.

## Next experiments

(none — verdict lane; P1b ratio-cliff and the >B=32 ladder remain with
the runtime owners per the knee report)
