# P1a retest — deactivate-route fix (dcd3824) — 2026-08-28

Worktree /tmp/lane-p1a2, branch `lane/p1a-retest2` (= main 0d09034; fix
dcd3824 verified ancestor). Node: spark8. Deployment:
`/home/spark8/sparkdata/qwen38.fp8.tp1` (rsync'd from the spark9 staging
tree, 29G pack, hash chain below).

Mission: prove or refute the P1a fix for the intermittent client-fatal
`FAILURE_DEACTIVATE_ROUTE` (status=17 reason=7) at B>=24 on the 27B TP1
(knee-sweep receipts, docs/AGENT_LANE_BRIEFS/reports/knee-sweep-2026-08-28.md
"Failed points").

## VERDICT: NOT REPRODUCED (partial — one clean full pass at B=24; B=48 blocked by a node incident; repeats not run)

- **B=24 (the 505-submission old-failing point): COMPLETE, full budget,
  zero error lines, on main+fix.** The old bug killed this exact point at
  submission 504/505. One clean pass of the same protocol = the fix held
  where it previously failed — but with an intermittent 1-in-~500 bug,
  one pass is "not reproduced", not "proven".
- **B=48: no data.** Attempt 1 was killed by an external SIGTERM (the
  glm53 wave launcher's TERM-sweep); attempt 2's residentd reached ready
  and the node then went ssh-dark and AUTO-REBOOTED (unified-memory
  pressure — my 80.6 GiB boot overlapped the glm53 rank-8 relaunch;
  kernel NV_ERR_NO_MEMORY storm at 16:43:20 UTC in `journalctl
  --boot=-1`, receipt below). Per coordinator decision (abort remaining
  points) no further GPU attempts were made.
- **B=16/B=32 regression points: not run** (no regression signal exists
  from the one B=24 pass: 35.79 decode tok/s — the ~40-class at the
  128-lane config, nowhere near the 3.5 tok/s P1b cliff).

## Contention timeline (why most points never ran)

spark8 was assigned to this lane but is also rank 8 of the glm53
all-16 TP16 fleet, whose automated wave launcher cycles the node
continuously (observed waves 16:11, 16:20 x2, 16:26 x2, 16:31; ~5-10 min
up, ~5 min gaps; the launcher double-launched rank-8 twice — two
simultaneous residentd pids) and TERM-sweeps every `sparkpipe_model*`
process (15-char comm match) before each wave. Co-residency is
infeasible: 128-lane 27B = 80.6 GiB device (knee K2) vs their 43-52 GiB
per wave pair on a 119.7 GiB unified node (110 GiB operator ceiling).
All points therefore ran in wave gaps; B=24 (~5.5 min) fits a gap, B=48
(~11 min) does not.

Incident (spark8 auto-reboot, 16:43-16:48 UTC): queue note
`p1a-spark8-incident-1643utc` filed at 16:46 UTC, before recovery.

```
16:38:12  chain fires B=48 attempt 1 (node free 50s sustained)
16:40:49  EXIT=1: "Terminated" + "batch tool failed status=143 (B=48
          budget=128)"  (SIGTERM from outside; glm53 last log write
          16:32, no wave up — TERM-sweep-then-staggered-relaunch pattern)
16:42:15  chain refires B=48 attempt 2 into the next free window
~16:43    residentd ready pid=1182507 (80.6 GiB claimed); glm53
          relaunch allocates on top -> NVRM storm:
$ ssh spark8 'sudo -n journalctl --boot=-1 -k | grep -iE "oom|nvrm" | tail'
Aug 29 01:43:20 spark8 kernel: NVRM: nvCheckOkFailedNoLog: Check failed:
  Out of memory [NV_ERR_NO_MEMORY] (0x00000051) returned from
  _memdescAllocInternal(pMemDesc) @ mem_desc.c:1359      (x8, 01:43:20-26)
16:43-16:48  ssh banner-exchange timeouts (node dark ~7 min)
16:48     system boot 2026-08-29 01:48 (KST) = auto-reboot; NO reboot was
          issued by this lane (queue denies such commands; protocol)
16:49:45  node responsive again; no sparkpipe daemons; ~30 min of
          results lost with /tmp; coordinator chose ABORT remaining points
16:53+    glm53 launcher has already relaunched rank-8 (pid 48873) — the
          cycle reclaims the node automatically; further p1a GPU work
          would repeat the collision
```

## R1 — provenance: rebuilt from main with the fix

```
$ git merge-base --is-ancestor dcd3824b1a27b375564affbd17dba6cd53f1caeb HEAD && echo HAS_FIX
HAS_FIX   (HEAD = 0d09034 "rdma: debug_enabled was dead code...")
$ ssh spark8 'cd /tmp/lane-p1a-build && git log --oneline -1 && git merge-base --is-ancestor dcd3824b... HEAD && echo HAS_FIX_AT_BUILD_HEAD'
0d09034a rdma: debug_enabled was dead code (never set) — env-gate SPARK_RDMA_DEBUG=1 ...
HAS_FIX_AT_BUILD_HEAD
$ ssh spark8 'cd /tmp/lane-p1a-build && make -j8 build/sparkpipe_model_residentd build/sparkpipe_model_batch build/sparkpipe_model_api'
cc ... node/model_residentd.c ... -o build/sparkpipe_model_residentd   [exit 0]
cc ... node/model_batch.c ... -o build/sparkpipe_model_batch           [exit 0, forced fresh rebuild]
cc ... node/model_api.c ... -o build/sparkpipe_model_api               [exit 0]
```

Install into the deployment (prior binaries kept: `*.before-p1a2` =
spark9-staged set; `*.before-p1a-20260828` = the first p1a agent's):

```
bbb22e8ec56cea9a2a3df7deea63083368dd653dccadb688875f77118c64493c  sparkpipe_model_residentd  (== build worktree, hash-verified)
2818d9ac6f600acbc94375b2a159e86f3521232b03f4686a28715a9e94efc81f  sparkpipe_model_batch
3616c7183d9147d864f8ecdc08865c53b96684aa5ab8b4f5ae7fa47ad8474574  sparkpipe_model_api
```

## R1b — deployment staging + config (exact)

`rsync -a spark9:/home/spark9/sparkdata/qwen38.fp8.tp1
spark8:/home/spark8/sparkdata/` — 29G both ends, 24 files, no errors.
Portability (spark9 -> spark8) + the knee-sweep 128-lane envelope applied
with `cp` backups (`*.before-p1a2`); live-vs-backup diff is exactly:

```
 "runtime_limits": {
-  "max_inflight_submissions": 2,     +  "max_inflight_submissions": 4,
-  "max_active_sequences": 64,        +  "max_active_sequences": 128,
    "max_input_rows": 128,               "max_input_rows": 128,
-  "resident_sequence_capacity": 64,  +  "resident_sequence_capacity": 128,
-  "kv_logical_page_capacity": 256,   +  "kv_logical_page_capacity": 2048,
-  "kv_physical_page_capacity": 64    +  "kv_physical_page_capacity": 1024
 }
 nodes[0].runtime_root: /home/spark9/... -> /home/spark8/sparkdata/qwen38.fp8.tp1
 nodes[0].control_endpoint.host: spark9 -> spark8
 config/qwen38_27b_tp1_rank0.json: max_sequence_positions 8192 -> 4096
```

Byte-for-byte the knee-sweep 128-lane diff (report K2). Protocol
identical to the sweep: budgets 128/256, 64-token prompts, one
`tests/test_batch_knee_sweep.sh` invocation per B (own residentd, TERM
at exit, spawn-captured pid in-script, TERM-first/no-KILL), 45s+ TIME_WAIT
spacing between own invocations, `drop_caches` before every residentd
start (knee operational finding), UNITS-FIXED rate columns (tokens/wall).

Runner: `/home/spark8/sparkdata/p1a/run-point.sh` (clean-name launcher —
see Operational notes for the pgrep -f self-match that cost one window).

## R2 — the old failing points (B=24, B=48 @ 128-lane)

**B=24 run1: COMPLETE, no route_failed.** The old bug killed this exact
point at submission 505 (knee receipt). Raw output:

```
$ [watcher fires run-point.sh 24 run1 at 16:32:07 UTC, node 108 GiB free]
residentd ready pid=1165535
B,budget_hi,wall_hi_ms,tokens_hi,aggregate_tok_s,decode_tok_s
24,256,185910,6144,33.05,35.79
EXIT=0 B=24 RUN=run1
$ grep -nE "route_failed|deactivate_after_abort|fencing|INTERNAL" residentd-b24-run1.log
(no matches; 50-line log kept at spark8:/home/spark8/sparkdata/p1a/)
```

Full budget delivered (tokens_hi = 6144 = 256x24); decode-window rate
35.79 tok/s (units-fixed). The fix's loud fencing line did NOT appear —
the deactivate disagreement did not occur in this pass (change #1's
already-released skip is silent by design). Residentd TERM'd cleanly by
the tool's own trap.

**B=48: BLOCKED — receipts in the contention timeline above.** Attempt 1
raw failure (observed live at 16:44 UTC from p1a-b48-run1.log before
attempt 2 overwrote it; mac-side chain log corroborates EXIT=1 at
16:40:49): "Terminated" / "batch tool failed status=143 (B=48
budget=128)" — an external SIGTERM, not a route failure. Attempt 2's
residentd reached ready (pid 1182507) and died with the node.

## Fix-path evidence

No `deactivate_after_abort failed ... fencing route` line exists in any
surviving log this lane produced (b24-run1's 50-line residentd log is
the only completed-run log; greps above). The path therefore did not
fire in the one clean pass — expected: the line only appears when the
adapter's abort cleanup disagrees with the residentd's bookkeeping,
which is the intermittent condition itself.

## Operational notes

- pgrep -f self-match cost one full window: the sweep tool's guard
  (`pgrep -f sparkpipe_model_residentd`) matched the LAUNCHING shell's
  own cmdline (it contained the guard text), refusing a start on a FREE
  node at 16:22 UTC. Fix: launch through a clean-name runner script
  whose cmdline carries no daemon names (run-point.sh). Same hazard
  class as the knee lane's "TERM killed the ssh wrapper" note.
- The first p1a agent got further than "nothing started": its residentd
  reached ready at the 128-lane config (device_gib=80.6, spark8, 03:44
  UTC) and published 48 prefix slots (B=48-shaped run) before dying with
  the agent — no route_failed, no fencing line in 98 log lines. Receipt:
  `p1a/agent1-residentd-log-stale.bak`.
- glm53 wave observations on spark8 (all UTC): up 16:11; down 16:21-16:26
  (109 GiB free); up 16:26 x2; up 16:31 x2 (their last log write 16:32);
  TERM-sweep ~16:40; relaunch ~16:43 (collision); auto-reboot 16:48;
  rank-8 back 16:53. Two simultaneous rank-8 copies twice.
- spark8 node state at lane exit: glm53 rank-8 residentd up (their
  launcher's, not touched — spawn-captured-pids rule); NO p1a daemons
  (mine TERMed by tool traps / died with the reboot); p1a receipts in
  /home/spark8/sparkdata/p1a/ (survives reboot); 29G deployment + config
  backups + bin backups left in place for the completing lane.
- Lane held no queue reservation (glm53's spark8 reservation, TTL 480m
  from 09:07 UTC, refused `reserve` until expiry; the GPU work ran
  under physical gap-timing instead — the gap discipline held for B=24
  and was defeated by the launcher's staggered relaunch for B=48).

## INTEGRATION REQUEST

1. **Node assignment collision (P0-class, caused one auto-reboot):**
   spark8 was assigned to this lane while glm53's automated all-16
   launcher also claims it. Their launcher neither checks queue
   reservations nor spares foreign daemons (TERM-sweep of every
   `sparkpipe_model*`), and boot-time allocation overlap of two CUDA
   reservations >119 GiB wedges then reboots the node (NVRM
   NV_ERR_NO_MEMORY storm receipt above; ceph OSD on the same node).
   Required: either the wave launcher respects reservations + skips
   nodes holding foreign sparkpipe daemons, or exclusive-node lanes get
   a real mutex (queue gate) before ANY GPU lane starts.
2. (suggestion, sweep tool) guard pgrep patterns should use `pgrep -x
   sparkpipe_model` (15-char comm) instead of `pgrep -f` full-cmdline
   matching — kills the self-match refusal class documented above.
3. (completing the retest) remaining points B=48, B=16, B=32, B=24 x2
   need ~40 min of EXCLUSIVE spark8 (or any idle GPU node with the 29G
   staging rsync'd, config portability is two fields). The staging,
   runner, and chain driver are all in place on spark8.

## Next experiments

1. Finish R2/R3 on an exclusively-held node (B=48 + B=24 x2 + B=16/32)
   — the verdict above is partial by blockage, not by doubt.
2. P1b ratio-cliff and the >B=32 ladder remain with the runtime owners
   per the knee report.
