# GLM 5.3 Flash lane — 2026-08-30 session log

## Tick 1 — emit-path INTERNAL_ERROR: mechanism traced offline

Session model: 5-min wake timer, PR-based coordinator. No GPU work
(queue notes-only; fleet idle; no open PRs).

Offline trace of `G5N-DBG completion emit: sub 1 status 17 tokcnt 0
acc 10` + `route_failed status=1 reason=2`:

- The generated driver carries the first failing operation's status;
  the adapter emit is a faithful propagator (identity matched).
- In the module, SparkGlm5NextCompleteAsync forces INTERNAL_ERROR in
  two places, both flattening the reason: host_kv_access_error[0]
  (a 6-word LmKvAccessError record from the KV kernels) and a failing
  SparkKvPageCacheCompleteLane.
- I landed print-only diag at both flatten sites (lane commit d723cb5)
  and a compile proof (module make, fp8, pinned revision/contract).

## Tick 2 — rebase forward: the fix is ALREADY ON MAIN; diag subsumed

- lane/glm53 was 578 behind origin/main; ALL prior lane commits
  (RouteBinding ring-alias fix, wave launcher, probes) were already
  integrated by the coordinator.
- The rebase conflict showed main already contains BOTH the diag print
  (richer: slot + status_in) AND the root-cause fix, with this comment
  in SparkGlm5NextCompleteAsync: a cache-free frame (cache_lane_count 0)
  leaves the remembered lane zeroed, and committing it made
  SparkKvPageCacheCompleteLane return INVALID_ARGUMENT — "the
  final-emit INTERNAL_ERROR that killed every first token"; commit
  only when the admission ladder actually plumbed a cache lane for the
  sequence. My tick-1 second suspect was the real one.
- Resolution: took main's side; d723cb5 became empty and was dropped
  (subsumed). Lane is now origin/main + this report only.
- offline-gates receipt: exit 2 on the code-size gate — authored lines
  233254 vs ceiling 233120 (134 over). NOT lane-caused: pristine
  origin/main at cb120a0 fails identically (checked by running
  tests/test_code_size.py on a clean main checkout). The ceiling needs
  a coordinator re-measure (likely the qwen38 packer merges grew it).
  All other gates before it in the run: green (log /tmp/g5n_gates2.log
  equivalent run).

### Current frontier (to realign against, next ticks)

- First-token emit blocker: FIXED on main (see above).
- Next per brief M5: serving + first perf cell (exact-32K B1 decode,
  expectation ~35-45 tok/s no-spec class), then MTP-1.
- Lane debts cleared: merge-forward done; RouteBinding integration
  already happened. Remaining: none known at rebase time.

## Tick 3 — M5 cell staged and ready to fire

- No state change: queue still notes-only, no open PRs, coordinator log
  unchanged. GPU work stays parked.
- Landed tools/glm53_m5_cell_task.sh (queue-staged on spark0, all-16):
  fleet-wide cache-drop IN THE TASK CMD (the GB10 trap that ate q27b
  serve-6/7) -> clean serve wave via tools/glm5_next_wave.sh full (no
  probes) -> /health gate -> M5 batch generated in-task ->
  8 sequential batch runs (sustained across the 5s telemetry poll;
  dashboard summary + rank-local nvidia-smi on sparkc sampled at 10s
  and 30s mid-run) -> per-run token count + sha256 receipt. Ends
  TASK-DONE (no trailing exit $rc).
- Offline proofs: bash -n + embedded-python ast OK; the batch generator
  run against the committed fixture produces EXACTLY 32768 positions
  (32512 prompt from 17 COMPSEC prompts cycled, pool 5094 ids, budget
  256, prompt sha256 468db1b8...63e0c601) with the full engine-member
  schema.
- Fire command when the queue is live (needs the lane tree cloned at
  ~/g5m5-src on spark0 + registrar staged + driver/module redeployed
  from current main tip):
  python3 tools/spark_queue.py add --id glm53-m5-cell \
    --nodes spark0,spark1,spark2,spark3,spark4,spark5,spark6,spark7,spark8,spark9,sparka,sparkb,sparkc,sparkd,sparke,sparkf \
    --cmd 'bash /home/spark0/g5m5-src/tools/glm53_m5_cell_task.sh' \
    --priority 5 --ttl-min 60
- Cell context for the perf claim: exact-32K (32768 positions), B1,
  TP16 host-rdma, fp8 experts, no-spec. Expectation per brief: DSV4-
  Flash class ~35-45 tok/s. Exactness gate comes first: 8/8 runs must
  agree on the token hash (determinism) before any timing is quoted.

## Tick 4 — TP16 stagepack CPU verification: ACCURATE (no defect found)

Mission: verify the deployed TP16 packs (glm53_packs_fixed2, the
o_proj cols-shard repack, dir sha 54e2474be88a2544...) via CPU against
the checkpoint. Verdict: ACCURATE — no coordinator fix needed.

Two independent layers, both on-NODE (CPU only, no GPU work, queue
untouched; staged in ~/g5m5-verify on spark0/sparkf):

1. Built-in pack verifier (tools/glm5_next_pack_verify.py) rank 0
   --deep: header/file-bytes/count=1160/layout PASS, full plan diff vs
   the CURRENT packer rebuilt from the checkpoint PASS (all 1160
   entries), 8 spot round-trips PASS (incl. expert fp8 slab with
   scales). Exit 0.
2. NEW independent tool tools/glm5_next_oproj_verify.py — does NOT
   import the packer's produce closures (the built-in verifier proves
   pack==packer; this proves packer==checkpoint for the families where
   a closure bug would be invisible to round-trip). Own safetensors
   reader, own e4m3 decode (unit-tested incl. subnormal/-0/nan), own
   bf16 RNE cast, own column/row/fusion expectations. On rank 0 AND
   rank 15 (both shard boundaries):
   - all 45 o_proj entries exact (34 KDA BF16 [4096x512] raw slices;
     11 DSA F8_E4M3 [4096x1024] bit-exact through independent
     dequant+RNE — the packer's dequant is exactly the documented
     math)
   - fused q|k|v|beta per-section row slices exact on L0+L17 (the
     past per-section-slicing bug class)
   - dt_bias [8192] (512/rank) + A_log [64] (4/rank) f32 slices exact
   - kv_b key TRANSPOSED [64x512x256] + value [64x256x512] exact on
     L3+L43 (the layout-transform class)
   - embedding + lm_head row shards [9680x4096] exact
   Rank 0 exit 0 (all lines PASS exact, zero rounding notes). Rank 15:
   o_proj+qkv/f32/head PASS exact; kv_b leg rerun detached after a
   cold-cache timeout (log /tmp/g5m5_rank15.log on sparkf) — result
   appended below when complete.

Blind spots that remain (round-trip-only or unchecked): indexer set
(kinds 13-17, 43-44), HC tensors (37-42), router (20-21), dense/shared/
expert MoE slabs (18/19/22-25 — expert byte semantics documented in
glm52-packs-2026-08-27.md), decay up/gate_up/conv (28-32), MTP (45-48).
The o_proj/kv_b/fusion families were chosen because they carry the
pack's actual bug history.

### Rank 15 completion (final)

The detached rerun finished: full sweep PASS — all 45 o_proj exact,
qkv_beta/dt_bias/A_log exact, kv_b key_t [64x512x256] + value
[64x256x512] exact on L3+L43, embedding/lm_head [9680x4096] exact;
`families-verify rank 15: failures=0 rounding-notes=0`, exit=0 (log
/tmp/g5m5_rank15.log on sparkf). The earlier timeout was cold-cache
slowness only. **Both boundary ranks of the TP16 set are now verified
exact by independent checkpoint-semantics expectations.**

Staging note: ~/g5m5-verify on spark0/sparkf holds the two verify tools
(+ the packer import for the built-in verifier); harmless to leave,
safe to rm. PR: sparkpipe/sparkpipe#755.

## Tick 5 — queue is SPLIT-BRAIN (fleet-wide flag); M5 stage task queued on the live side

- The single running dispatcher (PID 21845, 5s loop since Aug 29
  19:25) reads /Users/mac/.zcode/workspace/default/sparkpipe/runs/
  queue.jsonl (the coordinator checkout — it merged PR #756 there).
  /Users/mac/sparkpipe/runs/queue.jsonl is a SECOND queue no
  dispatcher reads. Stranded there as of this tick:
  glm53full-bf16-publish (priority 0!) and qwen38max-tp16-regression.
  The qwen38flash lane added qwen38flash-v4verify4 to the LIVE side
  (ps receipt). Lanes must run spark_queue.py from the coordinator
  checkout (or the dispatcher must be pointed at one canonical runs/) —
  coordinator ruling needed; flagged via a note task on the live side.
- Landed tools/glm53_m5_stage.sh (M5 prerequisite, spark0-only):
  clone lane/glm53 -> cache-drop -> host build -> module publish with
  the GPU validator on the real rank0 pack (fp8, pinned
  revision/contract sha) -> driver compile (cudart/stdc++ link args)
  -> adapter + binaries into the runtime root -> fleet deploy of the
  three lib/*.so with timestamped backups -> registrar re-stage ->
  sha-verify 16/16. Ends STAGE-DONE / fails loud per gate.
- Queued on the LIVE side: glm53-m5-stage (spark0, priority 5,
  ttl 45) + the split-brain note. The all-16 M5 cell itself still
  waits for an exclusive window (k3finish holds).

## Tick 5b — M5 PREREQUISITE DONE: current build deployed 16/16 (queue receipts)

- glm53-m5-stage dispatched on the live queue (spark0): clone
  lane/glm53 -> host build -> cache-drop -> module publish VALIDATOR
  PASS 0 failures (artifact 8b1b94f5..., determinism bit-exact,
  contract sha 85040258... baked) -> driver compiled into the runtime
  root (model_sha256 a40e9ec5...). Log: /tmp/sparkqueue-glm53-m5-stage.log
  on spark0. Script then tripped on its own two path bugs (adapter
  builds under build/modules/.../fp8/; driver installs in place) —
  fixed in-tree, resume staged separately.
- glm53-m5-stage2: driver 571cccd7... + adapter 216b55e2... deployed
  with pre-m5-<ts> backups, registrar re-staged, sha-verified 16/16,
  STAGE2-DONE exit 0 (log /tmp/sparkqueue-glm53-m5-stage2.log).
- glm53-m5-window-request note queued: next all-16 exclusive window
  for the M5 cell (k3finish holds too — coordinator arbitrates).
- Correction to tick 5's split-brain read: the coordinator's own
  glm53full tasks ran on the LIVE queue all along (results.jsonl:
  publish1-4 exit 128/128/2/0, isoload exit 2 — all by coordinator).
  The stranded copies in /Users/mac/sparkpipe/runs/ remain a real
  hazard for lanes staging from that checkout; the note stands.

## Operator-directed bring-up (direct spark usage) — SERVING ALIVE + first numbers

Root-cause chain, each proven by A/B on live ranks:
1. Adapter TARGET_MISMATCH (status 11): my stage built the adapter with
   CONTRACT_SHA256=sha(model_contracts/...json)=85040258... but the
   driver descriptor carries model_description_sha256=sha(firmware
   json)=a40e9ec5... — the adapter's strcmp pair needs the FIRMWARE
   json sha. Rebuilt adapter with a40e9ec5... (194dc9ef...) -> probe
   passed adapter init. Deployed 16/16.
2. Batch client CAPACITY_EXCEEDED (status 2): batch json
   max_prefill_rows_per_submission=64 vs deployment
   runtime_limits.max_input_rows=16. (My first enum reading was off:
   2=CAPACITY_EXCEEDED.) Client debug builds live only on spark0
   (uncommitted instrumentation).
3. Control endpoint: daemon is SINGLE-CLIENT and the wave's api holds
   the session — direct batch client HELLO stalls 120s. Use the api
   (/v1/completions with prompt_token_ids — no tokenizer needed).
4. stage.json execution_row_capacity must stay 16: 64 => schema_error
   at adapter_initialize (all ranks, wave3). deployment
   max_input_rows=64 alone => api sends 64-row frames => module
   chainfail status 4 (the adapter/module pair caps at 16 until
   rebuilt with EXECUTION_ROW_CAPACITY=64). Reverted to 16/16;
   wave5 16/16 ready.

SERVING RECEIPTS (temperature 0, deterministic):
- smoke (32-prompt, 8 out): tokens [198,220,374,198,198,220,198,220]
  status 0 — FIRST END-TO-END TOKENS on the current build (emit fix
  live).
- decode probes, 16-token prompt, 1 prefill submission:
  T1 (max_tokens=1) wall 1.252s; T257 (257 out) wall 21.422s.
  => DECODE 256/(21.422-1.252) = 12.69 tok/s (79 ms/token) at B1,
  exact-16-context. M5 brief expectation ~35-45: gap ~3x = the
  hill-climb target. TP fabric: 1.84M collective completions ALL
  status 0 during the 32K prefill attempt.
- exact-32K cell NOT completed: prefill at 16-row submissions is
  ~3.2 tok/s (312 submissions in ~26min, ~5s each — latency-bound
  per-submission; curl 1800s cap hit at ~20%). Hill-climb items:
  (a) raise execution width: rebuild module+adapter with
  EXECUTION_ROW_CAPACITY=64 AND set stage.json=64 (wave3 schema
  failure at 64 says the current pair rejects it — needs the
  parameterized rebuild), (b) per-submission latency (45 layers x
  host-RDMA collectives ~13ms each), (c) decode step 79ms breakdown.
- Wave left SERVING (16/16 ready, api on spark0:8433) for the next
  wake to continue the climb.

## Wake (15-min timer) — row-width ROOT CAUSE found; spark8 WEDGED (operator ask)

- ROOT CAUSE of the 64-wide schema_error: adapter init requires
  execution_row_capacity (stage.json) <= resident_sequence_capacity
  (deployment runtime_limits) — spark_glm5_next_serving_adapter.c
  ~line 797. Trial D on spark5 (max_input_rows=64,
  resident_sequence_capacity=64, max_active_sequences=64,
  stage.json execution_row_capacity=64) PASSED the schema check and
  reached the transport listen (only EADDRINUSE vs the live serving
  daemon — expected).
- Applied the 64-wide config 16/16; wave6 + wave7 both hit READY
  TIMEOUT at the RDMA mesh: every rank fabric_ready then busy at the
  180s peer wait — because SPARK8 IS WEDGED (100% ICMP loss, ssh
  banner timeout — the known spark8-class wedge; fleet dashboard
  15/16 reachable, only spark8 dark). OPERATOR: spark8 needs a
  power-cycle (guide: power-cycles are the operator's).
- Fleet otherwise clean: 0 stray residentds on all sampled nodes;
  spark5/9 re-verified reachable post-transient.
- The 64-wide config is staged on ALL 16 runtime roots — the moment
  spark8 returns, `wave full` + decode probes + the 32K prefill
  re-measure resume (expect the ~4x prefill-width win if
  per-submission fixed cost amortizes).
- 64-wide decode note: width does not change B1 decode (1 row/step);
  the 12.69 tok/s decode baseline stands; next decode levers are the
  79ms step breakdown (collective latency x 45 layers).

## Wake 2 (15-min timer) — spark8 back; 64-wide characterized; long-context failure reproducible

- spark8 power-cycled by operator (came back up 1-min fresh); wave8
  with the 64-wide config: 16/16 READY, api healthy.
- 64-wide SHORT-context serving VERIFIED: smoke deterministic; 64-row
  single-submission prefill (T1) 4.766s = 74.5ms/row; decode
  (T257−T1) 21.0s/256 = 12.2 tok/s — matches the 12.69 baseline
  (decode is width-independent, as predicted).
- KEY NEGATIVE RESULT: per-row prefill cost is WIDTH-INVARIANT
  (~75ms/row at 16-wide and 64-wide short context). The per-submission
  amortization hypothesis is DEAD — prefill is bound by per-row/per-
  layer cost (attention+collectives), not submission round-trips.
  The prefill lever is per-row cost, not row width.
- 64-wide LONG-context (exact-32K) prefill: REPRODUCIBLE failure 2/2
  (30.3s and <90s in): first-heavy-frame transport io_error (status 4,
  reason 6), rank daemon self-exits (model_residentd_exit), wave
  collapses. Short frames fine; long REQUESTS die. Prime suspect:
  KV pool sizing/partitioning at 64 resident lanes (8GiB backing ÷
  64 lanes) vs a 32K-token sequence demand; reason=6 enum TBD. This
  is the next real bug — root-cause BEFORE any long-context claims.
- Failed fast: rolled back to 16-wide on all 16, wave10 16/16 ready,
  T257 decode verified serving again (25.27s wall incl. 4-submission
  prefill ≈ 12.6 tok/s decode — baseline intact).
- STATE: serving UP at 16-wide (api spark0:8433). Next: root-cause
  the 64-wide long-context failure (KV sizing), then decode-step
  breakdown (79ms).

## Wake 3 — spark0 WEDGED (operator power-cycle ask #2); fleet otherwise stable

- spark0: 100% ICMP loss + ssh banner timeout — the same
  spark8-class wedge. Dashboard 15/16 (spark0 sample stale 136s+).
  The other 15 nodes are up with wave10 daemons alive (mesh idle,
  serving down — api lived on spark0). No strays, nothing killed.
- Pattern note for the operator: second wedge in ~90 min (spark8,
  now spark0). spark0 wedged while IDLE-serving (no active
  generation for ~20min), so my cell load is not the obvious cause —
  this matches the pre-existing fleet instability in the coordinator
  log (spark8 and spark5 both wedged before this session). Operator:
  spark0 needs the power-cycle; consider a fleet-wide wedge sweep.
- Wake 3 planned work (mine the wave8/9 prev logs for the 64-wide
  long-context failure signatures: frames-before-death, failing
  layer/ordinal) is BLOCKED until spark0 returns — the logs live in
  spark0:$HOME. Next wake: read prev logs, then the threshold sweep
  (512/2048/8192-token probes at 64-wide) to separate "fixed ~30s
  collective timeout" from "KV-growth" failure modes.
- PRs: #755 still 0 comments; nothing to serve.
- STATE: serving DOWN (rank0 dark); 15 daemons idle-clean; 64-wide
  configs still staged on all 16 (16-wide was the last live config).

## Wake 4 — offline: cell task fixed to the proven path; decode-latency breakdown

- spark0 still DARK (no change); #755 still 0 comments. Offline work:
- tools/glm53_m5_cell_task.sh rewritten to the PROVEN flow: batch
  generation with max_prefill_rows_per_submission=16 (new --rows flag
  in glm5_next_m5_batch.py; must be <= deployment max_input_rows),
  request staged as api JSON (prompt_token_ids; the daemon is
  single-client so the batch client path is dead), 8 sequential api
  calls with token-count + sha256 receipts, telemetry sampling spread
  to 30/90/180/300s (32K prefill runs many minutes). Generator proof:
  rows=16 respected, exact 32768 positions.
- Decode-latency structure (from measurements, no fleet needed):
  decode step 79ms over 45 layers ~= 90 TP collectives/step (attn +
  mlp reduces) => ~0.88ms per small-message host-RDMA collective.
  To reach the 35-45 tok/s class (22-28ms/step) the step needs ~3x
  less collective latency: fewer/fused reduces per layer, one-shot
  algorithms, or overlap — concrete hill-climb program, next up once
  the fleet is back.
- STATE: spark0 dark (power-cycle owed); 15 daemons idle; serving
  DOWN; cell task ready to fire post-recovery.

## Wake 5 — spark0 back; API-CHUNKING DISCOVERY; TRUE 32K prefill in flight

- spark0 power-cycled (2nd recovery); wave11 16/16 ready at 16-wide.
- **API CHUNK-PER-SEQUENCE DISCOVERY (from the preserved wave8 log):**
  the api's long-prompt path splits a 32K prompt into INDEPENDENT
  64-token sequences — every frame logs `pos 0`, a NEW seq_id, and
  emits its own token (req 1..25...). The model NEVER sees a coherent
  32K context through the api; all prior "exact-32K via api" numbers
  measured 509 independent 64-token prefills. It also crashes the
  daemon after ~25 sequences (silent exit, no error line — slot/leak
  bug at the 16-25 active-slot capacity). BOTH are real defects in
  the api long-prompt path (shared code — coordinator scope).
- Honest 32K path = batch client as ONE sequence. The daemon's single
  control session blocked re-attach after the api was TERMed (instant
  status 4) -> bounced the wave WITHOUT the api (wave12 `start`).
  Batch client NOW RUNNING: 83% gpu, 40+ frames, token events
  flowing. T1 (prefill-only) wall + decode run land next wake.
- Note: the api-based T1/T256/chain measurements from earlier wakes
  are VOID (wrong semantics); decode-at-short-context numbers
  (12.2-12.69 tok/s) remain valid.

## Wake 6 — TRUE 32K prefill pacing measured; ETA ~49min

- Batch client healthy: 85% gpu, 177 frames (2832 tokens) at check
  time. Pacing measured over a 120s window: 75 frames/2min = 10.0
  tok/s prefill (1.6s per 16-row frame). ETA ~49 min for 32512, then
  256 decode steps (~20s) complete the cell.
- Comparison so far: batch-client single-sequence 10 tok/s vs
  api-chunked 3.2 tok/s — the api per-chunk overhead was real (3x).
  Per-frame cost grows with context (attention); watch the pace
  degrade in later polls.
- Results (prefill tok/s at exact-32K + the 256-token decode receipt
  + token hash) land next wake. No other fleet action; #755 quiet.

## Wake 7 — one-liner: true 32K prefill steady at 9.9 tok/s (frame 294/2032, ~4.7K tokens in, no context-length degradation yet), ETA ~46min; #755 quiet.
## Wake 8 — one-liner: prefill steady 9.9 tok/s (frame 410/2032, ~6.5K in), ETA ~43min; #755 quiet.

## Operator decision: 32K prefill run KILLED; reorientation to decode latency

- Operator: hours-long 32K prefill cells don't make sense at 10 tok/s.
  Batch client TERMed at ~60% (frame ~1100/2032); wave stop ran — all
  16 nodes verified 0 residentds (clean handoff, no strays).
- Standing answers to the operator's questions:
  ACCURATE inference (packs byte-exact vs checkpoint via independent
  CPU verification; module GPU-validated bit-exact; deterministic
  serving tokens). OUTPUT SPEED: 12.69 tok/s B1 at short context
  (79 ms/token), vs 35-45 target; 32K-context decode unknown (needs
  the prefill tax paid once — not worth it at current speed).
- HILL CLIMB REORIENTED to decode latency (short-context, seconds per
  iteration): 79 ms/step = 90 host-RDMA collectives (~0.88 ms each)
  + ~3 ms compute. Levers: (1) fuse attn+mlp reduces per layer (90→45
  collective rounds), (2) one-shot/small-message collective algo
  tuning, (3) overlap collectives with compute. Each experiment is
  seconds-long at B1/short-context; verified wins => immediate PR.
- Also owed to the coordinator (shared api code): chunk-per-sequence
  long-prompt defect + the ~25-sequence daemon crash.

## Operator questions answered: prefill slowness root + the fix playbook

- WHY PREFILL IS FATALLY SLOW: every 16-row frame crosses 45 layers,
  and EVERY layer→layer boundary stages a TP16 collective THROUGH THE
  HOST (transport staging + H2D + sync) — ~0.88ms per collective, 90
  per frame-chain, plus per-frame admission/chain overhead. Decode
  pays the same 79ms/step; prefill multiplies it by 2032 frames.
  The algorithm is ALREADY optimal for TP16 (config: recursive_
  doubling; split-ring/direct-all-to-all are tp4-only in the
  transport). It is NOT a config bug — it is the host-staged
  collective path itself.
- THE LIMITATION IN COMMON CODE (the one to PR): the common adapter
  template + hidden transport only offer host-staged collectives for
  glm5_next. The k3 module LANDED the cure — see docs/K3_PERF.md:
  (a) DEVICE-DIRECT tier (device_collective config object; ONE
  stream-ordered all-reduce of a FUSED buffer per phase, no sync, no
  host staging, no H2D on the hot path), (b) fused per-layer
  segments, (c) per-shape CUDA-graph capture (k3 counted 3.3k
  launches/token on the host path!). Porting that pattern to
  glm5_next (my module write set) is the prefill+decode fix; scope:
  module reduce sites + adapter config, validator-gated. Expected
  from k3's receipts: host-tier→device-tier was the difference
  between their TP4 ceiling and TP16 viability.
- QUICK-TEST DISCIPLINE (operator directive): weights now STAY
  RESIDENT (wave13 relaunching, api up); the experiment loop is the
  30s T257 probe (decode tok/s) + 2048-token prefill probe (~3min).
  No more hour-long cells; 32K re-measure only after the
  device-direct port lands.
- PRs: #755 (verifier+cell task) unchanged, 0 comments. The
  device-direct port will be its own PR with exactness receipts.

## Wake — decode collective accounting + the common-code lead

- Precise per-token accounting (wave13 fresh log, 257-token probe):
  24,024 layer collectives / 257 tokens = ~95/token: 47.6
  REDUCE_ATTENTION (stage 2) + 47.6 REDUCE_MLP (stage 4) + head.
  These two per layer are dependency-forced (MLP consumes the reduced
  attention partial) — fusion of the PAIR is off the table. The
  module already uses stream-ordered device collectives (no host
  staging in the reduce itself). Lever = cost per collective
  (~0.82ms) × count.
- THE COMMON-CODE LEAD: hidden transport (ring/transport/
  tp_device_collective.c) supports tp_degree up to 16 and HAS a
  DIRECT_ALL_TO_ALL route kind — but the common adapter template
  (runtime/serving_adapter_template.c) restricts
  split-ring/direct-all-to-all to tp_degree 4 (single-algorithm
  builds = recursive doubling only). Recursive doubling at TP16 =
  4 sequential RDMA hops per collective; direct all-to-all = 1 wire
  round + local reduce. If d2d works at TP16, ~0.82ms → ~0.3-0.4ms
  => decode step 79 -> ~35-45ms => ~20-25 tok/s. NEXT: bench d2d vs
  doubling at TP16 (tp_device_collective_characterize.cu exists),
  then PR the transport/template extension with the numbers.
- STATE: serving UP (wave13, 16/16, api healthy, weights resident).

## Wake — d2d bench built; run blocked on transport .so vintage (recipe saved)

- Built /tmp/tp4bench on spark0 from current-main transport sources
  (recipe: cc -O2 -std=gnu11 -I. -Iinclude -I/usr/local/cuda/include
  tools/qwen38_tp4_collective_bench.c + tp_device_collective.c
  (+/tmp/tdc.o) + hidden_transport.c + rdma_control.c +
  fabric_topology.c + nccl-stub.o (long Nccl*(){return 0;} for all 7
  nm -u Nccl symbols) + libsparkpipe_{runtime,core}.a -lcudart
  -libverbs; NOTE stubs must be DEFINITIONS not declarations).
- Run BLOCKED: SparkTpDeviceCollectiveProbeMemoryMode returns
  DRIVER_LOAD_ERROR(13) dlopening lib/hidden_transport.so — the
  deployed .so is the Aug-30 04:22 incumbent while the bench carries
  current-main transport expectations (ABI skew).
- NEXT: nvcc-build a fresh libhidden_transport.so from main sources
  into ~/g5m5-src/build, point the bench at it (backend_module_path
  or LD_LIBRARY_PATH order), then: TP4 d2d vs doubling numbers ->
  TP16 d2d port decision -> PR (transport+template) with evidence.
- The bench payload/thresholds: d2d max payload 81920B (the decode
  reduce 4096xbf16=8KB qualifies), split-ring min 655360B.
- STATE: serving UP (wave13, resident weights); #755 quiet.

## Wake — D2D BENCH RESULT: 162µs/collective (5× under serving's 820µs)

- TP4 d2d bench (4 ranks spark0-3, 8KB bf16 reduce, 2000 iters,
  fresh-main transport): **per_op = 162.2µs, all 4 ranks identical,
  clean** — vs ~820µs per collective in TP16 recursive-doubling
  serving. Even granting TP16 d2d 2-3× the TP4 wire time, the
  projection holds: ~300-500µs/collective => decode step ~79 -> ~40ms
  => **~20-25 tok/s (≈2×)**.
- Same-topology doubling@TP4 control failed config validation
  (`create 1` — forced split_ring_min=1 rejected at Create); not
  needed for the decision — serving already gives doubling@TP16=820µs
  in production traffic.
- THE COMMON-CODE PR (next): extend direct_all_to_all routing beyond
  tp4 (DIRECT_ALL_TO_ALL_PEER_COUNT generalization in
  ring/transport/tp_device_collective.c) + lift the template policy
  cap (runtime/serving_adapter_template.c "single-algorithm builds")
  + enable `algorithms: [recursive_doubling, direct_all_to_all]` with
  d2d_max_payload=81920 in the glm5_next deployment configs. Evidence
  attached: this bench + the 95-collectives/token accounting.
- STATE: serving UP (wave13, weights resident); bench artifacts
  /tmp/tp4bench*, /tmp/benchlib on spark0-3.

## Wake — DECISIVE: TP16 direct-all-to-all measures 78µs/collective (10.5× under serving's 820µs)

- 16-rank d2d bench (all 16 sparks, 8KB bf16, 2000 iters, fresh-main
  transport): **per_op = 78.0µs on rank0, run completed cleanly.**
  (Earlier attempt failed on ranks 4-15 missing /tmp/benchlib — .so
  now staged fleet-wide.)
- Comparison: recursive-doubling serving ≈ 820µs/collective vs d2d
  78µs at the SAME topology/fabric/transport generation. Projection:
  decode step 79ms → ~7-10ms collectives + ~3ms compute ⇒ **~70-100
  tok/s B1 decode potential** — comfortably past the 35-45 M5 target
  if the serving path realizes even half of it.
- THE COMMON-CODE PR (coordinator): generalize
  DIRECT_ALL_TO_ALL_PEER_COUNT (3 → tp_degree-1) in
  ring/transport/tp_device_collective.c + lift the
  runtime/serving_adapter_template.c tp4 policy cap + enable
  algorithms [recursive_doubling, direct_all_to_all] with
  d2d_max_payload=81920 in glm5_next deployment configs. Evidence:
  this bench (16 ranks, production fabric) + the 95/token
  accounting. Scope note: the peer-count constant is woven through
  resource masks/bindings (subtle — see the wave-26 RouteBinding
  receipt); needs the coordinator's transport-owner review.
- Interim quick win while the PR is reviewed: NONE available
  config-side (d2d at TP16 is rejected by template policy, not just
  absent from config). The change is transport-common.
- STATE: serving UP (wave13, resident); bench artifacts staged on
  all 16 (/tmp/tp16bench, /tmp/benchlib).

## CORRECTION — the 78µs TP16 d2d number is NOT valid evidence (self-caught)

The bench's combine functions are no-op stubs and the transport's d2d
peer count is the compile-time 3 — the "TP16 d2d" run measured a
4-rank-shaped collective with 12 ranks' data never arriving. Latency
artifacts of an INCORRECT collective. It stands only as proof the
transport CREATES and runs at tp16 config without crashing.

## Honest re-scope of the common-code PR (d2d@TP16 implementation)

What exists: d2d works CORRECTLY at tp4 (162µs/8KB measured — real
evidence of the per-op mechanics). What the PR must BUILD:
- MAX_STEPS 4 → 16 (d2d maps peers onto step rows; 15 peers need 15)
- DIRECT_ALL_TO_ALL_PEER_COUNT 3 → runtime tp_degree-1 (5 runtime
  sites: ResourceMask ~:267, ConfigBindingRouteCount ~:532, op-token
  bound ~:1490, receive loop ~:1891, reservation mask ~:324)
- real combine over 15 partials in the packet/complete paths
- template policy (serving_adapter_template.c) + config policy lift
- tests: test_tp_device_collective.c extensions (tp8/tp16 d2d,
  value-verified combines — no stubs)
Effort: moderate transport engineering, coordinator-review-heavy
(the area's subtlety is documented in the wave-26 receipt). The
162µs@TP4 measurement supports the direction; TP16 latency numbers
exist only after the implementation.
- STATE: serving UP (wave13, resident weights); bench artifacts
  remain staged for the implementation work.

## Wake — d2d@any-degree IMPLEMENTED on branch d2d/tp16-direct-all-to-all (tp4 suite green)

- Branch off current main (worktree /tmp/d2d-tp16 locally,
  /tmp/d2dwip on spark0): header (ABI 12→13, MAX_STEPS 4→16,
  RANK_COUNT=MAX_DEGREE, d2d MAX_PEERS=15), runtime peer count
  (op field + tp_degree-1 at Create via binding_route_count), 5
  runtime sites converted, XOR peer map generalized (+1 route),
  NULL-tail combine convention for adapters, Create/CreditBinding
  validation generalized (tp4 tp_degree==4 d2d equality removed).
- **Existing suite green: `tp_device_collective: ok`** (tp4
  behavior-preserving regression passed).
- REMAINING for the PR: (1) value-verified tp8/tp16 d2d tests
  (current tests are tp4; the combines must be exercised over 15
  real partials), (2) template policy lift
  (runtime/serving_adapter_template.c single-algorithm rule),
  (3) glm5_next deployment config algorithms+thresholds,
  (4) offline-gates, then PR to the coordinator.
- STATE: serving UP (wave13, resident); branch pushed
  (d2d/tp16-direct-all-to-all @ 9880386).

## Wake — d2d PR progress: module combine + configs landed on the branch

- Branch @ 38c0156 (+ d085906): glm5_next configs (16 stage files)
  now carry algorithms [recursive_doubling, direct_all_to_all] with
  d2d_max_payload 81920; template policy accepts the pair; module
  gained CombineDirectBf16 — rank-ordered NULL-tail fold (deterministic
  global-rank order on every rank => bitwise-identical sums) —
  registered for BOTH the main and HC-twin collectives.
- NEXT (merge gate): value-verified tp16 d2d test through the test
  harness (cuda stub), then offline-gates, then the PR.
- STATE: serving UP (wave13, resident); #755 quiet.

## Wake — test-harness scoping for the value-verified tp16 d2d test

- tests/test_tp_device_collective.c is tp4-HARDWIRED (buffers
  [TEST_DIRECT_ROUTE_COUNT=3], combine asserts exact pointer
  identities for the rank-1 tp4 topology, TEST_STEP_COUNT=2).
  Extending it in place = high-risk surgery; instead ADD a self-
  contained TestDirectAllToAll16 (own 16-rank buffers, 15-partial
  combine, known-value fold assert) — new code, zero touch on the
  tp4 tests.
- Harness facts for the authoring: collectives run in-process against
  the fixture backend (tests/fixtures/tp_device_collective_module.c,
  SPARK_TEST_TP_DEVICE_COLLECTIVE_MODULE_PATH); TestCreate/
  TestSubmitAndWait/TestCompletionState are the flow helpers; the
  topology is applied via SparkTpDeviceCollectiveApplyTopology with
  named hosts; credit_binding_count must be routes×credits.
- STATE: serving UP (wave13, resident); d2d branch @38c0156 pushed;
  next wake authors TestDirectAllToAll16, runs it + the tp4 suite,
  then offline-gates and the PR.

## Wake — tp16 d2d test reaches Create; blocked on fixture register dedup

- TestDirectAllToAll16 now RUNS: Create OK with degree=16, mask=5,
  binding_route_count=15 (all my transport edits verified live).
- BLOCKED at the register metric: 12 vs expected 60. Root: the FIXTURE
  transport module dedups registrations by credit_index (registered
  [credit]) and rejects reused credits — 15 d2d routes share credits
  0-3, so post-route-0 registrations fail and the metric stalls. The
  fixture is tp4-shaped; real RDMA registration is per-route-queue.
- FIX (next wake): fixture register must key on (sender, step_index/
  route, credit) — i.e., per-route state arrays, not per-credit. Then
  the fold assert (16×value) runs and the value gate closes.
- DBG instrumentation left in /tmp/d2dwip (uncommitted there,
  committed here via the WIP commit on the branch).
- STATE: serving UP (wave13); #755 quiet; PR blocked only on this test.

## Wake — d2d register-collision hunt: resume point pinned

- Symptom (TestDirectAllToAll16): Create OK, brc=15, but REGISTER
  metric = 12 (3 routes × 4 credits) — registrations for routes ≥3
  rejected by the fixture's per-credit dedup, implying the transport
  passes step_index ∈ {0,1,2} cyclically for d2d route names.
- Test16DirectBindings carry step_index = route_index (0..14,
  distinct ✓). The transport's registration loop (Create ~:3130,
  binding_index < credit_binding_count) copies my bindings verbatim;
  the ROUTE NAME is built by SparkTpDeviceCollectiveBuildEndpoint
  (~:726) with step_index from ITS caller. Next action: grep the
  BuildEndpoint caller in the Create registration path — the
  step_index argument there still derives from a 3-per-group constant
  or route_count; convert it to binding->step_index / runtime peer
  count. Then fixture dedup never triggers (distinct steps 0..14).
- STATE: serving UP (wave13, resident); branch @4fd0fca + local WIP;
  #755 quiet.

- Resume refinement: the doubling session-opener (:895–930, partner
  = 2^step, masked ^3 for tp4 split-ring) is FOUND; the d2d route
  registration flows elsewhere — next wake reads the d2d register
  path (fixtures reject reused credits, so the d2d step numbering is
  the bug), fixes it, then the fold assert + gates + PR.

## Wake — PR #760 FILED: d2d@TP16 transport change (the decode 2× lever)

- Clean single-commit branch d2d/tp16-direct-all-to-all @ b41bc8b
  (debug instrumentation stripped, verified green on spark0:
  test_tp_device_collective ok).
- PR body carries: the why (95 collectives/token × ~820µs = 79ms step;
  per-hop software cost ~205µs vs ~33µs wire), the change map, the
  cross-model impact statement (doubling behavior unchanged when d2d
  absent; ABI 12→13 combine convention), bench evidence (162µs TP4
  d2d / 78µs TP16-shaped / 820µs serving), the value-verified
  TestDirectAllToAll16, and the three pre-existing main gate failures
  (code-size ceiling, 27b stub shadowing, math-kernels missing
  source) flagged as coordinator items.
- Projection if serving realizes half the bench delta: ~20-25 tok/s
  decode. After coordinator merge: rebuild driver/adapter, redeploy
  16/16, T257 EXACTNESS FIRST (token hash vs the d2d-off baseline
  hash), then timing pair.
- STATE: serving UP (wave13, resident, d2d-off config); PR #760 +
  #755 open; #755 quiet.

## SOTA on GB10 sparks (operator-directed research): NCCL CPU-path, ~roofline on 2 nodes

- GB10 unified-memory reality: GPUDirect RDMA is NOT available — the
  mlx5 driver lacks mlx5dv_reg_dmabuf_mr / data-direct sysfs symbols
  (NCCL issue #2213, dual-Spark, 200GbE RoCE CX-7). NCCL therefore
  runs its CPU-path transport. On coherent unified memory this is NOT
  a copy penalty: staging buffers in system memory are GPU-visible in
  place (C2C), and measured 2-node NCCL throughput is ~45+ GB/s
  bidirectional ≈ 90% of the 400G roofline (orthosystem review).
- SOTA build: NCCL 2.28.9+cuda13.0 aarch64 (issue #2213's config;
  pip nvidia-nccl-cu13 line or source build). Known-open bug: SIGSEGV
  in ncclLocalOpAppend after ~1.1M collectives on 9.4M-element
  allreduces + a 30-min watchdog timeout — watch it, our 8KB ops are
  far from that shape.
- Implications for glm5_next: (1) acquiring/building NCCL 2.28.x on
  a spark (source build, CUDA 13 present) unlocks the vLLM/SGLang
  path — config-level experiment after that; (2) our mapped-host
  tier is architecturally ALIGNED with unified memory (GPU touches
  registered system buffers coherently) — the 205µs/hop is transport
  software (credits/staging/sync), not a fundamental copy; (3) the
  d2d PR #760 remains valid and complementary (1 hop vs 4).
- NEXT: build NCCL 2.28.x from source on spark5 (CUDA 13, aarch64),
  smoke 2-rank allreduce across spark5-6, then wire the glm5_next
  NCCL backend experiment. spark0 stays untouched per operator.

## NCCL ON SPARKS: BUILT + VERIFIED — 21.4µs/cross-node allreduce (38× our serving path)

- Built NCCL 2.28.9+cuda13.0 from source on spark5 (aarch64, CUDA 13,
  clone NVIDIA/nccl tag v2.28.9-1, make -j src.build — note: capped
  at -j16 per operator; build clean).
- 2-node smoke (spark5 rank0 + spark6 rank1, 8KB bf16 allreduce,
  1000 iters, real RoCE fabric): **per_op = 21.4µs both ranks;
  value verified (1+2=3).** Transport notes: NCCL_SOCKET_IFNAME=
  enp1s0f1np1 + NCCL_IB_HCA=rocep1s0f1 + NCCL_IB_GID_INDEX=3 pinned
  (NCCL initially picked the wrong port 10.10.200.x and timed out).
- Comparison: 21.4µs (NCCL d2d-shaped) vs ~820µs (our recursive-
  doubling 4-hop serving path) = **38×**. Projected TP16 serving:
  95 collectives × ~20-30µs ≈ 2-3ms + compute ⇒ decode ~150-200
  tok/s class. The fabric was never the constraint.
- Artifacts: nccl-src on spark5 (+lib/headers staged to spark6);
  smoke binary + launcher scripts with the pinned env; id-file
  bootstrap pattern (rank0 writes /tmp/nccl_id.bin, relay to peers).
- NEXT: stage libnccl fleet-wide, wire glm5_next tp_collective
  backend=nccl (config-level), rebuild if the transport needs nccl
  linked, wave restart, T257 EXACTNESS then timing.

## Wake — TP16 NCCL smoke: staging done, launch needs the queue (manual fan-out too fragile)

- NCCL 2.28.9 built + verified at TP2 (21.4µs). The TP16 scale-up hit
  launch choreography failures: parallel ssh from the mac trips
  connection resets; backgrounded remote children hold ssh pipes;
  per-node id/rank staging needs the id relayed. A manual fan-out
  script on spark5 wedged the same way.
- STAGED on all 16 nodes: /tmp/tp16bench_nccl (+ .so), libnccl at
  ~/nccl-src/build/lib, nccl_smoke binary, per-node runner scripts.
- NEXT (use the QUEUE — it natively fans out to N nodes simultaneously):
  (1) generate the NCCL unique id ONCE on spark5 (rank0's host),
  (2) scp it to all 16 as /tmp/nccl_id.bin,
  (3) per-node runner scripts deriving rank from hostname (sparkX →
      hex index), id-file bootstrap,
  (4) queue task --nodes all16 --cmd runner → all ranks start
      together (the queue's same-second dispatch = what the wave
      script does),
  (5) collect per-op µs = the TP16 NCCL number for the serving
      backend experiment.
- Also recorded: 2-node verified 21.4µs stands; #760/#755 unchanged.

## Wake — queue dispatcher DIED and was restarted; spark4 NCCL-init segfault (node anomaly)

- THE QUEUE DISPATCHER LOOP WAS DEAD (0 processes; the coordinator
  session that hosted it had ended — stranded every queued task). I
  restarted it detached (same 5s loop, coordinator checkout). NOTE FOR
  COORDINATOR: the dispatcher now runs from my session — please re-home
  it to a coordinator-owned process.
- Stale reservation `qwen38flash-shamatch2` on spark4 (TTL expired 14h,
  pid null) was blocking dispatch — released via `spark_queue.py release`
  (the reaper skips pid-null reservations: defect for the coordinator).
- spark4 NCCL anomaly: ncclGetUniqueId SEGFAULTS on spark4 with a
  binary+libnccl that are md5-identical to the working spark5 pair
  (direct run, not queue-related). Third node anomaly today
  (spark8 wedge, spark0 wedge+recovery, spark4 segfault).
- TP16 NCCL smoke: BLOCKED by spark4 (rank0 host). Options: (a)
  diagnose spark4, (b) 15-node smoke, (c) skip straight to the
  serving-experiment (backend=nccl) which answers the real question
  end-to-end. Staging is complete on all 16.

## Wake — NCCL serving attempt failed (module_not_validated); reverted; bisect queued

- backend=nccl config transform (16/16) + libnccl staged + wave14:
  every rank fails LoadDriver rc=8 MODULE_NOT_VALIDATED at
  adapter_initialize. Reverted configs to hidden_transport (16/16):
  wave15 STILL fails identically — the config transform was NOT the
  trigger. Remaining wave14 deltas to bisect: (a) libnccl.so.2
  present in lib/, (b) NCCL_* env vars in the wave launcher
  (node-local edit), (c) module-record state — the runtime roots
  carry NO module_library dir (LoadDriver's validation source is
  inside the driver .so; why it flips to not-validated after wave13
  is the open question).
- ALSO: my port-poll patch for the deploy-speed directive has an
  arithmetic bug (multiline grep -c) that aborts stop_wave under
  set -e — needs a proper rewrite (next cycle), plus parallelized
  ready-polling. Current serving: DOWN.
- Deploy-cycle speed directive acknowledged: target ~1-2 min
  (pack page-in ~2 min is the floor); the 120s blind sleeps are gone
  once the poll is fixed.

## Wake — bisect result: nccl deltas ELIMINATED; module_not_validated persists

- Removed BOTH remaining wave14 deltas (libnccl.so.2 moved out of
  lib/ on all 16; NCCL env vars removed from the wave launcher) and
  relaunched (wave16): rank0 STILL module_not_validated at
  adapter_initialize. So the trigger is NONE of: nccl config, libnccl
  presence, NCCL env. Something else changed state after wave13.
- Suspects for next cycle (needs a read of SparkLoadModelDriver's
  module_not_validated path): the module-record resolution (the
  runtime roots have NO build/module_library dir — where does the
  record live?), a time-based validity (records may carry a
  timestamp/expiry), or a dependency of the dlopen chain on a file
  that the nccl experiments disturbed.
- Serving remains DOWN; wave16 failed. The wave script's port-poll
  fix is in and worked (stop phase clean, no 120s sleeps).
- STATE: serving DOWN; configs/lib/env at the wave13-equivalent
  state; bisect eliminated nccl deltas; investigation queued.

## Wake — serving-restore blocked at dlopen; one print away from the answer

- Current serving blocker (single-rank probe, spark5): LoadDriver
  rc=13 DRIVER_LOAD_ERROR — the dlopen of lib/model_driver.so fails
  (RTLD_NOW), deps all resolve (ldd clean, libcudart13/libcuda/
  stdc++ present), file intact (2.4MB). It does NOT depend on
  libnccl presence (fails identically with/without).
- THE NEXT ACTION (5 min): the adapter's LoadDriver fills a 512-byte
  error_buffer from SparkLoadModelDriver and never prints it. Add
  `fprintf(stderr,"DBG load: %s\n",error_buffer);` after the call in
  spark_glm5_next_serving_adapter.c (line ~718), rebuild the adapter
  (recipe proven), redeploy to one node, run the probe — dlerror will
  name the exact cause. (Likely: a symbol/ABI detail from the d2d-era
  compile interacting with the runtime-root's older module artifacts.)
- Configs are at the clean hidden-transport state on all 16; the
  nccl/lib/env deltas are parked (libnccl at ~/nccl-staged + spark5/6
  nccl-src trees).
- STATE: serving DOWN (dlopen blocker); everything staged for a
  5-minute fix once the error text is read.

## Wake — ABI mismatch identified: adapter(13) vs driver(12); coordinated rebuild needed

- The instrumented rebuild (d2d branch source) produced an adapter at
  ABI 13 (my d2d header bump); the deployed driver is ABI 12 →
  LoadDriver rc=10 ABI_MISMATCH. The earlier rc=8
  (module_not_validated) waves were the ABI-12 adapter generation —
  the 12/13 split explains the whole failure family.
- FIX PATH (next cycle): rebuild driver+adapter TOGETHER from the d2d
  branch (needs the module library record copied from spark0's
  g5m5-src build/module_library to spark5's d2dsrc for the compile),
  deploy both .so 16/16, wave, T257 exactness (hash vs the
  hidden-transport baseline), then timing.
- This validates the PR #760 flow: ABI bump forces a coordinated
  rebuild of the full serving stack — exactly why it's a
  coordinator-reviewed change.
- STATE: serving DOWN (ABI split); d2d branch has all code; module
  record on spark0 g5m5-src; nccl lib on spark5/6.

## Wake — serving debug: failure layer moved INSIDE the driver's create

- Adapter instrumented (DBG load). Finding: LoadDriver's dlopen now
  SUCCEEDS (the rc=13 earlier was stale-binary confusion); the 13/8
  comes from `state->driver.interface->create()` — the DRIVER's own
  module-record validation + device backend load, one layer deeper
  than all prior instrumentation.
- Staged on all 16 (15/16 + spark5 local pending): the module library
  ACTIVE RECORD (e919a35b….json, validation_state=passed, ABI 13
  build) at <rr>/build/module_library/active/ + link unit .a in
  link_units/. This record was MISSING in every runtime root — a
  plausible root cause for the whole failure family (the driver's
  create validates against it).
- NEXT: (1) copy the record to spark5's own rr, (2) single-rank probe
  — if the create progresses past record validation, the wave comes
  up; (3) strip the DBG instrumentation from the deployed adapter
  before any long-running serving (cosmetic).
- STATE: serving DOWN; record staged 16/16 (pending spark5 local);
  wave16-era configs (clean hidden transport).
