# Nurse2 sweep 9 (final) + cycle summary — 2026-08-29 21:37–21:47 UTC (lane/nurse2)

Telemetry gate CLOSE: `active_nodes=0 avg_gpu_pct=0.0 busy_gpu_nodes=0
input_tok_s=0.0 reachable=16` @21:37Z — true state (only storage pushes
and builds ran this cycle; the two compute tasks both ghosted).

## ESCALATE (standing at cycle end — coordinator owns these)

1. **wave3 residentd survivors STILL on the fleet** (verified spark9
   rank9 pid 2317937, sparkc rank12 pid 4188297 at 21:15Z; counts of
   1 residentd on all 8 t9 nodes at 21:30Z; sparka showed 2 stacked).
   TERM-phase failed. **g5dsa-wave4 (p0, all 16) is now queued behind
   t9 and will hit these survivors on the shared hosts** — clean them
   BEFORE wave4 dispatches.
2. **Ghost-work class is repeating**: t8 hung ~16 min in `phase A:
   threshold-0 O128` (0% GPU on all 8 held nodes) then exit 1 at
   21:31:10Z; successor **t9 entered the IDENTICAL phase A at
   21:34:13Z** with survivors still in place on spark8/9/c. If t9
   hangs past ~21:45Z it is the same ghost; the blocker is almost
   certainly the survivors, not the r3flash driver (its "fleet
   firmware" rebuild likely won't matter).
3. **"pid gone without exit file" x2** (t2 19:48Z, q27b-build-1
   21:00Z) — dispatch-layer bug, queue-infra owned.

## Correction to the pass2 final gate (verified, load-bearing)

The documented coordinator-visible check — "all 16 ranks at exactly
98,019,368,448 B" — is UNREACHABLE: rank0 on disk after its pass2
re-push = **98,019,454,976 B = spec + 86,528**. The 86,528 B delta is
the same figure the earlier lane diagnosed as the spec mismatch that
forces the full 16-rank re-push. Use **98,019,454,976 B** as the
per-rank settled size (verify all 16), and expect
`PLACE-PASS2-DONE rc=0` as the completion line (ranks 0-12 re-pushed
by 21:36Z; 13-15 remaining; pace ~4-5 min/rank; ETA ~21:55Z —
successor nurse should verify).

## Cycle summary (9 sweeps, 20:05-21:47Z)

- **Node alarms: all resolved.** spark8 power-cycled (driver-wedge
  ended, ssh+nvml healthy); spark4's D-state residentd gone with its
  reboot; spark3 was never truly down. 16/16 reporting all cycle,
  error_streak=0 throughout.
- **Dispatcher: UP every sweep** (wrapper pid 21845) — no restart ever
  owed; all blocks/holds/priority-barriers correct, notes skipped.
- **Queue throughput**: smoke4 (fatal, residentd deployment=io_error —
  config exists, invocation bug, owner qwen27b-serve who re-queued
  build-1; pid-gone; build-2 now RUNNING on spark5, build-3 staged);
  g5dsa-wave3 (28-min ghost, exit 1; wave4 queued with start_api
  setsid fix); r3flash t5-t8 exit 1, t9 running; weightd-vmm t4 exit 1
  (receipt STILL missing, owner cell-runner); bf16-replace-verify-true
  rc=0. Every failed task got an owner re-queue — reconciliation loop
  healthy.
- **bf16 pass2** (glm53full): ranks 0-12 re-pushed, PLACE-PASS2-DONE
  ETA ~21:55Z; revalidation rc=0/errors 0 (raced pass2, gate is
  pass2's own per-rank check — with the corrected size above).
- **Lane flags**: false alarms on toksidecar/k3finish WITHDRAWN at
  sweep 5 (my dir-mtime quiet proxy was wrong; newest-dirty-file ages
  were 3 and 0 min — method corrected mid-cycle). All lanes active all
  cycle: glm5dsa (wave4 queued), r3flash (t9), qwen27b-serve
  (build-2), cell-runner, k3finish (staging its 16-rank launch),
  debts, p1d2. Only drywave1 dormant: detached HEAD + unresolved
  merge conflicts (UU PACKAGE_MANIFEST.json, UU SHA256SUMS), ~14h
  quiet — coordinator cleanup call.
- **Nothing killed, cancelled, or TERMed; no source edits; no lanes
  spawned.** One documented re-arm class (dispatcher) monitored, zero
  restarts needed.

## Actions

Read-only + 9 reports (this file + nurse2-2015/2032/2042/2052/2100/
2110/2122/2132). Escalations 1-3 above are the open surface for the
next nurse cycle.
