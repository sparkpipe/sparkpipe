# probe-fix — the o_proj transposition (real, fixed, deployed) + the collective proven innocent + what still degenerates

Lane `lane/probe-fix`, worktree /tmp/lane-probefix, tip `d1235a9` (pushed).
Fleet: spark0..sparkf glm5_next.tp16, api spark0:8433. Every claim =
command + raw output. Reservations: `lane-probefix` on all 16, 90-min
rolling (renewed through the session).

## Chain receipt

1. **Rebased clean on main** (old 843af4d → b59dafa over the 34 new main
   commits), pushed. Re-armed the probe-armed wave with the ×8 window:
   registrar two-phase startup (all 16 `registrar exit status=0`), 16/16
   ready, **zero** `status=4|BUSY|chainfail|io_error` on every rank, rank
   0's L0 deep ladder completed between ordinals, wave reached decode
   steps (10,590 G5N-PROBE lines / 1,012 tp completions on r0). The
   probe-BUSY regression from 843af4d is CLOSED.
2. **THE DISCRIMINATOR (retention-advance @ L34, cold first request,
   consecutive passes).** On the fresh cold wave, canonical curl
   `p=[154819,11,1875,525] max_tokens=8` → `[66188 x8]` (still
   degenerate at that point). Rank-0 log, L34 retention across the 11
   consecutive passes (4 prefill positions + 7 decode steps):

       1  0.991829 0.426630 0.295710 0.778097
       2  0.993333 0.441033 0.280110 0.746621
       3  0.991461 0.441548 0.252736 0.735230
       4  0.991661 0.483174 0.269342 0.769595
       5  0.990643 0.479589 0.245914 0.766321
       6  0.989608 0.513970 0.235881 0.765332
       7  0.988842 0.543282 0.238366 0.770245
       8  0.987858 0.560281 0.235881 0.767307
       9  0.987053 0.572805 0.237536 0.769595
       10 0.985983 0.585174 0.230141 0.765332
       11 0.985206 0.591594 0.235056 0.768616

   **Retention advances normally (no stall, no rewind); state_pre[k+1] ==
   state_post[k] exactly on every consecutive pass.** VERDICT per the
   scoreboard rule: conv-window edge INNOCENT → fused-shape-vs-kernel
   family.
3. **ROOT CAUSE #1 FOUND AND FIXED (2cc9de3).** Independent byte-level
   comparison of rank 1's deployed pack vs the checkpoint
   (/mnt/model-warm/glm-5.3-flash, my own regeneration code, not the
   packer's closures):
   - checkpoint `o_proj.weight` shape = **(4096, 8192)** =
     [hidden OUT, heads·dim IN] — the down-projection family.
   - the packer row-sharded the OUTPUT axis: rank 1's `K_KDA_OUT` entry
     was `[256, 8192]` and byte-equal to `W[256:512, :]`
     (`pack == rows-slice: True`).
   - the module's out-GEMM consumes the TP partial form `[4096, 512]`
     (`K = rank_v/attn_output_columns, N = HIDDEN`): it read the
     transposed blob with a 512-element stride against an 8192-stride
     layout. Bounded garbage entered the residual via EVERY attention
     sublayer on EVERY rank (no rank holds the cols-slice it needs), from
     layer 0 — the cold-first-request degeneration.
   - TP1-invariant (rows- and cols-slices coincide), which is why the M3
     oracle gates passed; the pack verifiers diffed against the packer's
     own contract, so they passed the wrong contract.
   Fix: `ATTN_OUTPUT` + `KDA_OUT` moved from row-sharded to col-sharded
   in the module stagepack policy (`spark_glm5_next_stagepack_format.h`),
   packer flipped to `shard="cols"` for both. Post-fix proof on the NEW
   pack: `K_KDA_OUT L34: rows 4096 cols 512`,
   `pack == cols-slice W[:,512:1024]: True`.
4. **REPACK 16/16** with the fixed packer (`glm53_packs_fixed2`, staging
   dirs /tmp/g5nrepack-src, logs /tmp/packbuild2-r*.log). All 16 receipts
   `1160 tensors, 21706046976 bytes`; header provenance patched
   (contract a40e9ec5…, config bb8f01c4…, recipe a1915eb3…); verifier
   PASS 16/16, uniform `dir_sha 54e2474be88a2544`; deep spot round-trip
   PASS on ranks 0, 1, 8, 15. Rank 8 (spark8) and rank 11 (sparkb) hit
   the slow-ceph path (~300 KB/s) and were REQUEUED solo on spark0/spark9
   and shipped (33 s transfer, sha-verified flow) — the closeout's
   playbook, now twice proven. Swapped: `packs/<name>.g5nsp` →
   `~/glm53_packs_fixed2/...` on 16/16 (old kept at `.pre-probefix2-bak`
   symlinks; original packs still at `~/glm53_packs/`).
5. **Cross-rank invariant (6d3dac4, cb86368).** New diag probe prints the
   POST-REDUCE `attention_out` checksum on EVERY rank. Result on the cold
   request, L0: `bf16sum 2354566 f -0.0165691 -0.017851 -0.0034597
   -0.00152764` — **identical on all 16 ranks**. The TP collective is
   bit-exact; the fleet-wide model state is identical garbage, i.e. the
   remaining defect is RANK-INVARIANT.

## Status: STILL DEGENERATE after the fix — but a different, richer distribution

- Old packs cold: `[66188 x8]`, head scores declining 8.02 → 7.08.
- Fixed packs cold: `[113235 x5, 13765, 94944 x2, 16026 x2, 1617 x7,
  80346 x6]` ("侄侄侄侄侄esis hemat hemat uuid uuidleteleteletelete…");
  real fixture prompt (the 92x's case 1, a 300-token physics MCQ) →
  `alianalianalian…`. Head scores ROSE to 7.5–8.7. The fix changed the
  model's output distribution and raised logit confidence — the repair is
  real — but a repeat attractor remains.

## Where the remaining defect lives (evidence, not chase)

Everything byte/layout-level now audits clean: qkv_beta / convs /
decay-gate-down / decay_up / gate_up / o_proj / shared+dense gate_up+down
/ router / correction bias / indexer / kv_b (replicated, bind-offset per
rank at module line ~334) / embedding+lm_head / HC f32 trio — all
independently regenerated from the checkpoint and byte-compared OK at
rank 1, layers 3 and 34. The collective is bit-exact. The TP1 validator
PASSes (its oracles were written from the same reading of the model as
the module, so they cannot catch a shared semantic misunderstanding).
Next lane's instrument: a reference semantic diff of the
rank-invariant math against fla/vendor `modeling_glm5` — in order of
suspicion: (a) KDA bounded-decay reduction (decay_logit [H·K] + dt_bias
[H·K] → per-head mean-then-softplus × −exp(A_log)) and the
norm↔gate order at the KDA output (module: RMSNorm then sigmoid gate);
(b) the MoE noaux_tc router (sigmoid + frozen bias, select-by-biased /
weight-by-unbiased, renormalize) and the routed-expert grouped GEMM at
fp8; (c) the DSA indexer + rope path at real positions (the validator
exercised rope at position 0). The TP4 bisect (build TP4 packs, 4-node
wave) is the cheap half-step if the semantic diff stalls: identical
failure at TP4 would confirm rank-invariance down to sharding-free math.

## Artifacts / receipts

- Driver (probe + fix, diag builds): `43a5fda4…` deployed 16/16
  (non-probe twin of the same source: `980fd0fcb42b39cb…`).
  Module artifact `01b22fb6…`, validator `glm5_next validator: PASS
  (0 failures)` against the NEW rank-0 pack.
- Packs: 16 × `~/glm53_packs_fixed2/glm5_next_stage.tp16.rank<r>.g5nsp`,
  21,706,046,976 B, 1160 tensors, `dir_sha 54e2474be88a2544` (uniform).
- sha256 repair on `src/spark_sha256.c` (9971daa): current main did NOT
  compile on aarch64 gcc (`vreinterpretq_u8_u32` handed a uint8x16_t;
  appleclang's lax vector conversions masked it). w1-loader sha test
  green on spark0.
- Offline gates green through the tip (build-all, run-tests,
  package-manifest). Ratchet 222168 → 222218 → 222236 → 222250, all
  justified in-commit.
- Fleet left UP: 16/16 ready on the diag (probe-armed) driver + fixed
  packs, api spark0:8433 healthy (`served` counter live). DO NOT run the
  quality gates on this wave: the probe build stalls the chain by design;
  relaunch non-probe (same driver source) for gate runs.

## Integration request

1. Merge `lane/probe-fix` (tip d1235a9): sha256 build fix (unblocks every
   spark build of main), the o_proj sharding policy fix (module), the
   packer flip (tools/), the cross-rank probe + wave tooling, ratchet
   bumps, LAUNCH-STATE refresh.
2. REQUIRED after any future repack: the header provenance patch
   (LAUNCH-STATE carries the command).
3. The header-patch tool should hard-assert the pack file size (it
   patched a mid-build 16.4 GB file happily; caught because the verify
   listed size).
4. Next lane (semantic diff): per the instrument list above; the probe
   harness (`SPARK_GLM5_NEXT_PROBE`) + cross-rank checksum are in the
   tree and ready.
