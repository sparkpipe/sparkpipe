# Nurse2 sweep 6 — 2026-08-29 21:04–21:12 UTC (lane/nurse2)

Telemetry gate: `active_nodes=0 avg_gpu_pct=0.0 input_tok_s=0.0
reachable=16`, zero busy — true state: no compute anywhere.

## ESCALATE

1. **wave3 left residentd SURVIVORS on the fleet** (its notes promised
   "TERM phase cleans the timed-out ranks" — it didn't). Verified:
   spark9 pid 2317937 `sparkpipe_model_residentd --deployment
   model_resident.json --rank-index 9`; sparkc pid 4188297
   `--rank-index 12`. wave3 exited 1 at 21:00:25 without cleaning its
   16 ranks. Coordinator should sweep-TERM ranks 0-15 of this
   deployment BEFORE the next 16-node window (k3finish's launch-ready
   note + any wave retry) — survivors hold memory and will poison
   bring-up. Never mine to TERM (coordinator standing order).
2. **"pid gone without exit file" = 2nd occurrence** — q27b-build-1
   (reaped 21:00:49, exit -1) after weightd-vmm-verify-gpu-t2 (19:48).
   Dispatch-layer bug class losing tasks; owner: coordinator/queue
   infra. qwen27b-serve must re-queue the build (its prerequisite was
   not done).

## 6c reconciliation (all reaped ~21:00:25-56, wave3 ghost died naturally)

- `g5dsa-wave3` exit 1 (21:00:25) — owner glm5-dsa: THIRD consecutive
  wave failure (wave2 overlap-cancel, wave3 silent-at-API 28 min then
  exit 1). Prerequisite for wave4: rank survivors cleaned + the
  silent-at-API stage debugged.
- `r3flash-glm52-exact-cell-t5` exit 1 after ~15s — owner
  r3-flashdecode (fast staging fail; re-queue after fix).
- `weightd-vmm-verify-gpu-t4` exit 1 after ~31s — owner cell-runner
  (t2 pid-gone -> t3 superseded -> t4 failed fast; the verify receipt
  is still missing).
- `q27b-build-1` exit -1 pid-gone — see ESCALATE 2.

## WATCH

- **bf16-replace-verify-true** (coordinator) RUNNING on spark5 since
  21:00:56, lease only spark5; its log 0B (output goes to
  `/home/spark5/glm53full_bf16_revalidate.log`; decision line =
  `REVALIDATE-DONE rc=`). **Sequencing risk**: it validates/verifies
  packs WHILE bf16 pass2 is still pushing (rank 3 -> spark3 placed
  ~21:05, ranks 4-15 to go, ~15min/rank now, ETA ~22:15-22:45Z).
  rc=0 over mid-write packs would repeat the VOID-validate lesson.
- pass2 pace improved to ~15min/rank (rank0 20:03Z -> rank3 ~21:05Z).

## Healthy / verified

- Dispatcher ALIVE; promoted the whole stack correctly the moment
  wave3's exit freed the 16 leases (21:00:25 -> dispatches by 21:00:56).
  Queue now: 1 running (bf16-replace-verify-true) + 3 notes. Idle-node
  alarm N/A (15/16 free but no executable task queued — notes only).
- Nodes 16/16 reporting, error_streak=0. Lanes: all active per sweep 5
  method (newest-dirty-file); drywave1 still the only dormant one.

## Actions

Read-only + this report.
