# glm53full lane report — 2026-08-30 (new model dev session onboarding)

Lane: `lane/glm53full-dev` (worktree /Users/mac/sparkpipe, branch from
main 879fc5a). Scope (operator): GLM 5.3 FULL, all three expert
resolutions — 16-bit (BF16), 8-bit (FP8), 4-bit (NVFP4) — on the glm52
module. This session: onboarding + plan; GPU work waits on the queue.

## Inherited state (verified against main, not re-derived)

- All three pack sets placed fleet-wide and receipted (roadmap
  2026-08-30): nvfp4 526G (33G/rank), fp8 866G (54.14G/rank, THE
  SERVING ARM), bf16 1.568T (98.02G/rank). Contract frozen:
  model_contracts/glm53_full_authoritative.json.
- Prior lane PR #740 was CLOSED 2026-08-29 (was the live 3res lane PR;
  its landed work is on main via the coordinator's merges).
- NVFP4 correctness is banked (GPU validator PASS, routed cosine
  0.99999); nvfp4 node copies are slated for retirement.
- OPEN BLOCKER (bf16 arm): the glm52 module has a static_assert refusal
  of expert codec 1 (BF16 experts). Roadmap Phase 2 makes 16-bit glm
  5.3 an operator-named test target, DEPENDENCY: codec-1 module
  acceptance ("ruled, queued behind the coherence chain"). The prior
  lane's INTEGRATION REQUEST for this stands.

## Plan (priority order)

1. **M1 — codec-1 (BF16 expert) module arm, offline-first.** Implement
   behind existing patterns in modules/glm52_resident_decode_stage +
   host oracle test (THREADS==1 CPU-shim law). Offline gates green by
   exit code. PR tagged as satisfying the roadmap's codec-1
   dependency so the coordinator can rule with code in hand.
2. **M2 — Phase 2 bf16 gate (when the queue is live).** Isolation load
   on ONE node → 8-token inference (weights-loaded-properly bar) →
   16× fleet load. Load_seconds logged. Then nvfp4 retirement is
   unblocked (housekeeping item).
3. **M3 — fp8 serving arm perf hill climb (the standing duty).**
   Exactness verified before timing (kill-switch discipline); cells
   via spark_queue at priority 0 (glm5.3-family directive), TTL
   declared; receipts in this reports/ dir as glm53full-*.md.
4. **M4 — NVFP4 arm maintenance.** Verbatim packer stays in tree;
   re-validate only on source/module change (freeze policy).

## Session notes

- Onboarding only; no GPU, no queue tasks submitted (queue handoff in
  progress per operator). No daemon touched.
- Recurring 5-minute self-timer running for driver progress + PR
  follow-ups + task alignment.

## Tick 1 (05:20Z) — M1 codec-1 module arm LANDED on the branch (commit 061832f)

- Module accepts expert codec 1 end to end: unity.cu/layer.cuh refusals
  dropped (scale pointers optional exactly for BF16), ExpectedShape
  emits the BF16 payload/no-scale-plane contract matching the packer and
  validator mirrors, node context accepts codec 1, Makefile
  EXPERT_CODEC=bf16 -> id 1.
- Host gates green on this Mac: layer host oracle 7 codecs byte-equal
  WITH a new explicit "status moe 0" gate per codec; module contract 7
  codecs; stagepack C shape test (codec-1 payload/scale byte contract);
  bf16 pack passthrough (validator side, untouched, green).
- LmGemmWeightOnlyLaunch is format-generic (bf16 degenerates to the
  dense-path bf16 GEMM); NO shared-code change needed — no INTEGRATION
  REQUEST for runtime/gemm.cuh.
- Gotcha fixed en route: a preprocessor #if over enum codec names sees
  both as 0 — the harness now folds a runtime if on the constant.
- Next: GPU tier-2 validator cell for bf16 packs (queued, priority 0)
  once the queue is live; then the Phase 2 16-bit gate (M2).

## INCIDENT: shared-worktree cross-lane commit (resolved)

- /Users/mac/sparkpipe is shared by concurrent lane agents; a kimi-k3
  session switched HEAD mid-tick and my commit b6f874b landed on
  lane/kimik3-dev and pushed there. Repaired: their branch restored
  local+remote at their 28a4aed (no commits of theirs touched), my work
  cherry-picked to lane/glm53full-dev as 061832f, worktree left on
  their branch.
- Prevention: ALL glm53full work now happens in the dedicated worktree
  /tmp/lane-g53full-dev (branch lane/glm53full-dev). Never commit from
  /Users/mac/sparkpipe.

## Operator task (14:35Z): CPU-verify the bf16 TP16 stagepack — VERDICT: ACCURATE

- Structural: `tools/glm52_validate_pack.py` 16/16 ranks, errors: 0
  (ranks 10-15 named decimal `rank10..15`, uniform fleet-wide — sparka
  holds rank10, sparkf rank15; no hex/decimal mismatch).
- Byte-level vs the promoted source (`/mnt/model-warm/glm-5.3-bf16` on
  spark5, CPU-only): new `tools/glm53full_bf16_tp16_source_verify.py`
  (commit 0152944): experts layers {3,40,77} × 16 ranks × up/gate/down
  sampled + full-256-expert sweep on L3/rank0 — all byte-exact; spine
  (embed/lm_head row shards, q_a, o_proj col shard, router, F32
  correction bias) byte-exact; replicated kinds identical 16/16; the 16
  rank slices reassemble one full expert tensor exactly. RESULT: PASS.
- NO coordinator fix PR needed (the pack is the contract: byte-exact).
  The verifier itself landed as the reusable receipt gate on
  lane/glm53full-dev (PR #749).
- Ops notes: ~/g53full-src on spark5 is contended (something reset the
  checkout twice mid-tick — my staged copies now live in ~/g53full-verify/
  and the node-side bundle fetch is force-ref); queue tasks must submit
  from the CONTROLLER checkout (~/.zcode/workspace/default/sparkpipe),
  not the shared /Users/mac/sparkpipe (my first add landed in an inert
  queue copy; resubmitted).
- glm53full-bf16-publish2 (GPU tier validator, priority 0) still queued
  behind dsv4flash-m1-build4 on spark5.

## Tick 3 (05:41Z) — M1 COMPLETE: bf16 module arm GPU-VALIDATED and PUBLISHED

After two repair cycles (queue must be submitted from the controller
checkout; git refuses to fetch into a checked-out branch — stage the ref
first; and the validator TU's own #error gate needed the codec-1 arm),
`glm53full-bf16-publish4` (priority 0, spark5, via the queue) ran the
full publish+validate:

- glm52_validation tier=dense PASS (cosine 0.99998-0.99999)
- tier=routed PASS — routed_expert_forward cosine 0.99998819,
  routed_layer_forward cosine 0.999975055 (the BF16 expert kernels
  decode and execute)
- tier=dsa PASS (cosine 0.99999)
- PUBLISHED: module=spark.glm52.resident_decode_stage.bf16.expert_bf16.
  h6144.l78.e256.k8.v2 artifact=b2c526c7...f4b1d855
  record=build/module_library/active/4e29e1d6...bc0b69.json (spark5
  ~/g53full-src)
- commit abc9ad8 (validator codec-1 arm + tier2 selftest pins fp8 AND
  bf16); identity pins MODEL_REVISION 304b8051... CONTRACT_SHA256
  3c273ef7... rendered by glm52_model_contract.py --print-build-identity bf16

Combined with the byte-exact pack verification (tick 2), the 16-bit arm
is ready for the Phase 2 fleet gate: isolation load → 8-token inference
→ 16× parallel load (operator's test target). Awaiting coordinator
scheduling for the fleet window; the module artifact + packs are on-node.

## Tick 4 (05:50Z) — M2 prep: TP16 deployment tree generator landed (89bce21)

- tools/glm53full_gen_deployment.py {bf16|fp8|nvfp4}: 16 host configs per
  arm (spark0..sparkf ranks 0-15, pure TP16 single stage per the module's
  compiled STAGE_COUNT=1/LAYERS 78, decimal pack names, exact-member
  schema-v3 stage configs, residentd deployment json). Self-checked.
- Per-arm model_revision cites what each published module carries: bf16
  304b8051 (this lane's artifact b2c526c7), fp8 the legacy 5.2 pin (until
  the re-frozen contract lands — coordinator flag), nvfp4 363e8f08.
- This unblocks the M2 isolation-load cell once the runtime root's
  bin/lib payload source is confirmed (the q27b serve pattern: residentd
  + config + lib per rank; next tick wires the bf16 rank0 isolation load
  as a queued task).
- PR #749: still open, no review yet. Queue idle (3 stale notes).

## Tick 5 (06:05Z) — M2 isolation cell fully specified

Reverse-engineered the serving runtime assembly chain (the part no
in-tree doc names): model_driver.so is linked by
`build/sparkpipe_model_compile --model <firmware json> --stage
glm52_resident_decode_stage --library build/module_library --output
<dir> --cc-arg -lcuda -lcudart ...` (recipe: dsv4's
tools/devcycle/build_remote.sh on spark9, the lanes' known-good path;
module_publish output IS the library the compiler resolves). The bf16
isolation-load cell is therefore fully specified:

1. make build/sparkpipe_model_residentd build/sparkpipe_model_batch
   build/sparkpipe_module_compile build/sparkpipe_model_publish
   build/libhidden_transport_spark_host_rdma_verbs.so  (node ~/g53full-src)
2. make -C modules/glm52_resident_decode_stage adapter EXPERT_CODEC=bf16
   -> libglm52_serving_adapter_bf16.so
3. sparkpipe_model_compile --model
   examples/model_descriptions/glm52_resident_decode_stage_bf16_firmware.json
   (my rendered per-source pin) --library build/module_library (holds the
   GPU-passed b2c526c7 unit) -> model_driver.so
4. assemble ~/sparkdata/glm53full.bf16.tp16/{bin,lib,config} (packs in
   place; configs from tools/glm53full_gen_deployment.py, spark5=rank 5)
5. queued task: drop_caches, residentd --rank-index 5, poll the log for
   the module/pack load receipts (TP16 collective will wait for peers —
   the LOAD receipt is the isolation gate), nvidia-smi memory beside,
   TERM by cwd-filter, exit.

Next tick assembles + submits this as glm53full-bf16-isoload (priority
0, ttl ~30). PR #749 still awaiting review.

## Tick 6 (06:15Z) — isolation cell assembled; queued behind dsv4flash build

- Root-caused the build failure chain: (1) ~/g53full-src on spark5 is
  periodically RESET by something (likely a stale 3res-lane automation;
  no sweeper process found) — my node checkout moved to
  ~/g53full-dev-src; (2) tools/sparkpipe_module_compile.c exists on NO
  branch (glm5-next lane-local, commit d0218d1) while main's Makefile
  rule references it — extracted from d0218d1, staged at
  ~/g53full_model_compile.c, and the cell copies it in; (3) the
  Makefile rule for it misbehaves under GNU make 4.3 (rule parses into
  the database yet the goal falls to implicit search) — the script now
  links it DIRECTLY (cc ... libsparkpipe_compiler.a libsparkpipe_core.a,
  verified working on the node: --help answers).
- glm53full-bf16-isoload3 queued (priority 0, ttl 40): builds
  residentd/batch/transport + bf16 adapter + driver.so (model_compile
  over the published b2c526c7 unit + my rendered bf16 firmware),
  assembles the runtime root, generates spark5=rank5 configs, launches
  residentd, captures load receipts + nvidia-smi, TERMs by pid.
- Currently BLOCKED: dsv4flash's tp16_build_all.sh holds spark5 (since
  14:43) — the dispatcher correctly parks my task ("nodes busy").
- NOTE for coordinator: tools/sparkpipe_module_compile.c should land on
  main (its Makefile rule + demo target already reference it; glm5-next
  lane-local at d0218d1). Cross-lane dependency, not mine to commit.

## 2026-09-01 — operator realignment: non-spec decode speed is the work

- Read docs/SPARK_QUEUE_RUNBOOK.md (new on main); queue migrated to the
  machine-global durable state ~/.sparkpipe/queue; doctor PASS; one
  dispatcher daemon. My lane worktree rebased: origin/main now CONTAINS
  this lane's work — **PR #749 MERGED** (cd2a54a, 2026-08-30 20:58,
  clean, incl. the nvfp4 firmware-pin correction). M1 closed.
- Operator directive: keep pushing non-speculative inference speed;
  prefill hill climb OPENS when kimidev fixes the prefill bugs (the
  held multirow/duplicate-dispatch chain). Until then decode-only.
- Fleet prep DONE (CPU-only, direct): all 16 sparks hold the bf16
  runtime payload (residentd+batch, model_driver.so 3186294a linked
  from the GPU-passed b2c526c7 module unit, bf16 adapter,
  host-rdma transport — sha-verified identical on 16/16), rank configs
  (spark<hex>=rank), and the byte-exact packs. 15/15 rank-pack hashes
  matched the spark5 reference set (ranks 0-3 of 16 confirmed before
  the reference run paused; the 15 remote ranks all verified).
- glm53full-bf16-wave1 QUEUED (priority 0, ttl 30, all 16 nodes):
  cleanslate check -> cache drops -> same-second 16-rank launch (180s
  connect-window law) -> ready gate -> 8-token exactness gate BEFORE
  timing -> sustained B1 decode wall-clock receipt + nvidia-smi ->
  TERM by recorded pids. Dispatches when the coordinator-stagepacks
  holds (spark1,4,a,b,c,d,f) release.
- Decode hill-climb inventory (next waves, from the dormant-capabilities
  audit + collectives math): glm52 split-K threshold=0, screened-head=0
  knobs; collectives-per-token count on TP16; sync-callback overlap.

## 2026-09-01 tick — wave1 arbitration + collectives/token audit

- wave1 was CANCELLED in the coordinator's pass3 arbitration (fleet
  mid-handover to kimidev serving); resubmitted as **wave1b** (priority
  0, ttl 30) with the deferral note — dispatches when
  glm5next-serving-kimidev (ttl 240m, holding 15/16) releases.
- **Collectives/token audit (TP16, source-read)**: the decode chain is a
  serial stage machine — per token: 1 embedding all-reduce (BEGIN) +
  78 layers × 2 (attention-out all-reduce, MLP-hidden all-reduce) +
  1 head max-loc reduce = **158 stream-ordered collectives per decode
  token**, each with a host callback round-trip advancing the chain
  (SparkGlm52TpChain stages BEGIN→ATTENTION→REDUCE→MLP→REDUCE→…→HEAD).
  Both per-layer reduces are data dependencies (TP16 partial sums), so
  the hill-climb levers are (a) reduce latency itself (NCCL CPU-path is
  ~90% roofline on GB10 — already near floor), (b) the host
  chain-advance serialization between collectives (launch-ahead /
  deeper pipelining), (c) the arithmetic between them (split-K,
  screened-head knobs). Wave1b's B1 number locates the bottleneck.
- PRs I own: none open (#749 merged). #765 (qwen38max TP16 kernels)
  merged by coordinator; k3-prefill #766 held with review — my prefill
  gate remains the glm5next multirow chain, unchanged.

## 2026-09-01 tick — hill-climb knob inventory resolved (screened head is FREE for bf16 B1)

- Source-read result: `SparkGlm52BuildHeadShadow` runs unconditionally in
  module Prepare when the rank owns the final head — it quantizes the
  LOADED lm_head on-device (no pack entry). So the bf16 arm gets the R1
  certified-FP8 screened head at B1 (rows==1) AUTOMATICALLY; wave1b's B1
  baseline already includes it. The earlier "screened head = cheapest
  win" note applied to families where the shadow build was gated off.
- Wave2 design implication: the controlled pair is B1-screened vs
  B1-full-vocab head (rows==1 forced through Glm52HeadFullVocab) to
  QUANTIFY R1 on this arm (glm5_next/qwen38/dsv4 saw 2.7-5.3x full-vocab
  speedups), plus a B8 cell to verify the multi-row full-vocab path
  amortizes as designed.
- "split-K threshold" from the dormant-capabilities audit: NOT an env
  knob — it is plan logic in inference/kernels/gemm.cuh (grouped GEMM
  splits per group once rows exceed TILE_M; line 30). Making it a tunable
  is shared-code work (runtime//inference/) — coordinator territory if
  wave1b's numbers point there.

## 2026-09-01 tick — fp8 arm qualification staged + queued

- fp8 publish cell (`glm53full-fp8-publish`, spark0, p5, ttl 30) queued:
  module publish EXPERT_CODEC=fp8 vs the rank0 fp8 pack + GPU tier
  validation, on this codebase state. Note: spark5's fp8 build-node
  locals were retired post-placement (derived artifacts) — the cell
  targets spark0 which HOLDS the rank0 fp8 pack. Queue orders it after
  wave1b (both need spark0; no lease overlap).
- All three staging nodes (spark0/5/9 checkouts) refreshed to the
  current tip 05b581b via fresh bundle.

## 2026-09-01 tick — wave1b window extended with the batch ladder

- wave1b's script (not yet dispatched; safe to edit) now also runs B8 and
  B16 aggregate decode cells (128 tokens each) in the SAME exclusive
  window, after the B1 baseline: batch-scaling baseline for free, no
  second window needed. The ladder locates where the 158-collectives/
  token serial chain stops dominating (aggregate tok/s vs B).
- Blocker unchanged: dsv4pro CPU builders (p5, ttl 120m) hold
  spark0/8/9; wave1b (p0, all-16) dispatches when they release.

## 2026-09-01 tick — wave1b RED-STOP root-caused and fixed; wave1c queued

- wave1b failed instantly on all ranks: residentd `deployment=schema_error`.
  Root cause (verified by linking the REAL validator locally via a 15-line
  probe over runtime/{model_resident_deployment,json,filesystem}.c +
  src/spark_status.c): the deployment schema requires stage_index UNIQUE
  per node forming a contiguous 0..N-1 chain, and the glm52 adapter
  ASSERTS tp_rank == stage_index while overriding the module context to
  the single 78-layer stage. My generator had stage_index=0 on all ranks.
  FIX (cd0f5fe): stage_index = rank. Regenerated + probed 16/16 load=ok,
  restaged to all nodes.
- Wave script: ready-poll now fast-fails on `deployment=<status>`; noted
  the poll's real wall-time is ssh-bound (~15s/sweep) — future waves
  parallelize the sweep or shrink the ceiling.
- wave1b cancelled (its poll would have burned ~30 more minutes; all
  daemons were already dead). wave1c queued (p0, ttl 30) — dispatches
  when qwen38flash-wave's all-16 window (in flight when the leases
  freed) completes. No preemption of running leases; correct FCFS.
- Reusable diagnostic landed at /tmp/deploy_probe.c (Mac): link the real
  deployment validator and test configs without a node.

## INTEGRATION REQUEST (filed while wave1c waits; shared code, coordinator to integrate)

**Split-K / grouped-GEMM row-split threshold as a tunable.**
- Where: inference/kernels/gemm.cuh — the grouped GEMM splits per group
  once rows exceed TILE_M (comment at line 30; the plan builder picks
  tile/split from the row count). The dormant-capabilities audit named
  "glm52 split-K threshold=0" as a candidate cheapest-win: at B1 decode
  every routed group has rows<=8 (top-k=8 packed rows over 256 experts),
  far below TILE_M, and forcing the split earlier (threshold 0 = always
  split) changes the per-group tile shape the kernel launches with.
- Why it needs the coordinator: runtime/ + inference/ are shared write
  set; seven families consume this path. My lane supplies the test
  cells and numbers, not the edit.
- My commit upon integration: wave2 cell pair (baseline vs threshold-0
  build) with exactness gate before timing, on the bf16 arm, TP16,
  B1 + B8; receipts to this reports/ dir.

Second, smaller request if wave1c's ladder shows the head dominating:
the R1 screened head currently engages only at rows==1 (hard-coded in
SparkGlm52LaunchCudaWaveHead's glm52 cuda.cu — module-local, MINE to
flip if evidence supports rows<=k engagement; no request needed, noted
for completeness).

## 2026-09-01 tick — personal build node adopted; wave1c io_error fixed; wave1d queued

- Operator ruling adopted: **spark1 is my personal build node** (cpu-class
  only, coexists with GPU windows, modest RAM, no giant /tmp intermediates;
  spark0 excluded from everything new). Build checkout staged at
  ~/g53full-build on spark1 (tip 05b581b+), bundle + model_compile.c
  staged. Future: compiles/builds/tests run there; fp8-publish cell will
  move to spark1 (uses spark1's rank1 fp8 pack — pack-agnostic cell).
- wave1c fast-failed (the new deployment=<status> pattern worked — no
  wasted poll): `deployment=io_error` = ValidateDirectories, root cause
  kv_backing_directory missing. ~/kvcache/glm53full.bf16 created on all
  16 nodes. wave1d queued (p0) behind the glm53-burst-* series that
  claimed the fleet.
- Two distinct launch-blocking classes found so far: schema (config
  CONTENT) and io (config-referenced DIRECTORIES). Both now verified
  locally/fleet-wide; next failure class would be new.

## 2026-09-01 tick — THE adapter root cause (TP8 hard-code); wave1e queued

- wave1d: `initialize=target_mismatch` at phase=deployment_validation.
  SparkModelResidentDeploymentValidateForAdapter pins deployment
  node_count == descriptor.stage_count, and the glm52 serving adapter
  hard-coded the 5.2 TP8 band (STAGE_COUNT 8u, TP_DEGREE 8u — even its
  identity string said tp8). A 16-node deployment could NEVER pass, and
  the adapter's tp_degree assert would have refused 16 next. This is the
  actual blocker that made the 5.3-full TP16 deployment impossible until
  now — waves a/b/c/d each peeled one layer (schema content, dirs, this).
- FIX (4b60362, module-local): ADAPTER_FLAT_RANKS build-time selection
  (default 8 byte-identical for 5.2; 16 = tp16 identity +
  16x78 stage-layer table). TP16 adapter built on MY spark1 build node,
  identity string verified, fanned to all 16 runtime roots
  (model_serving_adapter.so).
- Wave script fail pattern now also catches initialize=<verb>.
- wave1e queued (p0, ttl 30).

## 2026-09-01 tick — fp8 cell moved to spark1; wave1e waits on spark0

- fp8-publish cancelled on spark0 (excluded by the personal-build-nodes
  ruling) and re-queued as glm53full-fp8-publish2 on MY spark1 (rank1
  fp8 pack, ADAPTER_FLAT_RANKS=16, ttl 15 per the TTL ruling). The cell
  is pack-agnostic — any real rank pack pins the identity.
- wave1e (p0, all-16) blocked on spark0 only: qwen38flash-s1-standalone
  (p5 gpu) holds it; 14/16 nodes free. Dispatcher priority semantics
  correct (no preemption of running leases). Wave lands when spark0
  frees; the glm53-bx p0 burst series shares the same gate.

## 2026-09-01 tick — TTL compliance + head-of-line arbitration note

- wave1e cancelled/resubmitted as **wave1f**: ttl trimmed 30→15 (the
  operator's 15-min-max ruling), settle 75s→10s (the preceding window is
  a spark0 burst, not our port family; cleanslate still runs), and the
  ready-poll now fans 16 CONCURRENT ssh per sweep (~7s/sweep, 60 sweeps
  ≈ 7min ceiling) instead of 16 sequential (~15s/sweep → 30-40min).
- Head-of-line: wave1e/f blocked ONLY by the coordinator's glm53-bx
  p0 burst series on spark0 (single-node). Arbitration note filed
  (glm53full-wave-window-note, p0): either slot my ~15-min wave before
  the remaining bx cells or tell me the series end time.

## 2026-09-01 tick — QUEUE PRIORITY BUG filed (evidence captured)

- At the glm53-bx-24576-paced ttl expiry, the dispatcher released the
  nodes and dispatched dsv4flash-tp16-cell1 (p5, all-16) in the SAME
  pass while glm53full-bf16-wave1f (p0, all-16, queued, no --after) was
  ahead of it. p0 must select before p5 (runbook invariant 3; the
  priority-order regression gate claims this). Log excerpt saved;
  filed as glm53full-queue-priority-bug note for the coordinator
  (their tooling, their fix; also re-asks the wave-window arbitration
  since bx cells v3/v4 keep leapfrogging my wave within p0-FCFS).
- Practically: the fleet is now dsv4flash's M4-gate window (all-16,
  ttl 15). When it ends, wave1f competes with bx-burst2/v3/v4 again —
  the coordinator's arbitration (or a queue fix) decides; both notes
  offer the --after re-add as the compliant fallback.
