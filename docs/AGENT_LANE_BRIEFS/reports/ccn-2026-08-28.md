# Complexity lane report — the cyclomatic plan grows teeth (2026-08-28)

Branch `lane/ccn`, worktree `/tmp/lane-ccn`. Brief: CLEANUP_PROGRAM.md's
cyclomatic section — honest scoping, stage zero, conjunction-soup tables,
and the binding ceiling. "Never merge own PR" observed: PR opened, not
merged.

## Scoreboard relevance

The audits' sharpest finding was ENFORCEMENT, not abstraction: the max
CCN grew 157→158 while the plan sat in a doc. This lane makes the plan a
merge gate: `python3 tests/test_complexity_ceiling.py`, wired FIRST in
`tools/gates.sh` and registered in PYTHON_TESTS. The ceiling committed
here is **75** — the production max at landing, down from 159 at the
baseline scan. Every future increase now needs an in-commit ledger
justification, exactly like the size ratchet.

## 1. Honest scoping (validation/ out of the merge-gate metric)

`tools/complexity_report.py` now reports two scopes; the merge-gate
metric is production only. Scope discriminator: the `validation` path
component (`modules/<family>/validation/` — the control-vs-candidate
CUDA harnesses, deliberately independent per doctrine, never merged).

Raw movement (same scanner, same snapshot rules):

| scope | functions | mean CCN | max CCN | hotspots >25 |
|---|---|---|---|---|
| MIXED (the old metric) | 3275 | **8.01** | 159 | 155 |
| PRODUCTION (the metric now) | 2945 | **7.90** | 159 | 125 |
| VALIDATION (own budget) | 330 | **8.98** | 90 | 30 |

- The audit's "mean 7.33 vs 8.01" gap: the honest production-only number
  is **7.90** — closer to the 7.33 external baseline than the mixed 8.01,
  as the audit predicted (validation harness functions were inflating
  production numbers; 5 of the then-top-25 were validation functions at
  this snapshot — the audit's snapshot had a third).
- The report's "max 157" was a hardcoded string (stale); the max is now
  COMPUTED from the scan.
- The ratchet's counting (`tests/test_complexity_ceiling.py`): production
  max/mean gate the merge; validation has its own visible budget (max
  90, mean 9.50) that never touches the production metric.

## 2. Stage zero — SparkQwen38_27bModuleRunDsparkBlockForward: 159 → 75

File: `modules/qwen38_27b_resident_decode_stage/source/
spark_qwen38_27b_resident_decode_stage_module.c` (635 → 341 lines).

- **Six per-call getenv sites evicted** into the typed
  `SparkQwen38_27bDflash2Config`, parsed ONCE at configure by
  `SparkQwen38_27bDflash2ConfigLoad` (called from ModuleConfigure).
  The audit said five flags; the file carried six env names — all six
  are evicted, receipt each (also in
  `tests/test_qwen38_27b_dflash2_config.py`, which pins the mapping and
  fails if getenv re-enters the forward or a dump path returns):
  1. `SPARK_QWEN38_27B_DFLASH2_WINDOW` → `window_bound` (uint, default 2048)
  2. `SPARK_QWEN38_27B_DFLASH2_BLOCK_KV` → `block_kv_on` (present && [0]!='0')
  3. `SPARK_QWEN38_27B_DFLASH2_CTX_TAIL` → `ctx_tail` (uint, default 0; range-clamped)
  4. `SPARK_QWEN38_27B_DFLASH2_CTX_CACHE` → `ctx_cache_on` (absent||[0]!='0', forced off when tail≠0)
  5. `SPARK_QWEN38_27B_DFLASH2_CTX_DUMP` → `ctx_dump_present`/`ctx_dump_nth`
  6. `SPARK_QWEN38_27B_DSPARK_SEL_CHECK` → `sel_check_present`
  Env NAMES are unchanged and remain the config source (the runbook's
  exports and every tool that sets them keep working).
- **The 55-line inline /tmp ctxdump block deleted** (wrote
  /tmp/ctxdump_*.bin, /tmp/ctxwin_*): its taps-neighborhood + anchor
  data is re-derivable with the G5N-PROBE ladder pattern if ever needed.
- **The UNCONDITIONAL one-shot /tmp parity dump deleted** (wrote
  /tmp/dflash2_taps|c0|logits|hidden.bin on the first block forward of
  EVERY process — unconditional debug residue in the hot path). This is
  a /tmp-artifact behavior change, not a serving change; validator
  receipts unaffected. The GATED captures remain and still feed the
  offline parity tools (`tools/qwen36_dflash2_round_parity.py` et al.),
  now reading the config struct instead of calling getenv per call.
- The SEL_CHECK parity oracle, the incremental context projection, the
  block-KV history staging, and the multi-block plan were extracted as
  helpers — **verbatim code motion** (fragment-equality verified
  mechanically; the SEL_CHECK core is a 1524-char exact match).
- Behavior notes (receipts below): an out-of-range `CTX_TAIL` used to
  fail EVERY forward with CAPACITY_EXCEEDED; it now fails configure with
  the same status and message — a config that can never run a draft is a
  configure error. Absent/edge env semantics preserved (incl. the old
  BLOCK_KV `benv[0]!='0'` and CTX_CACHE `cenv[0]=='0'` rules).

**Receipts**: `tests/test_qwen38_27b_dflash2_config.py` (registered in
PYTHON_TESTS: six env names → loader, forward getenv-free, deleted dumps
stay deleted, clamp fail-loud); `cc -fsyntax-only` parity — the edited
file produces an IDENTICAL pre-existing error set vs HEAD (8 cuda-stub
gap errors, all in untouched regions; zero new errors, zero warnings in
new code); fragment-equality motion proof; dsv4/glm5_next-style host
syntax discipline referenced. GPU validator runs remain a qualification
step (no CUDA on this host) — flagged for the coordinator.

## 3. Conjunction soup → tables

| function | file | CCN before → after | conversion |
|---|---|---|---|
| `SparkPipelineRuntimeValidateRankPlan` | `runtime/pipeline_runtime.c` | 87 → 10 (walk) + 10 rows ≤ 32 | 10-rule predicate table, rows in the original if-chain order; the mode tail split into non-transported/transported rows carrying the original branches verbatim |
| `SparkModelServingAdapterValidateDescriptor` | `runtime/model_serving_adapter.c` | 80 → 6 (walk) + 12 rows ≤ 23 | 12-rule predicate table, original order/statuses preserved |
| `SparkDsv4ModuleConfigure` | `modules/dsv4_.../spark_dsv4_resident_decode_stage_module.c` | 75 → 46 | the ~30-term `||` decomposed into four named context predicates (slice / capacities / codecs / topology) + page-capacity and tp-collective predicates; same order, same short-circuit verdicts |
| `SparkGlm5NextStagePackExpectedShape` | `modules/glm5_next_.../spark_glm5_next_stagepack_format.h` | 84 → 21 (+16 `CheckLayerKind` helper) | the 30-case switch became a 49-row designated-initializer shape table; `payload_type==0` marks uncovered kinds → still the old `-6`; the -3/-4 layer/kind rules are a named helper |

**Verdict-pair receipts (same inputs, same verdicts, old vs new builds):**

| validator | method | trials | result |
|---|---|---|---|
| glm5_next StagePackExpectedShape | EXHAUSTIVE grid (kind × layer × codec × tp_degree, global-layer sentinel) | 462,672 | verdict+shape streams byte-identical |
| ValidateDescriptor + ValidateRankPlan | PRNG byte-mutations of valid linear/fanout/hybrid descriptor+plan baselines (pointer fields excluded) | 42,002 | verdict streams byte-identical: 8,864 OK / 31,149 INVALID_ARGUMENT / 1,989 ABI_MISMATCH |
| Dsv4ModuleConfigure | PRNG mutations of a TP1 node context, linked against the REAL old/new module objects (`-Dstatic=` extraction, CUDA launchers stubbed — never reached on the TP1 path) | 20,001 | verdict streams byte-identical (1,049 OK / 18,863 INVALID / 89 other statuses) |

Harness scripts live in `/tmp/ccn-fuzz/` (ephemeral); the durable pins
are the registered tests listed above plus the ceiling gate itself.

## 4. The binding

- `tests/test_complexity_ceiling.py` (registered in Makefile
  PYTHON_TESTS, invoked FIRST in `tools/gates.sh`): production max CCN
  must not exceed the committed ceiling — **moved down this lane from
  the measured 159/158/157-line to 75** — with a ledger for in-commit
  justifications (the size ratchet's discipline), a production MEAN
  ceiling (7.81), and the validation harness's own budget (max 90).
  Negative-tested: with the ceiling set below the real max the gate
  fails with the justification guidance.
- The plan now has teeth: any PR that raises the production max fails
  the merge gate; lowering landings are expected to keep moving the
  constant down ("note: ceiling is N above reality" nudges like the size
  ratchet's).

## Metric movements, raw

| metric | baseline | at landing |
|---|---|---|
| production max CCN (merge-gate) | 159 (function 2354, "158" in the audit, "157" hardcoded in the report) | **75** |
| production mean CCN | 7.90 (post-scoping; 8.01 mixed pre-scoping) | **7.81** |
| production hotspots >25 | 125 (155 mixed) | 123 |
| production functions | 2945 | 2979 (helper extraction) |
| validation max / mean (own budget) | 90 / 8.98 | unchanged (untouched scope) |
| named P1 targets left on the board | qwen38_27b SubmitSpeculativeDecode 73, glm5_next InitializeTpCollective 70, glm52 StagePackExpectedShape 49, LmGemmLaunchAsymmetric 67 | next landings move the ceiling DOWN from 75 |

## Size ratchet

The lane nets NEGATIVE on authored lines (dumps deleted, switch→table):
`tests/test_code_size.py` reports 214730 exact vs ceiling 216956; the
ceiling is lowered to the exact count in the landing commit per the
ratchet's own "lower it with the next landing" note (net −2226).

## Write-set note

The default family-lane boundary (no Makefile/runtime/etc. edits) is
superseded by this lane's brief, which names `tools/complexity_report.py`,
the ratchet's counting, and the runtime validators as the work set. Files
touched: tools/complexity_report.py, tools/gates.sh, Makefile (PYTHON_TESTS
registrations only), tests/test_complexity_ceiling.py (new),
tests/test_qwen38_27b_dflash2_config.py (new), runtime/pipeline_runtime.c,
runtime/model_serving_adapter.c, modules/{qwen38_27b,dsv4,glm5_next}/*
(the three families named by the brief), docs/AGENT_LANE_BRIEFS/reports/
ccn-2026-08-28.md (this file), tests/test_code_size.py (ceiling lowered
to exact count). No other lanes' files touched.

## Integration request

None — everything is in-tree on `lane/ccn` and self-contained. PR opened
by the coordinator flow; per the standing rule I do NOT merge it.
