# WAVE-1 DRY lane — all five clusters landed, every merge proven equivalent (2026-08-28)

Branch `lane/dry-wave1`, worktree `/tmp/lane-drywave1`, base **1d8ef88**
(four commits: c5d22ac, 7ecf1f5, 61d6edc, 3321769). NOT merged (never merge
own PR). Note: main advanced to 9d581ee while this lane ran — the per-cluster
receipts below are all taken against the merge base, and the ratchet must be
re-run after any rebase/conflict resolution per the merge gates. Spark5 was
not needed: every receipt is host-side (byte hashes, shim replays, compiled
probes, unit tests).

## The headline

**Code-size ratchet: 214,264 (base 1d8ef88, exact) → 212,723 — NET DELETION
of 1,541 authored lines**, all of it pasted code, all of it proven
byte/behavior-equivalent per cluster. dup_report ratchet: **80 → 76 hits,
3,948 → 3,760 matched lines** (the 4 died rows are all pack_synthesize
clones; zero new rows). dry-law: PASS (177 shared files checked, zero model
tokens — the three new shared files are parameterized cores in
model-families/common and include/sparkpipe).

## Cluster ledger (before → after, author-authored lines)

| cluster | before | after (family files + shared core) | net |
|---|---|---|---|
| 1. pack_synthesize quartet | 1,587 | 587 + 415 = 1,002 | **−585** |
| 2. work_control trio | 1,011 | 27 + 360 = 387 | **−624** |
| 3. batch_tuning pair | 450 | 161 + 172 = 333 | **−117** |
| 4. validate_*.sh sextet | 759 | 460 + 190 = 650 | **−109** |
| 5. python residual | − | −99 dormant + packer migration | **−106** |

## 1. pack_synthesize quartet → one parameterized generator

`tools/spark_pack_synthesize_common.h` (model-families/common/include — on
every family synthesizer's documented include path). The pasted xorshift
noise lane, per-tensor seeding, alignment, payload/scale fillers, chunked
region writer, and the whole qwen-slice directory build + CLI move into the
core; the four family files become family configuration (stage-pack types,
kind inventory, quantization policy). glm5_next keeps its tier-aware CLI,
tier header layout, and shape-table kind-walk family-side and consumes only
the primitive tier — its main (CCN 51, the redundancy lane's P12 named plan)
is untouched and stays on the books.

**Proof — byte identity vs the quartet's output:** nine packs regenerated
before (git-HEAD sources) and after, sha256-identical each: qwen38_max GDN
slice 26.76 GB, qwen38_max attn bf16 52.49 GB, qwen4_flash 2.67/5.17 GB,
qwen38_27b 0.20/0.74 GB, glm5_next KDA 0.58 GB, DSA tp2/mxfp4 2.09 GB,
MTP+head tail 24.70 GB — **78.5 GB hashed, zero differing bytes**. stderr
report lines, stdout, and exit codes identical run-by-run, INCLUDING the
pre-existing qwen38_max full-stack failure (whole-stack inventory 1,679 >
1024-entry directory → rc=4 "directory build failed") reproduced exactly —
a latent capacity limit, recorded in the family file, deliberately not
"fixed" (that would be a behavior change).

## 2. work_control trio → shared include

`spark_work_control_common.h`: the 337-line body pasted across
qwen38_max/qwen38_27b/qwen4_flash, differing only in the family prefix,
parameterized by three family macros (FN/TYPE/CONST). Family headers keep
the typed API.

**Proof:** preprocessed translation units token-identical modulo family
prefix for ALL THREE (blank-line-insensitive); `test_qwen38_work_control`
and `test_qwen38_27b_work_control` PASS on the branch (they link the family
host libraries built from the migrated sources).

## 3. batch_tuning quartet → one header, two defines (scoped to the identical pair)

`include/sparkpipe/spark_batch_variant_tuning_common.h` (in include/, not
model-families/common, because the contract probe compiles with `-Iinclude`):
the eleven-bucket ID compositions, compiled-bucket module-ID selection,
derived grouped-tile geometry, and the runtime ceiling/module-ID selection
functions move out of the glm52/glm5_next headers — the pair the audit found
byte-identical except two identifiers. Each family header keeps its identity
(guard, bucket #error guard, codec gate, ID prefix/suffix) and aliases the
legacy macro spellings, so **no consumer changes**.

**Scope honesty:** k3 and dsv4 stay family-side. `tests/test_batch_variants.py`
compiles glm52+k3+dsv4 tuning headers into ONE probe TU, so family-neutral
macro names are only adoptable by headers that never coexist; folding k3/dsv4
needs a paste-parametrized-name redesign worth more than their ~400 lines,
and dsv4's bucket set genuinely differs (DSpark spec-step buckets 6/9/11).
That is the DRY law working, recorded.

**Proof:** `tests/test_batch_variants.py` PASS unchanged (source contract +
per-bucket compile-and-run probe, 11 buckets); macro-expansion diff old-vs-
new IDENTICAL for glm52 and glm5_next at b1/b8/b512/b1024 (codec int8)
covering every exported macro and both selection functions; glm5_next
firmware header compiles at b1/b8/b1024 × int8/mxfp4.

## 4. validate_*.sh sextet → one parameterized driver

`modules/spark_resident_decode_stage_cuda_validation_common.sh`: the pasted
skeleton (usage, directory derivation, retained validation directory,
source-digest pin, archive/pack checks, sm_121a/nvcc gate, core-library
make, nvcc link, validator run). Each family script keeps its own admission
gates AND their original order (tier ladders, codec ladders, reference
fixtures are per-family policy, visible family-side), configuring label /
env-prefix / include-dirs / nvcc-splice position.

Deliberately preserved paste artifacts (behavior, not bugs to fix silently):
qwen4_flash's toolchain labels say "Qwen38_27b"; glm52's digest label says
"Glm52" while its toolchain label says "glm52"; glm5_next skips the
hash-format check; qwen38_max has no CPU oracle and includes
model-families/common first in its -I order.

**Proof — generated command lines diffed (the brief's requirement):** stub
nvcc/make/sha256sum recorded exact argv; old (git HEAD) vs new scripts
replayed under identical configs. Happy path IDENTICAL for all six families
(every nvcc flag, include order, -D splice position, output name, make argv,
validator-run, stderr, exit code); dsv4 replayed with
SPARK_DSV4_VALIDATION_DEFINES set AND unset — the unquoted
`${validation_defines:+...}` single-word expansion is reproduced exactly.
Seven failure paths identical (stderr text + rc 2): usage, missing pack,
CUDA_ARCH≠sm_121a, 27b STAGE_COUNT=1 rejection, max TP>1 rejection,
glm5_next rogue codec, dsv4 bad batch bucket, glm52 digest mismatch.

## 5. Python residual

- **DELETED `tools/glm52_resident_pack_common.py` (−99).** Receipt: repo-wide
  scan finds zero code importers (only the generated receipts and three docs
  calling it dormant); its tp_shard_range/align_up/PackFailure already live
  verbatim in spark_pack_common.py and its SafetensorReader was superseded
  by SafetensorsSource; the file was broken anyway (`Tuple` used, never
  imported — latent NameError). TOP10_glm52 item 8, BACKLOG DRY block item 1,
  and PACKER_CORE_PLAN's dormant-seed note updated to landed.
- **glm52_stagepack.py migrated onto spark_pack_common.py** (TOP10 item 7,
  the last un-migrated packer): inline `align()` delegates to `align_up`,
  the duplicated `sha256_file` (identical 16 MiB streaming) is deleted, and
  all 13 raise sites move ValueError → PackFailure(RuntimeError). main()'s
  except tuple keeps ValueError (library int/json errors still surface), so
  the CLI failure surface is unchanged. PACKAGE_MANIFEST.json/SHA256SUMS
  hand-edited to drop exactly the deleted file's entries (regenerating would
  have swept in ~1,100 lines of unrelated manifest staleness — and
  `verify_package_manifest.py` fails 493 on the PRISTINE baseline too, a
  stale-on-main receipt this lane does not own; flagged for the
  coordinator).
- The zero-reference scan triaged the REST of the python residual as
  hand-run operator tooling (qwen36_dflash2_* = the live DFlash2 toolchain
  per redundancy-2026-08-27; perf/estimate tools run ad hoc) — recorded,
  not deleted.

**Proof:** old vs new module loaded side by side — align() identical over
0..4999 + boundary values, sha256_file hex-identical, exception type/message
preserved; `test_glm52_stage_pack` (six codecs, bit-exact layouts),
`test_production_selection_contract`, `test_glm52_quantized_cuda_contract`
PASS unchanged (no "default=" introduced; the required --expert-codec
spelling untouched); `test_qwen38_27b_stagepack` PASS — it compiles and runs
the consolidated 27b synthesizer and passes its pack through the python
verifier.

## Gates (re-run post-landing; merge-base comparisons)

- **code-size**: base 1d8ef88 exact 214,264 → branch 212,723 = **−1,541 NET
  DELETION**, under the 216,956 ceiling (4,233 headroom for concurrent
  lanes; the ceiling itself is the coordinator's to lower).
- **dup_report**: base 80 hits/3,948 matched lines → branch 76/3,760. Died:
  the four pack_synthesize clone rows (188 matched lines). New: none.
- **dry-law**: PASS, 177 files, zero model tokens in shared code.
- **cyclomatic**: no new function over CCN 25 in any consolidated file;
  glm5_next_pack_synthesize main stays at its pre-existing 51 (P12).
- **make -j8 test**: branch and base-1d8ef88 failset IDENTICAL — both abort
  at `test_dsv4_serving_adapter.c:202` (Error 134), a pre-existing Mac
  failure unrelated to this lane; every test before the abort ran
  identically. tools/gates.sh's CUDA compile gate needs spark5 (queued by
  coordinator if desired; no CUDA surface was touched except the tuning
  headers, which compile host-side per bucket in test_batch_variants).
- Note: `tests/test_template_adoption.py` (the program's enforcement gate)
  is not yet in the tree; when it lands, all five clusters already comply —
  every family file imports its shared core.

## Coordinator decisions needed

1. **Merge order**: the four commits are independent per cluster; all are
   net-negative against the base ratchet. Rebase onto current main (9d581ee)
   then re-run `test_code_size` + `dup_report` per the merge gates.
2. **verify_package_manifest.py is red 493 on pristine main** (stale
   receipt, pre-existing): either regenerate the receipts on a clean tree or
   re-scope the gate; this lane only removed the one deleted file's entries.
3. **qwen38_max full-stack synthesize overflow** (1,679 tensors > 1,024
   directory capacity): pre-existing, preserved, now documented in the
   family file. Fix would be a behavior change (MAX_TENSORS 1024 → 2048
   costs 8 KB BSS per tool and needs a deliberate go).
4. test_template_adoption.py: my clusters satisfy it; the gate itself still
   needs an owner.
