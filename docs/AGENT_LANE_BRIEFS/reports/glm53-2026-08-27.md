# GLM 5.3 Flash lane report — 2026-08-27 (M1: contract freeze)

Worktree /tmp/lane-glm53, branch lane/glm53. Node used: spark2 (light IO
only: file reads, header parses, one 49s strided sha256 pass).

## M1 COMPLETE — commit 0c8e01a

Artifacts:
- `model_contracts/glm53_flash_authoritative.json` — frozen contract
- `tools/glm53_contract_freeze.py` — parameterized generator (`--spark HOST`)
- `tests/test_glm53_contract.py` — 53 offline checks, all PASS
- code-size ratchet 186500 -> 187123 with justification comment

## What ran (commands + raw output)

### Source pinning
Source: `/mnt/model-warm/glm-5.3-flash`, repo `zai-org/GLM-5.3-Flash`,
revision `84c6a6aa9497188e15a635ba793b0f95a79b1033` (from ARCHIVE-RECEIPT.json).
62 shards, index total 328,326,771,576 bytes (305.8 GiB).

```
$ ssh spark2 'cd /mnt/model-warm/glm-5.3-flash && sha256sum SHA256SUMS'
a919d3e143be4e17dee5897e6e320ef084b31ae7ad4e4b39e4176d9099678574  SHA256SUMS
```
Matches `ARCHIVE-RECEIPT.json` `sha256sums_sha256` — the sums file is
receipt-anchored. All small files hashed and compared against it: all
`matches_source_sums: true` (config.json `bb8f01c4...`, generation_config
`230c3060...`, index `3c3f4036...`, tokenizer.json `19e77364...`, etc.;
full table in the contract's `source.files`).

### Strided shard sampling (noted, as required)
Sampling = shards 1, 9, 17, 25, 33, 41, 49, 57 (stride 8 from 1) + final
shard 62 => 9 of 62 shards re-hashed on spark2 against the pinned sums:

```
$ ssh spark2 'cd /mnt/model-warm/glm-5.3-flash && grep -E "model-000(01|09|17|25|33|41|49|57|62)-of-00062" SHA256SUMS > /tmp/glm53_stride_sample.txt && sha256sum -c /tmp/glm53_stride_sample.txt'
model-00001-of-00062.safetensors: OK
model-00009-of-00062.safetensors: OK
model-00017-of-00062.safetensors: OK
model-00025-of-00062.safetensors: OK
model-00033-of-00062.safetensors: OK
model-00041-of-00062.safetensors: OK
model-00049-of-00062.safetensors: OK
model-00057-of-00062.safetensors: OK
model-00062-of-00062.safetensors: OK
real 0m49.381s
```
9/9 OK. All 62 per-shard sha256s are nonetheless pinned in the contract
(taken from the receipt-anchored SHA256SUMS).

### Tensor census (header-only reads, no payload IO)
76,108 tensors; census payload = 328.327 GB == index `total_size`
(asserted in the tool). Byte accounting (contract
`source.byte_accounting`):

| component | GB | tensors |
|---|---|---|
| moe_routed_experts (43 layers x 288) | 311.730 | 74304 |
| kda (34 layers) | 7.085 | 476 |
| dsa_mla (12 layers incl MTP) | 3.893 | 142 |
| lm_head / embed (BF16 each) | 1.269 + 1.269 | 2 |
| vision (OUT OF SCOPE) | 1.127 | 347 |
| moe_shared_experts | 1.082 | 258 |
| dense_mlp (3 layers) | 0.453 | 18 |
| dsa_indexer | 0.179 | 84 |
| hyper_connections | 0.071 | 270 |
| mtp_head + norms | 0.168 | 207 |

Text stack = 327.2 GB => TP16 rank pack = 20.45 GB decimal = 19.1 GiB
(matches the fleet-table estimate; GiB-vs-GB explained).

### Contract test
```
$ python3 tests/test_glm53_contract.py
... 53 lines ...
PASS glm53_flash_authoritative contract (53 checks)
```

## Findings that change the plan (all shape-evidenced)

1. **No fused KDA projection exists in the checkpoint.** The
   `modules_to_not_convert` list contains `qkv_proj` and
   `fused_qkvbfg_a_proj` module names, but the safetensors index has
   NEITHER. KDA layers store separate `q_proj/k_proj/v_proj` [8192,4096]
   BF16 + depthwise `q/k/v_conv1d` [8192,1,4]. M2's name mapping maps from
   these separate tensors (good news: closer to K3's `kda_qkv_beta` split
   layout than the fused name suggested).
2. **`b_proj` [64, 4096] is a per-head scalar beta** (one row per head),
   not a [8192,4096] projection. `dt_bias` [8192] is per-head-per-channel.
   K3's packed KDA layout must be checked against both shapes in M2.
3. **Rope-0 MLA confirmed at the tensor level**: `q_b_proj` [16384,1536]
   = 64 heads x 256 nope ONLY; `kv_a_proj_with_mqa` [512,4096] = pure
   lora latent (glm52: [576,4096] with rope segment). Rope exists only in
   the indexer (`wq_b`/`wk`, 128-dim, interleaved).
4. **The indexer has NO wq_a** — `wq_b` [4096,1536] consumes the MLA
   `q_a_layernorm` output directly: the MLA q_a path IS the indexer q_a.
   One packer consequence: indexer and MLA share the 1536 lora; the pack
   cannot treat them as independent components.
5. **k_norm has a bias** ([128] weight + [128] bias). Donor check needed:
   does dsv4-0731's k_norm carry a bias? (M2 mapping item.)
6. **MTP layer 45 has NO hyper-connections** (45 HC layers = 0..44 only),
   but IS a full DSA layer (MLA + indexer + MoE 288+1) + eh_proj
   [4096,8192]/enorm/hnorm/shared_head.norm. HC bring-up must special-case
   layer 45.
7. **`index_share_for_mtp_iteration: true`** and indexer_tensors are
   present on layer 45 too (12 indexer layers = 11 DSA + MTP).
8. FP8 set is exactly: experts + shared experts + dense mlp (layers 0-2) +
   MLA q_a/q_b/kv_a/o_proj on DSA layers. ALL KDA tensors, ALL indexer
   tensors, hc_*, router, A_log/dt_bias (F32) are BF16/F32. Full list in
   contract `precision.fp8_quantized_patterns`.
9. `use_full_rank_gate` absent from config (K3 sets true) — recorded in
   the contract as an open item; must be resolved from kernel semantics
   (or HF reference source) before wiring `kda_gate` in M3.

## Honest negatives / notes
- `.gitattributes` fetch was skipped (hidden file missed by plain `ls`);
   it is hashed inside the pinned SHA256SUMS but not independently
   re-hashed by the tool. Trivial; not re-run to keep the tool simple.
- main branch was already +2 lines over the code-size ceiling before my
  changes (brief edits); my ratchet bump covers the exact new count
  187123 (glm53 freeze tool ~620 lines; tests are excluded by design).

## Next (M2)
Geometry header + checkpoint->pack name mapping under
`model-families/glm5_next/` generated from the frozen contract, with a
round-trip test against the cached tensor manifest. First donor reads:
`modules/k3_resident_decode_stage` packed field names (kda_*),
`modules/glm52_resident_decode_stage` MLA/MoE pack fields,
`model-families/dsv4` indexer/compressor/HC field names.

## INTEGRATION REQUEST
None this milestone (no shared wiring touched).
