# DSV4 Pro lane — 2026-08-30 restart

Model dev: dsv4-pro (DeepSeek-V4-Pro-0813 GA, TP4PP4 / TP16 packs).
Worktree /tmp/lane-dsv4pro, branch lane/dsv4pro-dev (off origin/main
879fc5a). Identity verified via PAT wrapper: sparkpipe.

## State inherited

- Prior dsv4-pro lane COMPLETE: PR 729 merged; the Aug-15 ghost
  (model_revision) fix is in main
  (examples/deployments/dsv4_pro_tp4_pp4_stage.json now carries the
  2026-08-13 GA revision string).
- Packs: 16 ranks regenerated + verified, 14 deployed, 2 stashed warm
  (ranks 1-2, pending spark1/spark2 serviceability). Old rank3
  byte-identical.
- Known blockers at handoff (from dsv4pro-packs-2026-08-27.md):
  1. Aug-17 vintage bin/lib fail validation (status=9) at
     adapter_initialize after the config fix — needs the driver-lane
     rebuild of the Pro TP4PP4 runtime from current main + redeploy.
  2. spark1/spark2 availability gates full TP4PP4 (rank0 TP group is
     spark0..spark3).
  3. Concurrent /tmp/dsv4bisect-main deployment on spark4-7 (other
     actor) — respect cwd-filtered kill discipline.

## Baseline on my lane (receipts)

- offline-gates exit=2 on a CLEAN origin/main checkout:
  only failure is test_code_size (authored 233188 vs ceiling 233120,
  +68). Pre-existing main drift, not mine — flagging to coordinator;
  every PR from this lane must not grow it further and should shrink
  where cheap.
- All other gates PASS (batch variants, jit kv, api stress, header
  contracts, etc.) except host-unbuilt qwen38 math kernels (SKIP).

## Plan (M1..M4) — perf hill climb after unblock

- M1 (now, no GPU): verify queue status; fix anything in
  modules/dsv4* / tests that main's code-size drift touches; prep the
  smoke task script (tools/dsv4pro_smoke_fleet.sh is the known-good
  launcher — staged, not inline). Watch coordinator-log for the
  driver rebuild landing.
- M2 (driver rebuild landed + queue live): queue
  dsv4pro-smoke-fleet at priority 5 on available ranks; acceptance =
  16/16 ready lines + telemetry receipt
  (curl 127.0.0.1:8765/api/summary pair). spark1/2-gated ranks wait
  for coordinator windows; smoke what's group-complete first.
- M3: capacity claim — PR 721 testable: 33024-position prompt+decode
  on group-complete ranks; exactness before any timing.
- M4: perf hill climb — B1/decode throughput cells via spark_queue
  (staged scripts, ttl declared, TERM-only cwd-filtered cleanup),
  numbers with context/batch/topology/precision into
  PERFORMANCE_LEDGER.md; ratchets only with gates green by exit code.

## Cadence

5-min wakeup timer: advance current milestone, check my open PRs for
review (PAT wrapper only), re-align with docs/ROADMAP_TP16_FLEET.md
and coordinator-log.md, update this report. No manual GPU launches;
queue-only, priority 5 (0 reserved for glm5.3-family/coordinator).

## Tick log

- ~10:0x — Setup tick (see above): lane created, baseline gates
  receipt, report pushed. No open dsv4 PRs.
- ~10:1x — QUEUE IS LIVE (evidence: runs/results.jsonl coordinator
  tasks dispatched+finished through 03:12 today, e.g. q27b-serve-8
  exit 0). Only remaining M2 blocker = Pro TP4PP4 runtime rebuild.
  Submitted queue note dsv4pro-rebuild-status-note asking the driver
  lane/coordinator for rebuild ETA. M1 assets verified current on
  main: tools/dsv4pro_smoke_fleet.sh,
  tests/test_dsv4_pro_exact32k_stage.py (asserts the 2026-08-13 GA
  revision). Still M1; next unblocked item = pre-stage the smoke
  script to group-0 nodes via a queued staging task when rebuild
  lands. 
- ~10:2x — Merged main (96034ce; the two new commits are qwen38
  stagepack tooling, not Pro-relevant). Gates: exit=2, still ONLY
  code-size — main's drift grew +68 → +135 (qwen38 stagepack commits
  added authored lines without bumping the ceiling). Coordinator
  item; this lane adds zero authored lines (merge-only). Rebuild note
  still queued unanswered; no open dsv4 PRs. Still M1.
- ~10:4x — Merged main (d2eb821 quantization policy + fused-MTP log).
  POLICY COMPLIANCE CHECK against the new zero-exception rule: Pro
  packs are compliant — precision (fp8_e4m3 block 128x128) comes from
  the official DeepSeek-V4-Pro-0813 GA checkpoint (contract
  source_revision), the pro packer slices/shards codec-preserve; no
  self-quantization anywhere in this lane's history. No action
  needed. Rebuild still pending; note still unanswered. Still M1.

## ~14:3x — TP16 FIRST (driver direction)

- No TP16 Pro packs exist (warm stash holds only TP4PP4 ranks 1-2);
  spark6 local disk no longer fits the 893G full pack -> the whole
  build moved to warm (42T free).
- New tool tools/dsv4_pro_rank_pack_verify.py + unit test (PR 752):
  contract-derived rank directories for TP16 AND TP4PP4, bounds/geom/
  ordering checks, receipt sha, and sampled payload/scale byte-compare
  vs the GA checkpoint. Planning pinned to the deployed TP4PP4
  receipts (exact tensor-count match). Gates exit 0 (also fixes
  main's pre-existing manifest gate failures via regeneration).
- Queued (priority 5, CPU-only, spark3, staged in home dir):
  dsv4pro-tp16-fullpack (deterministic-rebuild proof vs pin
  0b1692a3..., ttl 150) -> dsv4pro-tp16-ranks (16 shards + verify,
  receipts to /mnt/model-warm/packbuild/dsv4pro-tp16/, ttl 540).
- Next: watch queue receipts; then deploy decision for TP16 packs.

## ~14:4x — queue NOT ready; TP16 build launched DIRECTLY (CPU-only)

- Driver confirms the spark queue is not ready — the earlier queued
  tasks could never dispatch (they also landed in this worktree's
  runs/queue.jsonl, not the dispatcher's; reverted that tracked-file
  pollution off the branch). "Queue is live" inference retracted.
- TP16 validation status: NOT YET — no TP16 packs existed to
  validate. Build now RUNNING directly via ssh (CPU/disk only, no
  GPU/daemon): spark8 (idle; spark3 busy with the coordinator's
  qwen38 nvfp4 build), setsid+nohup, pid logged at
  /home/spark8/dsv4pro_tp16/build.pid (chain pid 409588).
- First spark3 attempt failed fast (contract staged in tools/ not
  model_contracts/) — restaged correctly on spark8.
- Chain: full pack on warm (sha must equal Aug-27 pin 0b1692a3...,
  RED stop on drift) -> 16 TP16 rank packs, each with
  --verify-output + tools/dsv4_pro_rank_pack_verify.py (directory +
  sampled payload/scale bytes vs GA checkpoint). Receipts to
  /mnt/model-warm/packbuild/dsv4pro-tp16/. At ~700MB/s the full pack
  lands ~15:00; ranks take a few hours after.

## ~14:5x — build healthy; operator devolved pack generation to model devs

- spark8 chain alive (pid 409589): full-pack temp at ~140G and growing
  ~580MB/s -> ETA ~15:10, then the 0b1692a3 pin check gates rank
  sharding. PR 752 quiet.
- Merged main 4a3359d: operator directive — stagepack generation is
  now the MODEL DEV's job (guide build runbook). This lane is already
  compliant (family packer, warm source, receipts + verifier green;
  placement proof comes at deploy). Roadmap's dsv4-pro line unchanged:
  pro packer TP16, verifier pins fixed = PR 752.
- ~15:0x — build confirmed healthy via /proc/io sampling (~400-580MB/s,
  162G/893G written; the stale tmp mtime was a ceph timestamp quirk).
  ETA sha-pin checkpoint ~15:35. PR 752 still quiet.
- ~15:1x — 389MB/s sampled (184G written of 893G); ETA sha-pin
  checkpoint ~15:45. PR 752 quiet; no new main commits.
- ~15:2x — 213G/893G; bursty (instant 539MB/s, 5-min avg ~60MB/s) -
  read-side contention on warm (coordinator qwen build shares ceph).
  Letting it run; no resume support so restart is pure loss. PR 752
  quiet, no new main.
- ~15:3x — 330G/893G, ~390MB/s sustained this window (contention
  eased); ETA sha checkpoint ~25 min. PR 752 quiet, no new main.
- ~15:4x — PR 752 MERGED (01a5d84, ratchet re-measured exact) — the
  "fix verifier pins" roadmap item is landed. Build 628G/893G, ~11 min
  to the sha-pin checkpoint.
- ~15:5x — 684G/893G (77%), ~9 min to sha checkpoint. (Earlier "files
  missing" scare was ls-without-a missing the dotfile tmp; log is 0
  bytes until the packer finishes — use ls -a on future ticks.)
- ~16:0x — 783G/893G (~300MB/s); ~5 min to completion. No PR/main
  movement.
- ~16:1x — 883.8G/892.9G; write finishing, then verify-pack + full
  sha256 read pass (~30-40 min) before the pin verdict.

## 2026-09-01 — queue live era; TP16 fullpack pin PASS; decode-speed realign

- Worktree moved: /tmp/lane-dsv4pro was wiped by macOS /tmp cleanup;
  lane now at /Users/mac/lane-dsv4pro (branch lane/dsv4pro-dev, tip =
  main 6d84440 + lane-history merge 6451e4d). Queue state moved to
  ~/.sparkpipe/queue (docs/SPARK_QUEUE_RUNBOOK.md); per-checkout
  runs/queues are DEAD — always use
  python3 /Users/mac/sparkpipe/tools/spark_queue.py.
- TP16 FULLPACK DETERMINISTIC-REBUILD PASS: the spark8 direct chain
  finished ~15:18 + verify-pack green; FULLPACK-SHA equals the Aug-27
  pin 0b1692a3ebd58957... exactly. The packer is deterministic
  end-to-end from the GA checkpoint.
- Ranks chain bug (mine): passed --verify-output in the SAME call as
  the shard — the flag REPLACES sharding with verification, so it
  ENOENT'd on rank0. Fixed script (two calls/rank + resume-safety)
  = tools/dsv4pro_tp16_ranks.sh; staged to spark8/9/0/sparke.
- Queued via the live queue (p5, --by dsv4pro, ttl 120 each):
  dsv4pro-tp16-r0-3 [spark8], -r4-7 [spark9], -r8-11 [spark0],
  -r12-15 [sparke]. Currently HELD behind the fleet waves
  (glm53full-bf16-wave1 p0 + qwen38flash-wave p5-aging-to-0 — both
  cover all 16 nodes; the dispatcher's priority barrier is correct
  queue discipline, not a fault). They run when the waves clear.
- Decode-speed realign (driver directive 09-01: keep pushing
  non-spec inference speed; prefill hill climb waits for kimidev's
  prefill-bug fixes):
  1. The deployed TP4PP4 packs (14 nodes) are blocked ONLY on stale
     Aug-17 bin/lib. Compile recipe exists (tools/glm53_m5_stage.sh
     shape: host build -> module publish (GPU validator needs the
     node's GPU! publish validates against a real rank pack) ->
     sparkpipe_model_compile -> adapter -> registrar fleet deploy).
     NEXT: adapt for Pro (modules/dsv4_resident_decode_stage
     Makefile.pro; firmware jsons exist incl. a _b1 single-slot
     variant), queue the build on one node, then smoke.
  2. Firmware observation (checked, NOT the status=9 cause): both Pro
     firmware jsons say packed_dspark_layer_count:1 while packs carry
     PRO_MTP_PACKED=3 — but no code reads that field (grep: no
     consumers in .c/.h). Left as a doc-accuracy nit.
  3. No TP16 deployment config exists yet
     (examples/deployments has only dsv4_pro_tp4_pp4_*); TP16 serving
     needs one after the rank packs validate.

## 2026-09-02 — exit-4 root cause: MY verifier's stacked-fp4 layout; RED-stop cleared

- The queued rank builds r0-3/r4-7/r8-11 exited 4: ONLY the stacked-fp4
  expert samples (kinds 19/20) mismatched, every non-stacked sample OK.
  Byte-level diff on rank0: payload identical; first diff at exactly
  row0's scale offset — my expected-bytes builder interleaved
  payload+scale per row, the sharder writes all-payload-then-all-scales.
  Verifier bug, not a pack bug.
- Fix landed as PR 768 (ratchet 237353 exact, digests last, gates 0);
  fixed verifier restaged to spark8/9/0/sparke; r12-15 (running) picked
  it up for its remaining verifies; r0-3b/r4-7b/r8-11b resubmitted
  cpu-class (resume logic redoes ranks cleanly — failed receipts are
  empty by design). Coordinator's exit-4 note answered via
  dsv4pro-exit4-rootcause-note.

## ~05:4x — driver-rebuild task staged + queued; ranks redos running

- tools/dsv4pro_driver_rebuild.sh (queued as dsv4pro-driver-rebuild,
  spark0, gpu-class, ttl 15 + will need lease attention): host build →
  make -C modules/dsv4_resident_decode_stage -f Makefile.pro publish
  (GPU validator vs the DEPLOYED rank0 pack; geometry mirrored from
  the deployed config: 4/16 layers, 33024 positions, MTP=3, slots 13,
  pages 1024/16384, active 1024) → sparkpipe_model_compile with the
  Pro firmware json → adapter/binaries staged as .new (fleet deploy +
  smoke are separate later tasks). Staged checkout at
  spark0:~/dsv4pro_checkout (67M).
- Queue discipline note: glm53full-bf16-wave1b (p0) now holds all 16
  GPUs — the driver rebuild dispatches when that window clears. My
  cpu-class rank redos (r0-3b/r4-7b/r8-11b on spark8/9/0) coexist and
  are running; r12-15 still on rank12+ on sparke.
- TTL lesson: the queue caps tasks at 15 min now (renew hint exists in
  the error text but no renew subcommand yet — lease attention per
  tick, or the detached-launch + reserve pattern if the reaper kills
  long builds).

## ~07:1x — dedicated spark3; all 16 ranks in flight on 4 nodes

- Operator: this lane has a spark dedicated for CPU use = spark3
  (coordinator log "Kept: spark3 pro-repo"). Stalled sparke ranks
  12-15 (≈1h/rank under burst I/O) CANCELLED clean (remote reaped,
  verified) and resubmitted cpu-class on spark3 (dsv4pro-tp16-r12-15-s3,
  stale-tmp cleanup added to the script).
- rank0 FULLY VALIDATED (fixed verifier, by hand on spark8: directory
  OK, all 8 sampled sections OK incl. both stacked-fp4 experts). The
  earlier "scale mismatch" was sampler artifact #2: the combined
  payload+scale read overran the payload section into expert-8's
  weights; scales match at their own directory offset. Fix pushed to
  PR 768 (ratchet 237445 exact) + verify-only finish script.
- r1-3-s8 queued (1-3 were never sharded — r0-3b died at rank0 under
  set -e). In flight: r1-3-s8 [spark8], r4-7b [spark9], r8-11b
  [spark0], r12-15-s3 [spark3] → 15 ranks remaining to receipts.
- Gate flake: build/test_tp_collective HANGS on clean main too on this
  Mac (standalone rc=124) — documented on PR 768; branch's py/shell
  gates green throughout; full gates exit 0 receipt stands from the
  237353 run.

## ~07:3x — PR 768 landed (519a241); receipts 3/16 green, rest in flight

- Coordinator merged the verifier fixes (section-split sampler +
  verify-only + ratchet 237445) as 519a241; lane fast-forwarded.
- Receipts: rank0/1/4 fully OK; rank8 verifying now; 12 more in
  flight across spark8/9/0/3 (burst contention makes each rank
  ~30-60 min). No mismatches — the RED-stop class is closed.
