# hy4 lane report — TP16 shards deployed, 2026-09-01

## What was asked
Source of truth ruling (operator): **AngelSlim/Hy4-preview-GGUF `UD-IQ1_M`**
(235 GB, ~1-bit dynamic; image workload tolerates the error profile). Build
TP16 rank shards, place one per spark, include MTP data if it exists.

## Delivered
- Warm source: `/mnt/model-warm/hy4-preview-ud-iq1m-angelslim/` (gguf +
  README + llama.cpp patches + tensortypes; DOWNLOAD-RECEIPT.json, PUBLISHED).
  **Integrity proven cold**: after `drop_caches`, sha256 =
  `12d325844103bac75bd286d14e0e45f87e35e8e60401877282a30b6f26ba6ac6` — exact
  match to the Hub LFS oid (2m47s real-storage read).
- Deployed: `/home/<host>/sparkdata/hy4.ud-iq1m.tp16/packs/rank-{00..15}/` on
  all 16 sparks, rank r → peer_hosts[r] (spark0..sparkf). Per rank:
  `model-ud-iq1m-tp16-rank-XX.gguf` (18.72 GB) + `manifest.json` (hy4-tp16-shard-v1:
  per-tensor slice provenance) + `.sha256` sidecar + `verify.out`
  (**16/16 remote sha256 -c PASS**). df receipts in the place log; worst node
  keeps 168G free.
- Tool: `tools/hy4_tp16_shard.py` — GGUF v3 parser, **empirical type-geometry
  solver** (solves block/bytes-per-block from file offset windows — AngelSlim's
  build extends the ggml enum, e.g. custom types; never trusted), byte-integrity
  gate (solved tensor bytes + alignment integrate exactly to the data section),
  split1/split0/split2/replicate rules, `--verify` (re-parses each rank file,
  byte-compares sampled tensors vs source).

## Shard design (block-correct)
GGML `ne[0]` is the fastest/input axis and quant blocks pack along it.
- `split1` (safe at any granularity): token_embd/output (vocab 7552/rank),
  attn_q_b/kv_b/attn_gate, indexer q_b (head axis on dim 1).
- `split0` (block-aligned 1024): attn_output (o_proj, head input axis).
- `split2` (slab): fused routed experts `[in, ff, 256]` → 16 experts/rank;
  decomposed attn_k_b/attn_v_b heads.
- `replicate`: q_a (2048 lora-out can't divide at 256-block granularity),
  shexp + dense FFN, router, norms, hc params, indexer smalls — 4.28 GB/rank.
- Per rank: 18.72 GB, 2134 tensors. **No MTP tensors in the GGUF** (blocks
  0–77 only); the MTP layer exists only in the FP8 safetensors reference.

## Incidents + lessons (read before reusing any of this)
1. **Cache-resident sha256 proves nothing.** The first download "verified"
   by reading straight out of page cache; cold reads then hung. Every
   verification of a just-written big file must run after
   `sync; echo 3 > /proc/sys/vm/drop_caches`.
2. The hang itself was a **transient ceph client wedge** (Ds folio_wait),
   not corruption: after drop_caches the file scanned clean end-to-end at
   3.5 GB/s and cold-sha'd bit-perfect. Name quarantines until proven.
3. **GB10 multi-stream small-write pathology**: 16 interleaved ~1 MB writers
   jam page writeback to ~9 MB/s (even local NVMe); large per-rank sequential
   writes sustain 1.1 GB/s. Buffer per file, flush big. (Watchdog +
   vm.dirty_bytes tuning kept as belt-and-suspenders.)
4. Two writer bugs caught by gates before placement: slice length used the
   full-tensor size; slice offsets mixed data-section-relative with absolute.
   The manifest-vs-file-size cross-check is what exposed them — keep it.
5. `pkill -f` self-match killed my own ssh twice. Bracket-trick always.

## M1 follow-up (same day): contract freeze
- `model_contracts/hy4_authoritative.json`: geometry + identity pinned to the
  GGUF (`AngelSlim/Hy4-preview-GGUF` @779242ed, cold sha `12d32584…`, 2134
  tensors, 2,133,519,743,36? bytes — see file), **all 16 deployed rank digests**
  (probed back off the nodes), mtp.shipped_layer_count = 0 with the FP8 pointer.
- `tools/generate_hy4_contracts.py` emits `spark_hy4_model.h` (SPARK_HY4_MODEL
  defines incl. per-rank splits: vocab 7552, heads 4, index heads 2, experts 16)
  + `model_contracts/hy4.json`, byte-exact `--check`.
- `tests/test_hy4_model_header.py` registered in Makefile PYTHON_TESTS.
- Publisher config vendored at `model_contracts/references/hy4_preview_config.json`
  (FP8 revision pinned in the README entry).
- Gates run locally (exit codes): test_hy4_model_header PASS, test_code_size
  PASS at CEILING=237866 (+955 justified in-file), test_dry_law PASS,
  test_dsv4_contracts PASS, test_config_coverage PASS, test_staging_manifest
  PASS, test_template_adoption PASS. build-all/package-manifest left to the
  coordinator's offline-gates run.

## Addendum 2026-09-02: alert withdrawn, provenance re-closed, capability note
- **Capability:** Hy4-preview is TEXT-ONLY (no image input, no image output;
  model card + config + GGUF tensors + AngelSlim README all agree). Lane
  proceeds as a text model per operator. The "200GB GGUF" = UD-IQ1_M at
  219.83 GiB.
- **Storage alert withdrawn:** operator could not reproduce degradation
  (16-node 4KiB + direct-I/O checks identical, ceph HEALTH_WARN auth-only,
  zero writes). What IS reproducible from spark2: intermittent multi-minute
  stalls of cold bulk reads of this file (D-state folio_wait), each following
  heavy multi-stream IO or drop_caches; NaN "corrupt" reads were torn client
  pages during those windows. File itself healthy.
- **Provenance re-closed post-anomaly:** tensor-level probe across all 16
  nodes — replicated blk.0.attn_norm byte-identical everywhere and equal to
  warm; token_embd rank slices for ranks 0/4/9/15 equal to warm. Plus
  16/16 on-node sha256 -c re-verified. **Deployed shards = artifact of
  record.**
- **CPU verification (no GPUs):** BPE tokenizer round-trip from the GGUF KVs
  PASS (prose/code/prompt, exact decode; first word carries no space marker;
  byte-unicode inverse must map mapped-char -> chr(byte)).
- Operational rule: verification-grade warm reads of this file via direct
  I/O from a non-spark2 node; see /home/spark2/HY4-WARM-READ-NOTE.txt.

## 2026-09-02 (timer tick 1): IQ dequant ground truth landed
- Vendored `tools/hy4_dequant/hy4_iq_dequant_vendor.h`: tables + dequant math
  extracted verbatim from ggml-org/llama.cpp @0cea36222 (the commit the
  AngelSlim build pins) — kmask/ksigns_iq2xs, iq2xxs/iq3xxs/iq1s grids,
  iq2_xxs/iq3_xxs/iq1_m row dequant, IEEE-binary16 fp16 conversion. Byte
  geometries cross-check the sharder's empirical solve (66/98/56 B/256).
- CPU reference on REAL rank-02 tensors (spark2, node-local pack):
  type 16 blk.47.ffn_gate_exps 12.58M elems amax 0.2094;
  type 18 blk.47.ffn_down_exps amax 0.2035; type 29 blk.46.ffn_up_exps
  amax 0.2026 — zero NaNs, grid-quantized value structure as expected.
- Every weight class in the file now has a working reference path:
  F32, Q8_0 (GPU bitwise-PASS vs CPU), IQ2_XXS, IQ3_XXS, IQ1_M-class.
- Next: GPU bitwise cell for the IQ classes (same pattern as cell 2),
  then the rank-manifest loader.

## 2026-09-02 (timer tick 2): rank-manifest loader landed
- `tools/hy4_dequant/hy4_rank_loader.{h,c}`: opens a deployed rank bundle
  (manifest.json + rank GGUF), joins manifest slice provenance with GGUF
  header offsets, exposes typed tensor views + reads. Host-side; rank file
  only (no warm/ceph).
- Loader test vs real rank-02 on spark2 CPU: 2134 tensors; token_embd
  vocab slice (dim1, start 15104, 26.1MB), replicated Q8_0 kv_a_mqa
  (3.76MB), expert slabs (dim2, start 32, 16 experts, 51.9/77.1MB),
  F32 output slice — all verified. LOADER TEST OK.
- GPU IQ dequant cell (hy4-gpu3) queued but blocked behind
  glm5next-serving-kimidev's fleet-wide p0 hold; will dispatch when
  released. No nodes taken this tick.

## 2026-09-02 (timer tick 4): MoE FFN forward — WIP with one open bug
- Router implemented + run on real weights (rank-02, blk.47): sigmoid probs,
  bias-only-selection top-8, norm×2.827 weights — selections are stable and
  weights uniform ≈ 2.827/8 for correlated saturated inputs (expected).
- Forced-expert receipt path (expert 40, rank-local, slab dequant via dim-2
  contiguous slabs) runs but produces NaN in the routed partial. Dequant of
  the same full tensors is NaN-free, layouts check out (ne0-fastest row-major
  per matvec), so suspicion is in the slab path or an uninitialized buffer.
  NEXT TICK: bisect the slab dequant (dump slab stats) before any GPU work.
- Shared-expert path correctly refuses type 14 (Q6_K) — vendor needs Q4_K/
  Q5_K/Q6_K/IQ4_XS dequants added (they also appear in attn/o_proj weights).
- GPU cell hy4-gpu3 still queued behind glm5next p0 fleet-wide hold.
