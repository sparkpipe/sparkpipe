# glm5-dsa — the DSA site is CLEAN (checkpoint-semantics oracle, 199-pass
# ladder); the swiglu clamp kernel NaN'd the MoE and was convicted by bisect
# and reverted; the fleet runs the R3-abi-matched artifact set; generation is
# back at the attractor baseline ("way,ive database:") — gates stay blocked

Lane `lane/glm5-dsa`, worktree /tmp/lane-glm5dsa, pushed tip = this report's
commit (two commits on 8780fc1: the instrument+fixes landing, then the
wave-script detach fix). Fleet: 16/16 on driver `571cccd74ff9fff3`
(module artifact `3be964c4…`-lineage, validator PASS 0 failures at every
build), adapter `a3a051d592708f19`, residentd `1d4412f51737e5e3`, per-rank
configs regenerated (decode_split_context_threshold). All launches went
through the queue (g5dsa-wave3…wave10). Every claim below = command + raw
output (logs on spark0: /tmp/g5dsa-wave*.log, /tmp/g5dsa_vec1.log 195 MB,
oracle outputs /tmp/g5dsa_oracle_l3*.out).

## 1. The instrument (this landing)

- **G5N-VEC extended to the DSA site** (env `SPARK_GLM5_NEXT_PROBE_VEC_DSA`,
  layer `SPARK_GLM5_NEXT_PROBE_VEC_LAYER`, default 3; U16 dump cap widened
  8192→16384 for the streams surface): at the probed DSA layer the dump
  ladder is hc_streams → hc_collapsed/mixes/pre/post/comb → attn_normed →
  q_compressed → q → kv_slot → query_latent → attn_latent → attn_value →
  attn_out_partial.
- **`tools/glm5_next_dsa_host_oracle.py`**: independent host
  reimplementation from the CHECKPOINT's semantics — fp8 e4m3 block-dequant
  for q_a/q_b/kv_a/o_proj (expanded across the [128,128] scale blocks and
  rounded to bf16 exactly as the pack loader does), bf16 kv_b absorption
  (query_latent[h] = q[h]·W_k[h]), the mHC mix/sinkhorn/collapse math, the
  softmax over the oracle's own cache (kv_slot dumps), NoPE. Same stage-
  ladder discipline as the KDA oracle: every stage consumes the DUMPED
  upstream buffer, so a divergence convicts one kernel.
- **The KDA vec probe is layer-selectable** (`SPARK_GLM5_NEXT_PROBE_VEC_KDA_LAYER`)
  with binding-conviction dumps (w_attn_norm / w_qkvb_row0 / w_kda_out_row0)
  and HC-site state dumps (k_pre/k_post/k_mixes/k_streams_head/k_snapshot_head);
  `G5N_KDA_ORACLE_LAYER`/`G5N_KDA_ORACLE_PASS_OFFSET` parameterize the KDA
  oracle. The wave task script + `--probe-vec` knobs (G5N_VEC_DSA/LAYER/KDA_LAYER)
  are committed.

## 2. VERDICT: the DSA site is CLEAN

Cold canonical run (fixture case recNu3MXkvWUzHZr9, 176 tokens, first client
request, served:0), passes P1..P200 = positions 0..199 exactly (the engine
ran 11×16-row prefill frames as 176 single-row waves + 24 decode frames; NO
warmup traffic in this build's session — frame req 1 IS the fixture):

    attn_normed      100.00% exact   (attn RMSNorm mapping confirmed)
    q_compressed      99.8-100%      (q_a fp8 GEMM + q_a_layernorm)
    q                 99.9-100%      (q_b fp8 GEMM, rank rows = heads 0-3)
    kv_slot           99.2-100%      (kv_a fp8 GEMM + kv_a_layernorm; the
                                     oracle's cache row = the module's row)
    query_latent     100.00% exact   (THE ABSORPTION: q[h]·W_k[h] vs
                                     kv_b_key_transposed per head)
    attn_latent      100.00% exact at EVERY pass except one 41-ulp element
                     at pass 89 (a near-tie softmax flip; 99.95% exact)
    attn_value       100.00% exact   (v path from the dumped latent)
    attn_out_partial  99.9-100%      (o_proj col-shard rank partial)
    hc_collapsed     100.00% exact   (the pre-reduce)
    hc_mixes/pre/post/comb: f32 words differ (reordered 16384-length dots)
                     at max_rel ≤ 1.1e-5 — accumulation order, not math;
                     the oracle's f32 exact% gate is too strict for these.

**The entire DSA sublayer — mHC wrap, nope-absorbed scoring, latent cache,
softmax, v path, o_proj — matches the checkpoint's semantics within
last-ulp bf16 tolerance.** Suspect (c) from the attractor brief is CLOSED.

## 3. THE SWIGLU CLAP KERNEL: convicted NaN source, REVERTED

Wiring the reference's swiglu_limit clamp (Glm5NextSwigluLimitKernel,
gate.clamp(max), up.clamp(±10)) at the dense/expert/shared sites NaN'd the
MoE partial from the FIRST wave:

    G5N-PROBE r0 post L3 bf16sum 2006207 f -0.540016 1.05271 0.383289 ...   (attn post, healthy)
    G5N-PROBE r0 post L3 bf16sum 2714140 f nan nan nan -nan                 (mlp post, NaN)

The attractor-era log has ZERO `nan` in 160 MB. Bisect wave10 (revert ONLY
the swap, reset+probes kept): 0 NaN, and the curl returned the attractor
baseline `[3117,11,533,220,220,220,220,4625,25,220...]` = "way,ive
database:". The kernel is arithmetically incapable of NaN on finite inputs
(pure clamps + silu·mul), so the mechanism is codegen/register-latent in the
MoE chain — the clamp stays UNWIRED, now with this evidence recorded in
layer.cuh at the site. Fixture-traffic magnitudes (max gate 2.59, max |up|
2.27) mean the clamp was inert anyway; do NOT re-attempt without first
explaining the NaN mechanism.

## 4. Operational fixes landed tonight (each was a fleet-blocker)

1. **Adapter/driver ABI skew**: R3 flash-decode (7ceb7cc) bumped
   NODE_CONTEXT_ABI 4→5 after the deployed adapter was built — every wave
   failed `initialize=abi_mismatch status=10` at adapter_initialize.
   Rebuilt+deployed the full artifact set (adapter/residentd/api/transport)
   from main+lane; hand-verified rank0 passes init.
2. **Config schema**: the new adapter requires `decode_split_context_threshold`
   in config/stage.json (rc=6 schema_error without). Regenerated per-rank
   configs. NOTE: threshold semantics verified — 0 and 32768 both keep the
   base kernel (the split path never engages at fixture context; R3's split
   path is EXONERATED as the inf source by this run).
3. **start_api detach**: the wave script's `nohup … &` api launch held the
   launch ssh for 31 minutes (fd-inheritance) and set -e turned it into
   WAVE-FAIL; now setsid-detached.
4. **Same-sequence wave cap**: the module caps a multi-row same-slot frame
   at one row per wave (documented in RoundMajorWaveRows) — defensive; the
   frame walk was already per-row in practice (199 passes observed).
5. Rollback backups on every host: `*.pre-g5dsa` (driver/adapter/transport/
   residentd/api) and `config/stage.json.pre-g5dsa`.

## 5. What remains (evidence, not chase)

Generation is AT the attractor baseline, not past it: the DSA site, the KDA
cell, the MoE tail, and the HC numerics are all now verified; the residual
degeneration lives elsewhere. Ordered by leverage:

1. **The MoE body at depth is the last un-oracle-verified compute family**
   (router fp32 sigmoid noaux_tc, norm-topk, expert fp8 GEMMs, shared
   expert). The same dump-fed oracle method applies at L3's MLP site:
   dump route logits/topk/weights + the packed gate_up/intermediate rows
   and verify against checkpoint math. The L3 mlp-post NaN instrument
   (already in the log) gives the pass/fail signal for free.
2. **Depth effects in HC**: the L44 comb ~one-hot (0.9997/0.0009/…)
   remains unadjudicated; the hc_* f32 tolerance in the oracle should be
   rel-based (exact% is meaningless for reordered dots) before that arb.
3. **Indexer + kpool** for M5-32K: top_k=2048 TOKENS (512 pools + tail,
   output width 2051) is the module's reading and matches the name map;
   indexer rope is still skipped (recorded; live only >2048 context).
4. swiglu_limit: unwired (see §3) — do not retry blind.

## 6. Integration request

1. Merge `lane/glm5-dsa` (tip = this commit): the DSA-site instrument + the
   clean verdict, the swiglu NaN conviction + revert, the operational fixes,
   the layer-selectable KDA instrument. No serving-math changes vs the
   attractor driver except the (reverted) clamp — the deployed binary is
   behaviorally the attractor math + probes.
2. Keep the G5N-VEC ladder + both oracles until coherence; they are the
   localization template that closed suspect (c) in one run.
3. Ratchet 228764 → 229291 exact, justification in-commit and in
   tests/test_code_size.py (all instrument/diag lines; the serving path is
   byte-identical to the attractor math).
