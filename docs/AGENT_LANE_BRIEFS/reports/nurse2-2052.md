# Nurse2 sweep 4 — 2026-08-29 20:45–20:52 UTC (lane/nurse2)

Telemetry gate OPEN @20:45Z: `active_nodes=0 avg_gpu_pct=0.0
input_tok_s=0.0 reachable=16`, zero busy nodes — WITH 16/16 GPU nodes
leased to a task that claims a running wave. The discrepancy IS the
escalation:

## ESCALATE

- **GHOST WORK — g5dsa-wave3 holds all 16 node leases (TTL -> 21:01:32Z)
  with zero compute for 13+ min past bring-up** (owner: lane-glm5dsa;
  dispatched 20:21:32Z; ranks ready ~20:32Z). Evidence (spark0):
  `/tmp/g5dsa-wave3.log` last lines —
  `ready: 0/16` x4, `ready: 16/16`, `== api on spark0:8433 ==` —
  NO advance 20:32Z -> 20:47Z (three looks: 20:33, 20:40, 20:47);
  `nvidia-smi` spark0 = `0 %, [N/A]`; API port 8433 IS OPEN (listener
  alive, so ranks/up = real; the DRIVER is not driving);
  telemetry 20:45Z = 0 busy / 0 tok_s. Past the 10-min window ->
  ghost-work class per charter. Likely cause (one read, not
  troubleshooted): the wave driver's post-API stage (probe-vec
  PASSES=200 DSA=1 LAYER=3) never fired or is hung on the API not
  answering — coordinator owns fix/cancel; 16 nodes x ~26 min TTL is
  the minute-for-minute loss. Note wave1/2 both also died in
  bring-up — third consecutive glm5-dsa wave failure pattern.

## WATCH

- q27b-build-1 + weightd-vmm-verify-gpu-t4 + bf16-replace-verify-true
  all sit barriered behind wave3 — freeing the 16 nodes unblocks all
  three (spark5 one also clears pass2 coexistence question).
- wave3's TERM phase "cleans the timed-out ranks" — if the coordinator
  cancels, expect residentd cleanup on 16 hosts; any survivors are
  theirs to TERM (never mine).

## Healthy / verified

- Dispatcher ALIVE; blocks/holds all correct; 3 notes skipped.
- bf16 pass2: rank 2 -> spark2 in flight (18/19 lines) — on pace,
  ETA ~23:45Z. The ONLY fleet activity is this disk push.
- Nodes 16/16 reporting, error_streak=0; no node alarms.
- k3finish hard-flag carried (quiet 129m+ dirty=2);
  toksidecar flag carried (dirty=14, quiet ~110m).

## Actions

Read-only + this report.
