# Fleet staging completion report — 2026-08-28 (lane/staging-v2)

Mission served: every served model's packs verified on the right nodes per
the all-16 policy, disk pressure removed as an incident class, and staging
turned into a CHECKED state by tool. S1 gap table + S2 manifest (committed
before any deletion): `staging-inventory-2026-08-28.md` in this directory.

## Scoreboard for the fleet goal

- **dsv4_pro 16/16 COMPLETE** (was believed 14/16 with ranks 1/2 stashed on
  warm): the Aug-17 packs already on spark1/2 are byte-identical to the
  regenerated receipted set (deterministic packer) — verified by full sha256
  against the warm receipts this session; receipts written on both nodes.
  No 186G fill was ever needed; the warm stash (186G) is now redundant
  (its owning report says delete after deploy — coordinator's call, shared
  storage).
- **glm5_next TP16 16/16 verified present** with the full 16-rank sha256
  manifest pinned (the lane report's open item) and receipt.json written
  next to every pack. NOTE: spark9 still hosts the full 16-pack emission
  set (324G) — the lane's distribution residue, kept (their lane is
  active; flagged for their post-bring-up cleanup).
- **K3 stage-3 4/4 verified in place** (all four sha256 == warm receipts ==
  lane report digests). Stages 0-2 remain the K3 lane's build (pending).
- **qwen4_flash v3 = the verified set** (`packs_v3/`, 56.06 GiB/rank,
  per-node own rank on spark4-7; `deploy_v3/config/adapter-r*.json`
  references only packs_v3). v1+v2 retired fleet-wide after each node's v3
  pack sha-verified against its receipt.
- **qwen38max is NOT "not built"** — the brief's state was stale: 15/16
  rank packs exist on spark7 (pp0-2 complete + receipts, pp3 r0-2 present,
  `pp3.tp4-rank3` MISSING, rank3 receipts missing). Deliberately NOT
  distributed: baking a half-set onto 16 nodes is worse than the single-
  node hoard. Pending: shard lane builds pp3-rank3 + verifies rank3s;
  staging then distributes (~80-98G x 15 nodes over the fabric).

## S2 execution log (after the committed manifest; guards = lsof, sha)

| Node | Deleted (exact paths in the manifest) | Freed | df before -> after |
|---|---|---|---|
| spark1 | dsv4_pro_ga.val0p3/val0p4.spstage, glm52.fp8.pp13/ | ~241G | 63% -> 58% |
| spark2 | glm52.fp8.pp13/, qwen38.fp8.tp1.oldaug26/ | ~135G | 46% -> 44% |
| spark3 | qwen38.fp8.tp1.oldstack/ | ~57G | 79% -> 78% |
| spark4 | qwen4_flash packs/ (v1) + packs_v2/ | ~226G | 28% -> 21% |
| spark5 | qwen4_flash packs/ (v1) + packs_v2/ | ~247G | 49% -> 42% |
| spark6 | qwen4_flash packs/ (v1) + packs_v2/ | ~291G | 74% -> 67% |
| spark7 | qwen4_flash packs/ (v1) + packs_v2/ | ~247G | 56% -> 49% |
| sparka | qwen4_flash packs/ (v1 rank0 hub residue) | ~31G | 15% -> 14% |
| sparkb | dsv4_pro.full/val4/valtail/ga_hdr.spstage + empty rankwork/regenwork | ~1,052G | 59% -> 31% |

Total reclaimed: **~2.53 TB**. Every deletion: `lsof +D` clean first;
spark4-7 deletions ran LAST and only after each node's own v3 pack
sha-verified (spark4 `d08ccfec...`, spark5 `183bf7fc...`, spark6
`d3d66c6a...`, spark7 `4fbc9336...` — all equal to their receipts).

**Guard held once, as designed**: spark1's stray glm52 rank04 was NOT
deleted — its sha `a4f67c5e...` != the band's verified rank04
`b825eea6...` (old packer generation, not a byte-duplicate). It stays,
flagged: `/home/spark1/sparkdata/glm52.tp8.fp8/packs/` (96G, glm52 is not
a fleet model; retirement is the glm52 lane/coordinator's).

`*.predebug` backups: none exist on any node (find across /home + /tmp).
`rdma_debug_build` on spark0: untouched (mine, keep per brief). No .tmp
packs anywhere except inside the keep-flagged a95b tree on spark1.

## S3 fills (all receipt-level; zero pack bytes moved)

1. dsv4_pro rank1/rank2 receipt.json -> spark1/spark2 (from the warm stash
   receipts, whose shas the deployed packs match exactly). 16/16 nodes now
   carry receipts.
2. qwen4_flash v3 rank1/2/3 receipt.json -> spark5/6/7 (from spark4); all
   four nodes' own packs sha-verified == receipts.
3. glm5_next: full 16-rank sha256 sweep; receipt.json written next to
   every pack (digests embedded in tools/staging_manifest.py; rank0's
   `ca0e427c...` supersedes the emission-time prefix `4ba9b31f...` — the
   packs were header-patched in place post-emission, which the old prefix
   predates).
4. 27B trio (spark2/9/a) sha-identical: `ceef03e4...` — recorded as the
   set's receipt digest.

## S4 the CHECKED state

`tools/staging_manifest.py` (+`tests/test_staging_manifest.py`, registered
in PYTHON_TESTS; ratchet 208406 -> 208749 with in-file justification):

```
$ python3 tools/staging_manifest.py --fleet        # controller, ~40s
STAGING GAPS (1):
  dsv4_flash.fp8.tp16.b1@sparkf: SIZE-DIFF(10.1G != 9.0G)
$ echo $?
1
```

The one standing gap is REAL and pre-existing: sparkf's dsv4_flash tp16
pack is a different generation (10,072,381,148 B vs 9,013,048,832 B on the
other 15). Owner: dsv4flash lane/coordinator (staging flags, does not
arbitrate un-briefed models). `--verify` adds full sha checking;
`--strict` makes PENDING items gate too. Runbook line for every lane: run
it before any fleet bring-up and after any pack write.

## S5 disk budget proposal (for the README, from measured state)

Measured current-policy pack load per node class (post-cleanup):
spark0-3 ~124G; spark4-7 ~184G; spark8-b ~227G; sparkc-f ~325G.
Forward load with everything the fleet table promises (K3 stages 0-2
+~98G on their nodes, qwen-max +80-98G everywhere, 27B re-emit +3.4G):
worst node ~430G.

Proposed per-node budget line:

> **Pack budget: ≤ 450 GB of model packs per spark (the complete fleet
> table incl. qwen-max and the 27B re-emit fits with margin); free-space
> floor: no pack build/deploy below 500 GB free (KV page-store backing,
> OSD, and build scratch live below that); act at 85% filesystem use.**

Post-cleanup state satisfies it everywhere: all 16 nodes now 12-78% used,
worst node spark3 at 78% (its remaining bulk is the keep-listed
qwen38_2.4t_a95b 136G + k3 tp16/stage0 artifacts 143G + 27B variants —
all flagged in the inventory's keep-list for their owners).

## Pending / handoffs

1. qwen38max: build pp3-rank3 + rank3 receipts (shard lane), then staging
   distributes 16x ~80-98G rank packs (rerun manifest --strict).
2. dsv4_flash@sparkf generation drift (the tool's standing exit-1).
3. Warm cleanup candidates (coordinator): dsv4pro stash 186G (deploy
   verified), k3/packs reference copies 366G (stage-3 restored+verified on
   nodes; stages 0-2 rebuild from source anyway).
4. Keep-list retirements when their lanes land: spark9's 16-pack glm5_next
   emission residue (324G), spark1/2/3 k3 tp16+stage0 artifacts (429G),
   spark1/2/3 qwen38_2.4t_a95b (455G), spark0-3 dsv4_flash old topologies
   (~440G), spark1 stray glm52 (96G).

## INTEGRATION REQUEST

1. Makefile: one line added to PYTHON_TESTS
   (`tests/test_staging_manifest.py`) — shared-file edit directed by the
   staging brief; diff is that single line.
2. Coordinator note: local `main` in the shared checkout carries a
   committed conflict marker in tests/test_code_size.py at its tip sweep
   commit (origin/main is clean; my branch rebased onto origin/main).
