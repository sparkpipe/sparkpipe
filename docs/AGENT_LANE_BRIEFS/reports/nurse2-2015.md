# Nurse2 sweep 1 — 2026-08-29 20:05–20:15 UTC (lane/nurse2)

9 sweeps planned (~10 min each). Read-only + documented re-arms; nothing
killed/cancelled. Telemetry gate OPEN:
`active_nodes=0 avg_gpu_pct=0.0 busy_gpu_nodes=0 input_tok_s=0.0
reachable=16` @20:05Z; CLOSE @20:14Z identical (zeros are TRUE state:
wave2 cancelled 19:47Z, wave3 blocked on smoke4's lease, pass2 is a
disk push not GPU work). No report-vs-telemetry discrepancy found.

## ESCALATE

1. **smoke4 zombie-running — lease blocks p0 wave3** (since 20:01:14Z,
   TTL 25 -> self-expires 20:26:14Z). Remote work is FINISHED-FATAL:
   `/tmp/sparkqueue-qwen27b-inference-smoke4.log` on spark5 (mtime
   20:06Z) = `RESIDENTD-NOT-READY / usage: ... / model_residentd
   deployment=io_error` (task's designed exit-3 path, self-TERMs its
   residentd). BUT at 20:13Z: no `/tmp/sparkqueue-qwen27b-inference-
   smoke4.exit` and no `.pid` on spark5; `spark_queue.py list` still
   shows `running`; `runs/reservations.json` still holds spark5
   (holder task:qwen27b-inference-smoke4). Cost: dispatcher logs
   `blocked: g5dsa-wave3 nodes busy` every pass and `held:
   weightd-vmm-verify-gpu-t3 — priority barrier for g5dsa-wave3 (p0)`
   — the minute-for-minute loss class. Coordinator owns the cancel.
   Fallback: if the lease is STILL held after 20:26:14Z TTL expiry,
   the TTL reaper itself is broken -> next sweep escalates that.
2. **smoke bring-up is 0-for-4 — owner action needed (6c)**: attempts
   1-4 all died in residentd bring-up (smoke2/3 cancelled, smoke1/
   smoke4 deployment=io_error). Attempt 4's new config
   `sparkdata/qwen38.fp8.tp1/config/model_resident.smoke.json` EXISTS
   on spark5 (staged 19:55Z, 1071 B) yet residentd prints
   `deployment=io_error` -> invocation/path-resolution bug in the
   residentd launch, not a missing file. Owner: qwen27b-serve lane;
   a 5th re-queue without fixing this will fail the same way.

## Remote pipelines

- **bf16 pass2 LIVE** on spark5 (place_packs.sh pid 789725): pass1
  16/16 `placed` + `PLACE-PASS1-DONE rc=0`; pass2 rank0->spark0 placed
  20:03Z, ~25 min/rank -> `PLACE-PASS2-DONE` ETA ~23:45Z. Pack build
  process gone (build finished). Coordinator-visible check stands: all
  16 ranks exactly 98,019,368,448 B. Note: ranks 4 (spark4) and 8
  (spark8) were placed pre-reboot; pass2's byte check is the net.
- validate rc=0 VOID flag unchanged (ran over mid-write packs; pass2
  re-verify is the designed correction).
- No fetch/verify processes anywhere; no staging label anomalies
  visible from the controller; nothing wedged.

## Node alarms — RESOLVED this sweep

- **spark8 REBOOTED**: up 13 min at 20:07Z, nvidia-smi answers (0%),
  load 0.2. The power-cycle alarm (flagged since 19:48Z) CLOSES.
- **spark4**: D-state residentd pid 3722825 GONE — node rebooted
  (up 11 min). CLOSES.
- **spark3**: answering normally, up 1d20h (was never actually down;
  earlier listing was an ssh/collector path artifact). CLOSES.
- All 16 nodes reporting to telemetry, error_streak=0 everywhere.

## Queue / dispatcher

- Dispatcher ALIVE (wrapper pid 21845), passes clean, correctly skips
  the 3 kind=note entries and correctly blocks wave3 on spark5.
- `g5dsa-wave3` (p0, all 16 nodes, lane-glm5dsa) queued 20:06:53Z —
  first in line the moment smoke4's lease frees. `weightd-vmm-verify-
  gpu-t3` (p1, spark0, cell-runner) queued behind the p0 barrier.
- Idle-node alarm NOT fired: 15/16 free but the only executable queued
  tasks are deliberately barriered — dispatcher behavior is correct.

## Local lanes

- **glm5dsa**: worktree dirty=13, files quiet since ~19:26Z (charter
  >60m flag) — BUT the lane submitted wave3 through the canonical
  queue at 20:06:53Z, so it is alive; WATCH: worktree-quiet >120m.
- **toksidecar**: dirty=14, quiet 76m — FLAG (mid-edit-death watch).
- **k3finish**: dirty=2 (queue.jsonl+reservations.json), quiet 102m —
  FLAG; its launch-ready note task is parked in the queue.
- **drywave1**: detached HEAD with UNRESOLVED MERGE CONFLICTS
  (UU PACKAGE_MANIFEST.json, UU SHA256SUMS), quiet ~12h — dead lane;
  coordinator may want cleanup (never mine to TERM).
- Healthy: qwen27b (clean, active 3m — expected smoke churn), debts
  (committed, report pushed), p1d2 (clean, ahead 1, quiet 15m).

## 6c reconciliation

- `weightd-vmm-verify-gpu-t2` exit -1 "dispatch: pid gone without exit
  file" (19:48Z) — owner cell-runner ALREADY re-queued as t3. Cycle
  working; no routing needed.
- smoke2/smoke3: coordinator-cancelled (see ESCALATE 2 for the pattern).

## Actions

Read-only + this report. Next sweep (~20:25Z) decides ESCALATE 1:
smoke4 lease freed (reap or TTL) vs TTL-reaper-broken.
