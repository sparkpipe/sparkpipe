# Nurse2 sweep 5 — 2026-08-29 20:55–21:03 UTC (lane/nurse2)

Telemetry gate OPEN=CLOSE: `active_nodes=0 avg_gpu_pct=0.0
input_tok_s=0.0 reachable=16`, zero busy — while 16/16 remain leased
to a wave that is not computing. Escalation stands.

## ESCALATE (carried from sweep 4, evidence re-verified 20:56Z)

- **g5dsa-wave3 ghost**: log still frozen at `== api on spark0:8433 ==`
  (nothing since ~20:32Z, 4 wave-script processes alive on spark0),
  0% GPU fleet-wide, 0 tok_s — 24+ min of 16-node lease burn.
  Coordinator owns cancel. **TTL checkpoint 21:01:32Z**: if leases do
  NOT free at expiry, the TTL reaper is broken -> escalate that next
  sweep. If they free, the dispatcher should promote r3flash
  glm52-exact-cell-t5 (p0, spark8-f, submitted this sweep) and unstack
  q27b-build-1 / t4 / bf16-replace-verify-true.

## FLAGS WITHDRAWN — lane-liveness method corrected

My dir-mtime "quiet" proxy was WRONG (tracked-file edits do not touch
the dir mtime). Newest-dirty-file ages: **toksidecar 3 min, k3finish
0 min** — both are ACTIVELY editing right now. Sweeps 3-5 "quiet lane"
flags for toksidecar/k3finish are WITHDRAWN; both are alive with
long-uncommitted work (toksidecar dirty=14, k3finish dirty=4 —
k3finish is evidently staging its 16-rank launch for the next
exclusive window). Remaining dormant lane: **drywave1** only
(detached HEAD, unresolved UU PACKAGE_MANIFEST.json + UU
SHA256SUMS, quiet ~12h) — coordinator cleanup call.

## Healthy / verified

- Dispatcher ALIVE; queue grew correctly: r3flash-glm52-exact-cell-t5
  (p0, spark8-f, "durable ROOT/RES under lane home, commit 31d5302")
  behind the wave3 barrier; all holds/blocks correct; notes skipped.
- bf16 pass2 on pace (rank 2 -> spark2 push window); ETA ~23:45Z.
- Nodes 16/16 reporting, error_streak=0. Lanes alive: glm5dsa
  (committed), r3flash (queued t5), qwen27b-serve (build queued),
  cell-runner (t4 queued), p1d2, debts.

## Actions

Read-only + this report. Sweep 6 = TTL-reaper verdict.
