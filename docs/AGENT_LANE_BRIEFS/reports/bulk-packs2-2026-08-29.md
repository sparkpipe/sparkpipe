# bulk-packs2 lane report — 2026-08-29 (attempt 2)

Lane: ALL new warm sources → stagepacks across the 16 sparks. Branch
`lane/bulk-packs2`, worktree `/tmp/lane-bulkpacks2`, base = main `b415bba`
("log: three accelerators executed; housecleaning core complete"; main had
moved past 58eb672 between attempt-1's death and this spawn — fresher base
taken deliberately).

**Attempt-1 state check (mission requirement):** no `lane/bulk-packs` branch
exists locally or on origin (`git branch -a | grep -i bulk` → empty), no
`/tmp/lane-bulkpacks2` worktree, no `reports/bulk-packs*.md` report. The
predecessor died before committing anything. This lane starts from zero.

## Scoreboard tie-in

The bake-off (next campaign) is blocked on drafter packs; P1 exists so the
bake-off never waits on data. P2 refreshes the 27B target to the official
Apache FP8 source. P3 populates the quality-study arms (full-res spine vs
quant spine vs fully-quant).

## Capability survey (what the EXISTING family packers can actually take)

Method: read every family packer + its module's wire-format header and
serving adapter; verify against each warm source's `config.json` and
safetensors header. Ground rule (operator): existing packers only —
repackage, never quantize; missing branches are RECORDED, not improvised.

### P1 speculators (11 admitted sources, /mnt/model-warm/SPECULATOR-LICENSE-ADMISSION-20260828.json)

| # | source (destination) | bytes (admission) | arch per config.json | packable with EXISTING packer? |
|---|---|---|---|---|
| 1 | qwen3.8-27b-dflash2-incoai | 3,849,113,004 | DFlash2DraftModel, geometry = module-pinned DFlash2 (hidden 5120, 5 layers, 32Q/8KV×128, ffn 17408, block 8, taps {5,19,33,47,61}, selector 256/16, conv 2/16, window 2048, mask 248070, BF16, 81 tensors) | **YES** — `tools/qwen36_dspark_stagepack.py` (Q6SP v3 drafter packer); tensor names verified against the safetensors header (fc.weight / candidate_selector.{predecessor,successor}_codebook / layers.N.{self_attn,mlp,attention_conv,mlp_conv} / norm / hidden_norm) |
| 2 | qwen3.8-27b-dspark-radixark | 3,714,763,499 | DSparkDraftModel — block 7, FULL attention (no sliding), confidence_head.proj.{weight,bias} + markov_head.markov_w1/w2, yarn rope, 62 tensors, NO candidate_selector codebooks | **NO — GAP** |
| 3 | qwen3.8-max-dspark-radixark | 6,596,323,084 | drafter for qwen3.8-max | **NO — GAP** |
| 4 | qwen3.8-max-dflash-modal | 5,436,060,948 | drafter for qwen3.8-max | **NO — GAP** |
| 5 | kimi-k3-dspark-redhatai | 9,489,830,747 | drafter for k3 | **NO — GAP** |
| 6 | kimi-k3-dspark-inferact | 7,124,645,601 | drafter for k3 | **NO — GAP** |
| 7 | kimi-k3-dspark-radixark | 4,498,618,073 | drafter for k3 | **NO — GAP** |
| 8 | kimi-k3-dflash2-lightseek | 8,358,868,742 | drafter for k3 | **NO — GAP** |
| 9 | kimi-k3-dflash-modal | 5,196,963,769 | drafter for k3 | **NO — GAP** |
| 10 | deepseek-v4-flash-dflash-redhatai | 3,607,606,957 | standalone dflash drafter for dsv4-flash | **NO — GAP** |
| 11 | glm-5.3-flash-dflash2 (existing warm copy; upstream incoai repo REJECTED CC BY-NC-ND, copy archived before the rejection) | see SHA256SUMS beside source | dflash2 drafter for glm5_next | **NO — GAP** |

Gap evidence (commands + file:line in the tier-1 section below):

- **qwen38_27b dspark (row 2):** the only drafter packer in the family,
  `tools/qwen36_dspark_stagepack.py`, pins the DFlash2 geometry and a fixed
  81-tensor inventory (`build_inventory()`; constants at top of file). The
  radixark source has 62 tensors with confidence/markov heads the inventory
  does not carry and block_size 7 vs the pinned 8. The module side REJECTS
  it too: `modules/qwen38_27b_resident_decode_stage/source/
  spark_qwen38_27b_dspark_format.h` — "DFlash2 vs DSpark geometry delta"
  documents the dropped DSpark shape, and `spark_qwen38_27b_serving_adapter.c`
  (~line 158) says the superseded DSpark path "fails loudly at load time
  (the module geometry check rejects 40-head/FFN-10240 weights)". Packing it
  today would produce an artifact the runtime refuses. **Kernel-lane
  dependency: a DSpark-geometry branch in packer AND module.**
- **qwen38_max (rows 3-4):** `modules/qwen38_max_resident_decode_stage` has
  NO drafter-pack format (only in-checkpoint MTP: firmware.h "MTP head, one
  draft layer, pinned from the checkpoint safetensors index"; adapter
  `.max_speculative_token_count = 0u`). No packer consumes an external
  drafter checkpoint. **Kernel-lane dependency: drafter pack format + module
  load path for qwen38_max.**
- **k3 (rows 5-9):** `tools/k3_pack.py` has zero drafter/speculator support
  (grep dspark|dflash|drafter|speculator → no matches);
  `modules/k3_resident_decode_stage/source/spark_k3_serving_adapter.c:481`
  `.max_speculative_token_count = 0u`. No draft path in the module.
  **Kernel-lane dependency: k3 drafter pack format + module path.**
- **dsv4-flash (row 10):** the dsv4 format carries DSpark draft layers FROM
  THE TARGET CHECKPOINT's own mtp.0..2 namespace
  (`spark_dsv4_stagepack_format.h` "Three DSpark draft layers (checkpoint
  namespace mtp.0..2)"; dsv4_pro packs use targets [58,59,60]). A STANDALONE
  dflash drafter checkpoint has no pack slot; Flash GA packs set
  mtp_layer_count 0. **Kernel-lane dependency: standalone-drafter slot for
  dsv4 (or an in-pack adoption decision).**
- **glm5_next (row 11):** `modules/glm5_next_resident_decode_stage/source/
  spark_glm5_next_serving_adapter.c:152` `.max_speculative_token_count =
  0u`; no drafter-pack format in the family (`glm52_dspark_draft_backend`
  is the GLM-5.2 donor family's backend and loads manifest+config+
  safetensors directly — see `spark_glm52_dspark_draft_backend.h:39-41` —
  not a glm5_next path, and GLM 5.2 is not a serving target).
  **Kernel-lane dependency: glm5_next drafter path.**

### P2 official FP8 (Qwen/Qwen3.8-27B-FP8, Apache-2.0, rev 017b9c7a)

BUILDABLE: `tools/qwen38_27b_stagepack.py` is dtype-driven
(`check_shape`: "read the tensor's actual dtype and select FP8 only when it
is F8_E4M3", WEIGHT_FP8_E4M3_F32B128 = 5, per-128×128-block F32 scale planes
KEPT AS SHIPPED). Source config text_config matches every pinned expectation
(hidden 5120, 64 layers, 24Q/4KV×256 + attn_output_gate, GDN 16k/48v×128
conv 4, interval 4, vocab 248320, mtp 1). Sharded layout
(layers-0..63 + outside + mtp + model.safetensors.index.json) — the packer
is index-driven (INDEX_NAME). See tier 2 for the build.

### P3 NVFP4 (27B's four first)

NO family packer has an NVFP4 natural-format branch that the serving side
can consume:

- qwen38_27b wire format weight formats (firmware.h 78-89): BF16, F32, U32,
  MXFP4_E2M1, BF16_RANS, FP8_E4M3_F32B128, FP8_E4M3_E8M0B128 — **no NVFP4**.
- qwen38(max), qwen4_flash, k3, glm5_next packers: no nvfp4 handling at all
  (grep nvfp4|e2m1 → no matches in those four packers).
- dsv4 packer: CODEC_IDS carries `"nvfp4_e2m1": 6` as an ID only
  (tools/dsv4_stagepack.py:113, contract-selected) but the dsv4 module has
  ZERO nvfp4 references (grep → empty): no kernel decodes codec 6. Packing
  NVFP4 sources for dsv4 would produce undecodable packs.
- glm52_stagepack.py's nvfp4 codec is an ENCODER (`encode_e2m1`,
  line 358) for the GLM-5.2 B12x resident MoE path — it quantizes from
  values at pack time; using it on NVFP4 checkpoints would be both a
  self-quantization (policy-forbidden) and a GLM-5.2-only path.

**Kernel-lane dependency (fleet-wide): NVFP4 first-class kernels
(coordinator log 2026-08-29 ~21:4x: "currently only probed; MXFP4 in-tree").
Until a family's module gains an NVFP4 natural-format decode, its NVFP4
sources stay UNPACKED.** Per-source record in tier 3.

(sections: tier-1 / tier-2 / tier-3 appended below as batches land)

---

## Tier 1 — P1 speculators (2026-08-29)

### Source integrity gate (ALL 12 admission entries, the packable one and the gaps)

Command: per-source reconciliation of `DOWNLOAD-RECEIPT.json` vs
`SPECULATOR-LICENSE-ADMISSION-20260828.json`, then sha256 of EVERY receipt
file on disk (payloads re-hashed). Ran on spark5 (`/mnt/model-warm/packbuild/bulkpacks2-verify-sweep.log`).

Result: **12/12 OK** — receipt totals equal admission bytes exactly
(e.g. incoai 3,849,113,004 == 3,849,113,004; official FP8 30,890,049,597 ==
30,890,049,597) and every file sha256 matches. A raw `du`-style directory
walk reports every dir slightly LARGER than admission bytes (+1.5-20 KB):
that is the post-download `PUBLISHED` markers / helper files the download
lane adds after admission — the receipt-total comparison is the honest one.
All 8 NVFP4 defaults: dir sizes match `expected_bytes` modulo the same
markers, `PUBLISHED` marker present on all; non-default artifacts present.

### BUILT + PLACED: qwen3.8-27b-dflash2-incoai (the bake-off challenger)

- Build (spark5, from committed branch `lane/bulk-packs2` @ 52f1c2d3,
  checkout `~/lane-bulkpacks2`):
  `python3 tools/qwen36_dspark_stagepack.py --checkpoint
  /mnt/model-warm/qwen3.8-27b-dflash2-incoai --output
  /mnt/model-warm/packbuild/qwen3.8-27b-dflash2-incoai/qwen38-dflash2-drafter-incoai.qwen38_27bsp`
  → `wrote ... tensors=81 file_gib=3.58 round_trip=ok`
  (verify() header/geometry PASS + verify_payload() byte-for-byte
  pack-vs-safetensors round-trip PASS — this is the --verify-output PASS).
- Pack sha256 `7bd3a8404bd2f3e484e64489a03eef9175919a78564ecf0f6c5c4000f4857f44`
  (independent `sha256sum` == hash-while-write `output_sha256`), 3,848,813,824
  bytes — byte-count-identical to the incumbent z-lab drafter pack (same wire
  format, new weights), as expected from identical geometry.
- Placed: spark2, spark9, sparka (the fleet `qwen38.fp8.tp1_27b` trio
  convention) at `~/sparkdata/qwen38-dflash2-drafter-incoai.qwen38_27bsp`.
  Far-side `sha256sum` on all three == 7bd3a840… (size + 16-hex prefix
  re-verified per node).
- Receipts beside every pack: the packer's
  `*.receipt.json` + `*.PLACEMENT-RECEIPT.json` (repo/revision/license/
  admission reference/integrity result/serving env/commit).
- The INCUMBENT (`~/sparkdata/qwen38-dflash2-drafter.qwen36sp`, z-lab) is
  untouched — the bake-off flips `SPARK_QWEN38_27B_DSPARK_PACK_PATH`
  between the two; no daemon was touched by this lane.

### RECORDED GAPS: the other 10 P1 sources (SKIP, kernel-lane dependency)

See the survey table above for per-source evidence. Summary of the exact
missing pieces, per family:

1. **qwen38_27b + DSpark-geometry drafters** (qwen3.8-27b-dspark-radixark,
   3,714,763,499 B, 62 tensors): packer `tools/qwen36_dspark_stagepack.py`
   pins DFlash2 geometry + 81-tensor inventory; source carries
   confidence_head.* + markov_head.* (62 tensors), block 7, full attention.
   Module side rejects non-DFlash2 geometry at load
   (spark_qwen38_27b_serving_adapter.c ~L158). NEEDS: DSpark branch in
   packer + module drafter path (kernel lane).
2. **qwen38_max** (qwen3.8-max-dspark-radixark 6,596,323,084 B;
   qwen3.8-max-dflash-modal 5,436,060,948 B): family has NO drafter-pack
   format; module spec = in-checkpoint MTP only
   (spark_qwen38_max_resident_decode_stage_firmware.h L155-158;
   adapter max_speculative_token_count=0u). NEEDS: drafter wire format +
   module load path.
3. **k3** (kimi-k3-dspark-{redhatai 9,489,830,747 / inferact 7,124,645,601
   / radixark 4,498,618,073}, kimi-k3-dflash2-lightseek 8,358,868,742,
   kimi-k3-dflash-modal 5,196,963,769 — 35.7 GB across five sources):
   k3_pack.py has zero drafter support; k3 adapter
   max_speculative_token_count=0u (spark_k3_serving_adapter.c L481). NEEDS:
   k3 drafter format + module path. All five verified on warm, ready to
   pack the day the format lands.
4. **dsv4** (deepseek-v4-flash-dflash-redhatai 3,607,606,957 B): the dsv4
   wire format carries only the TARGET checkpoint's own mtp.0..2 draft
   layers (spark_dsv4_stagepack_format.h L33-36); standalone dflash drafter
   has no slot; Flash GA packs set mtp_layer_count 0. NEEDS: standalone
   drafter slot or in-pack adoption.
5. **glm5_next** (glm-5.3-flash-dflash2, archived warm copy with
   ARCHIVE-RECEIPT.json + SHA256SUMS; upstream incoai repo itself REJECTED
   CC BY-NC-ND — treat as archived reference only): glm5_next adapter
   max_speculative_token_count=0u (spark_glm5_next_serving_adapter.c
   L152); no drafter format. NOTE the license: even after the kernel lane
   lands, serving this copy needs a policy decision (admission REJECTED the
   upstream; the warm copy predates rejection). NEEDS: kernel path FIRST,
   license ruling SECOND.

**INTEGRATION REQUEST (kernel lane):** drafter-pack formats + module spec
paths for qwen38_max, k3, dsv4-standalone, glm5_next, and a DSpark-geometry
branch for qwen38_27b, in roughly that bake-off value order. This lane
re-packs each source the day its format exists (sources verified + pinned).

---

## Tier 2 — P2 official qwen3.8-27b-fp8 (2026-08-29)

- Source: Qwen/Qwen3.8-27B-FP8 rev 017b9c7a, Apache-2.0, integrity OK
  (tier-1 gate).
- Build (spark5, branch commit 52f1c2d3):
  `python3 tools/qwen38_27b_stagepack.py --checkpoint
  /mnt/model-warm/qwen3.8-27b-fp8 --output
  .../qwen38_27b-fp8.official.tp1.qwen38_27bsp --first-layer 0 --layer-count 64`
  → `slice=0+64 tensors=866 file_gib=27.89` then the explicit gate
  `--verify` → `verify ok: slice=0+64 tensors=866 bytes=29948282112` **PASS**.
- Wire truth (dumped from the pack's entry headers): 407 tensors
  FP8_E4M3_F32B128 each with its F32 128×128-block scale plane (kept as
  shipped — repackage, no requant), 363 BF16, 96 F32 (widened A_log/dt_bias
  class). Receipt quirk found: the receipt's static
  `weight_formats.projections: "bf16"` label does not track the
  dtype-driven FP8 selection (tools/qwen38_27b_stagepack.py L741 — convert()
  never updates it). The PLACEMENT-RECEIPT carries the real histogram;
  packer label fix is a one-liner for the 27B family lane (not mine to
  bolt on mid-build).
- Pack sha256 `7f536a1c63c2d87714d426096efc08de6eac78b882c60e53bdae9ba124f08c38`
  (independent sha256sum == output_sha256), 29,948,282,112 bytes.
- Placed spark2/spark9/sparka at
  `~/sparkdata/qwen38.fp8.official.tp1/packs/qwen38_27b-fp8.official.tp1.qwen38_27bsp`
  (+ `.receipt.json` + `PLACEMENT-RECEIPT.json` beside). Far-side shas
  identical on all three. The staging-manifest sha-pinned incumbent trio
  (`qwen38.fp8.tp1/packs/qwen38-fp8.tp1.qwen36sp`) is untouched.
- Swap-over is a config flip (`stage_pack_path` → the new pack) on the dev
  instance when the 27B lane is ready — deployment/daemon work is
  deliberately OUT of this lane's hands (glm5_next fleet live).

---

## Tier 3 — P3 NVFP4 variants (2026-08-29) — ALL RECORDED GAPS

Warm side is DONE: all 8 defaults + 5 non-default artifacts verified on
warm (sizes vs `NVFP4-MODEL-DEFAULTS-20260828.json` modulo the PUBLISHED
markers, `PUBLISHED` present). The 27B quality-study arms, verified and
pinned, awaiting a packer:

| warm path | bytes (dir) | note |
|---|---|---|
| qwen3.8-27b-nvfp4a16-bf16-spine (default) | 30,993,760,181 | BF16 spine/attn/lm_head/MTP, NVFP4 dense MLP |
| qwen3.8-27b-nvfp4-radixark | 21,945,300,141 | NVFP4 lm_head |
| qwen3.8-27b-nvfp4-radixark-bf16-lmhead | 23,772,926,263 | BF16 lm_head, FP8 spine |
| qwen3.8-27b-nvfp4-redhatai | 23,444,513,356 | FP8 lm_head + spine |
| qwen3.8-27b-nvfp4-inferact | 26,404,423,373 | BF16 lm_head + linear-attn; NVFP4 full-attn |

Blocker (exact): the qwen38_27b wire format has NO NVFP4 weight format —
`spark_qwen38_27b_resident_decode_stage_firmware.h` L78-89 defines
BF16/F32/U32/MXFP4_E2M1/BF16_RANS/FP8_F32B128/FP8_E8M0B128 only — and the
packer mirrors that. Same story per family: qwen38(max), qwen4_flash, k3,
glm5_next packers have zero nvfp4 handling; dsv4_stagepack.py carries codec
ID `"nvfp4_e2m1": 6` (L113, contract-selected) but the dsv4 module contains
zero nvfp4 references — no kernel decodes codec 6, so a packed file would
be undecodable; glm52_stagepack.py's nvfp4 codec is a pack-time ENCODER
(quantizes from values, `encode_e2m1` L358) — policy-forbidden as a
quantization path and GLM-5.2-donor-only anyway.

**INTEGRATION REQUEST (kernel lane, the ONE blocker for the whole NVFP4
portfolio + the 3-way quality study):** NVFP4 natural-format decode in the
module kernels (coordinator log 2026-08-29 ~21:4x: "NVFP4 kernels... currently
only probed; MXFP4 in-tree"), then weight-format IDs per family wire format,
then packer branches. E2M1 dequant machinery overlaps MXFP4's per the
coordinator's note. This lane re-runs immediately after: sources are
verified, pinned, and sitting on warm.

---

## Lane incidents / deviations

1. **spark4 ceph client wedged (report per cluster rules):** warm reads on
   spark4 measured **1.7 MB/s** uncached (`dd 384MB ← qwen3.8-27b-fp8/
   layers-0.safetensors: 231s`) vs **1.0 GB/s** on spark5 (same file class,
   same window). The documented route-around applied: my build + sweep were
   TERM'd (own pids only) and re-run on spark5. A build started on spark4
   before the diagnosis left a partial `.tmp` — removed. spark4 needs a
   look (client remount) by whoever owns infra; its glm5_next residentd
   (rank 4) was NOT touched.
2. **Self-match lesson re-learned:** the TERM sweep matched my own ssh
   wrapper (the search pattern text appears in its own cmdline) and clipped
   my session — exactly the documented fuzzy-match hazard; captured-pids
   TERM for the build worked as intended. Doing pid capture + exact match
   next time.
3. **Full-fleet reservation by lane-glm5rt** (all 16 nodes, acquired
   22:28Z, TTL 480min → expired ~06:28Z; sweep cron deleted so nothing
   released it): my work is CPU-only pack building + placement ssh — no GPU,
   no daemon contact — so it proceeded without touching the reservation
   state. Flagging the stale full-fleet hold for the coordinator.

## staging_manifest state

`staging_manifest.py --self-test` → PASS (76 checks, 8 models). Fleet walk
→ exactly ONE gap, the PRE-EXISTING documented `dsv4_flash.fp8.tp16.b1@sparkf`
generation drift (the table note itself flags it: "real gap"). **No new
gaps introduced**; this lane's packs live at new paths and are NOT yet in
the fleet table. Suggested table additions for the staging lane /
coordinator (all verified above):

- `qwen38.dflash2_drafter.incoai` — spark2/spark9/sparka,
  `~/sparkdata/qwen38-dflash2-drafter-incoai.qwen38_27bsp`,
  3,848,813,824 B, sha 7bd3a840…
- `qwen38.fp8.official.tp1` — spark2/spark9/sparka,
  `~/sparkdata/qwen38.fp8.official.tp1/packs/qwen38_27b-fp8.official.tp1.qwen38_27bsp`,
  29,948,282,112 B, sha 7f536a1c…

## Next experiments

1. Bake-off on spark2 (dev instance): z-lab vs incoai drafter packs through
   the DFlash2 runbook env; acceptance 5-7/round at k=8 gate + COMPSEC-17
   per the quality-gate rule. (27B lane / coordinator.)
2. FP8 official swap-over on spark2 (`stage_pack_path` flip) + ds4_eval
   baseline before/after. (27B lane.)
3. The moment the kernel lane lands an NVFP4 decode for any family, this
   lane packs that family's NVFP4 sources (all pinned + verified).
4. k3 drafter format is the largest unblock-by-value (35.7 GB of admitted,
   verified speculator sources waiting).
