# Wave T1-G53 — glm53flash (glm5_next) T1 accuracy gate + serving-gated
# instrument (2026-09-15)

Agent: T1-G53 (mgr2 dispatch; operator directive: ACCURATE INFERENCE FIRST,
then hillclimb — no cheating, lying or fraud; actual real results). Branch
`lane/t1-g53` off origin/main e60479b (merge of lane/wave-t1-offline cc5d5f8 =
instrument + fixtures). Clone /Users/mac/t1g53. Identity `sparkpipe` verified;
all git/gh through tools/sparkpipe_github_pat.sh. Mesh lease HELD-FIRST +
ACTIVE in sparkpipe-coord/MESH_LEASE.

## VERDICT (headline)

T1 = **FAIL as committed**, with high-confidence defect localization on the
FIXTURE side and the full comparison instrument built + staged for the
streams/routes half, which is blocked by a fleet incident (below).

## Deliverable 1 — offline gates (all MEASURED, exit codes receipted)

- verify-manifest: PASS. Both committed glm5_next fixtures match
  MANIFEST.json sha256/bytes.
- corrupt-fixture negative control: PASS. One flipped byte in
  pos0000_layer0003_streams → compare exits 1 naming the array with
  FIRST DIVERGENCE.
- Tool defaults recorded (ids/routing exact; streams/scores/weights banded
  rel 0.02 / abs 1e-3). Operator tolerance ruling pending; nothing loosened.

## Deliverable 2 — the serving-gated half did not exist; it does now

- The committed instrument had no driver-side candidate producer. Built:
  - env-gated G5N-T1 dump in modules/glm5_next_resident_decode_stage
    (streams = post-MLP residual streams for every row at every layer,
    route ids/weights per packed row at every routed layer, head top-1
    token + score bits per row; log-line contract documented in
    tools/t1_g53_log_assembly.py which assembles a candidate T1R1).
  - Graph path disabled under SPARK_GLM5_NEXT_T1 (decode steps run the chain
    state machine so dumps fire; prefill unaffected).
  - GPU component validator: PASS (0 failures) on the fp8 archive.
- Assembly chain exercised end to end: module archive → variants → validate →
  publish (module library) → sparkpipe_model_compile → stages/stage_000/
  model_driver.so (sha 5c71c82c…, artifact 9790647c…).

## Deliverable 3 — serving-arm decode receipts (MEASURED)

Production fp8.tp16 stack (hub core deployed 00:09 node time, weightsd live,
adapter tp8.expert_fp8.v1, revision 84c6a6aa…):

- capital_of_france [785,6722,315,9621,374] → engine [12089,13,758,8584] =
  " Paris. In French" (fluent, semantically correct) vs committed fixture
  [3837,271,271,12] = mojibake. **Token-id gate FAIL at the first generated
  token.** The wave-t1-offline report's own anchors (position-0 top-1
  [gMASK]/264) are consistent with a template-seeking raw continuation, so
  the fixture's garbage tokens alone do not prove which side is wrong — but
  the divergence is real, receipted both ways, and the bf16-arm adjudication
  directed by mgr2 is the decider (staged, see blocker).
- count_up: blocked by the production engine defect below.

## Findings (each independently actionable)

1. **Engine wedge (coredev, routed via mgr2).** The production fp8.tp16 stack
   serves exactly ONE request per residentd boot, then every later request
   hangs: rank-0 residentd main thread spins at 100% CPU with zero log errors,
   the api eventually loops engine_connect_failed status=4 and dies.
   Reproduced twice end to end. A serving stack that wedges after one request
   cannot reach ≥14 tok/s.
2. **Fleet incident (escalated, open).** At 01:22:52-54 node time (same
   second on every node checked) the kernel began logging NVRM NV_ERR_NO_MEMORY
   fleet-wide. spark1's CUDA device became unusable to ANY process (operator
   rebooted it ~02:48, verified healed); spark0's production root then went
   into a KV-alloc crash-loop. This predates all lane launches (first lane
   process ~02:00). My 16 lane residentds were stopped to release ~12 GB/node
   during the incident; sparkf was returned to production-only.
3. **main does not compile three targets** (all fixed on lane/t1-g53):
   the serving adapter references tap defines absent from the firmware header;
   node/model_residentd.c has an unused-parameter -Werror break; the adapter's
   serving stage count / tp degree defines are unguarded so the documented
   deployment-override pattern cannot compile.
4. **Hub core is ahead of main.** The deployed adapter descriptor hardcodes
   serving stage_count=16 (a TP8 lane mesh cannot load under it) and its
   adapter compiles against header content main does not have. Lane-ABI
   mixing (hub adapter + main module) fails at create with INVALID_ARGUMENT.
   The lane therefore builds residentd + adapter + transport + module from one
   branch. TP8 lane attempt parked on this; TP16 lane used instead.
5. **Deployment generator emits execution_row_capacity 1024** but the module
   enforces the weightd mesh law SPARK_WEIGHTD_MESH_MAX_BATCH_ROWS=128 —
   every generated glm5_next deployment json fails deployment_validation
   status=11 on the current module. Configs were patched to 128 in the lane;
   the generator should emit 128 (or the law should be re-derived).

## Blocker at close

Lane mesh 16/16 residentd ready with the instrumented module; api connected
(session accepted). Decode fails inside the first TP collective
(tp_device_collective.c:379 status=17 → chainfail stage 0 layer 0) under the
incident-degraded weightd mesh while production roots crash-loop on the nodes.
The moment the fleet is quiet/healed: relaunch (scripts staged at
/tmp/t1g53_launch16.sh on every node, lane roots at
~/sparkdata/glm53flash.fp8.tp16.t1g53, purge after T1), decode both prompts,
assemble the candidate T1R1 from rank-0's G5N-T1 lines with
tools/t1_g53_log_assembly.py, run tools/t1_reference_compare.py, then run the
directed bf16-arm adjudication (bf16 packs placed on all 16 nodes; same lane
pattern; TP16 requires a lane process on sparkf — disclosed in the lease).

## Receipts

runs/t1g53/T1R1_RECEIPT.json (fixture SHAs, per-prompt streams, verdicts,
weightsd socket, engine build shas, blockers), prod_api_wedge.log,
prod_residentd_wedge_tail.log, lane_build_sha.txt. Grades: every claim above
is MEASURED with the named receipt except the bf16 adjudication outcome
(pending run) and T2 (NOT YET MEASURED — the honest serving number cannot be
taken while the production engine wedges and the fleet incident is open).

## ADDENDUM — offline adjudication (same day, operator-authorized)

Directive: prove or clear the reference decoder offline via the bf16
checkpoint variant before any engine rerun.

- **Reference verdict: CLEAN.** The fp8 dequant was hand cross-checked
  (independent bit-level e4m3 decode vs `fp8_block_to_bf16` on
  `layers.3.mlp.experts.0.gate_proj.weight` — match within bf16 rounding;
  scale layout [N/128, K/128] confirmed against the checkpoint; weight
  statistics sane). Determinism holds. The o_norm-class defect hunt finds
  no sibling in the reference.
- **bf16-official through the reference** (flattened text_config, 120
  shards, spark5, ~55 min): capital → **[12089, 154822, 3308, 315]
  = " Paris [gMASK] course of"** — first token equals the engine's.
  count_up → [10701, 2251, 28872, 25878] (" skip ency shoe - between"):
  the raw continuation degrades past token 1 on BOTH checkpoints —
  token-exactness beyond token 1 is not a stable gate for raw prompts on
  this model (matches the wave's cross-node tie-flip finding).
- **The material defect is ASSET IDENTITY.** Router-gate fingerprint
  (u16 value multiset, layers.3.mlp.gate.weight, native BF16 in both
  checkpoints): pack ∩ bf16-official = **0.984**, pack ∩ fp8-release =
  0.925, fp8-release ∩ bf16-official = 0.925. The placed
  glm53flash.fp8.tp16 packs carry **bf16-official-class weights** (the
  two checkpoints are different snapshots — their routers differ), while
  the committed fixtures were generated from the fp8-release. Fixture
  checkpoint ≠ served checkpoint: the committed fixtures cannot pass
  against the packs at any tolerance, and the packs' declared revision
  (84c6a6aa, the fp8 release) misrepresents their content. This is the
  concrete "packer==pack proven, packer≠checkpoint" gap.
- Candidate fixtures for the packs as-deployed (bf16-official snapshot):
  runs/t1g53/bf16off_candidate/ (both prompts + MANIFEST + run log).
  Operator ruling required: canonical snapshot (regenerate fixtures from
  bf16-official, or rebuild packs from the fp8-release); then the
  serving-gated streams/routes compare reruns within one snapshot.
