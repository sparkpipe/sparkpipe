# glm53full lane report — 2026-08-28/29 (three-resolution GLM 5.3-full)

Lane: glm53full-3res (worktree /tmp/lane-g53full, branch
`lane/glm53full-3res`, PR #740). Build node: spark5 (spark4's ceph client
still wedged — dd of 160MB timed out at 25s vs spark5 280 MB/s; all heavy
IO routed to spark5 per the bulk-packs2 finding). Stale lane-glm5rt
reservation on spark5 (already noted for cleanup in the coordinator log)
observed; my work is CPU/IO-only, no GPU taken yet, no daemon touched.

Serves the scoreboard goal: GLM 5.3-full is the same-checkpoint
three-expert-precision quality/perf study model (NVFP4/FP8/BF16) on the
glm52 module; 5.2 deprecation rides this lane.

## M-A: GLM 5.2 fully deprecated — DONE (commit 2f5cb14)

- docs/MODEL_SUPPORT.md: GLM row now "5.3 Flash (active), 5.3 Full
  (onboarding), 5.2 (fully deprecated: kernel-donor role superseded by
  5.3 Full — same glm52 module, new weights)".
- docs/GOALS.md: new section "GLM 5.2 FULLY DEPRECATED (operator
  directive, 2026-08-29)" — 5.2 packs stay on disk
  DEPRECATED-NOT-DELETED pending 5.3-full validation; no new work
  against 5.2 sources.
- tools/staging_manifest.py: `glm52.tp8.fp8` entry flagged
  DEPRECATED-NOT-DELETED; `--self-test` PASS (76 checks, 8 models).

## M-B: contract freeze — DONE (commit 98177bd)

`tools/glm53full_contract_freeze.py` (glm53 pattern; `--spark` parameterized)
→ `model_contracts/glm53_full_authoritative.json`:

- Source: RadixArk/GLM-5.3-NVFP4, revision 363e8f086905afd83db356a620f9aa401c23800a,
  license_class zai-commercial-use-granted, DOWNLOAD-RECEIPT
  (ds4-hf-warm-download-receipt-v1) at /mnt/model-warm/glm-5.3-nvfp4-radixark,
  464,874,334,135 bytes, 58 files.
- Verification (command + output in the freeze run): small files 11/11
  sha256 match receipt; strided shard re-hash on spark5
  (shards 1,9,17,25,33,41,47) = 7 OK / 0 FAILED.
- Census: 47 shards, 232,385 tensors; dense layers [0,1,2]; routed 3-77;
  NVFP4 on routed-expert linears ONLY (modelopt, group 16, kv FP8),
  full-resolution BF16 spine everywhere else.
- INDEXER_TYPES RESOLVED (the one code check the coordinator flagged):
  the 5.3 NVFP4 config OMITS the key but the checkpoint does NOT drop
  the split — full indexer sets (wq_b, wk, weights_proj, k_norm.*) on
  layers [0,1,2,6,10,...,74] (21 in 0-77) + MTP layer 78. Same
  share-group-4 pattern as 5.2; glm52 `has_full_indexer` unchanged.
- up/gate per-expert global scale equality (load-bearing for the pack
  layout): weight_scale_2(up) == weight_scale_2(gate) on 20/20 sampled
  (layers 3,20,40,60,77 x experts 0,7,128,255); down differs (own
  global) — matches the wire format's one-global-per-expert fused
  EXPERT_UP_GATE entry exactly.

## M-C1a: NVFP4 VERBATIM packer branch — DONE (commit c2d5402)

`tools/glm52_resident_stagepack.py --expert-codec nvfp4` (default fp8,
backward compatible; fp8 test suite PASS unchanged):

- Expert bytes copied VERBATIM: U8 packed-e2m1 payloads (nibble-aligned
  TP slices; 2048/16-row and 128-col shard boundaries are nibble safe),
  F8_E4M3 per-16 block scales, one F32 global per expert. Header expert
  codec 6, scale encoding ue4m3_f32_global — byte counts equal
  SparkWeightCodecScaleBytes exactly (globals plane 256x4B replicated on
  every rank; block scales sharded). NO quantization anywhere — the
  source is already-NVFP4; policy-compliant repackage.
- Per-expert up/gate global-share enforced at pack time (a violating
  source fails the pack, not the serve).
- `tools/glm52_validate_pack.py` reads the expert codec from the pack
  header (fp8/nvfp4) and mirrors the C scale-plane math.
- `tests/test_glm52_pack_nvfp4_passthrough.py`: 4/4 PASS (reader
  verbatim slices + alignment rejection + scalar global + up/gate
  enforcement; plan-vs-validator mirror at tp1/tp16; tp16 partition with
  replicated globals accounted). `tests/test_glm52_pack_fp8_source.py`
  regression: PASS.

TP16 plan numbers (from the plan mirror, tp16): 1344 tensors,
32,902,952,448 payload+scale bytes per rank (~32.9 GB/rank; ceiling 110).
The brief's ~27 G/rank estimate was source/16; the pack adds the e4m3
scale planes (+~4 GB/rank) — still ~3.3x headroom.

## M-C1b: full TP16 NVFP4 packs — DONE (16/16 built, verified, receipts; placement in flight)

- Full build (spark5, spawn-captured PID 1774590, ~20 min wall:
  reads ~465GB warm at ~2GB/s lockstep, writes ~526GB):
  `--layer-range 0-77 --tp-degree 16 --all-ranks --expert-codec nvfp4
  --model-revision 363e8f08...` →
  spark5:~/sparkdata/glm53full.nvfp4.tp16/packs/glm53full.nvfp4.tp16-rank{0..15}.glm52sp
  each 32,903,038,976 bytes, 1344 tensors.
- Validator (tools/glm52_validate_pack.py, tp=16): **16/16 errors: 0**
  (header expert_codec=6, linear=1, kv=1 on every rank).
- Receipts: 16/16 .receipt.json — expert_codec nvfp4, expert_policy
  "VERBATIM passthrough ... never quantizes", model_revision
  363e8f0..., source arch GlmMoeDsaForCausalLM, config sha, per-tensor
  sources.
- SHA256SUMS (all 16 packs) pinned at
  spark5:~/sparkdata/glm53full.nvfp4.tp16/packs/SHA256SUMS (e.g. rank0 =
  36fef98008986799217ae26dc217cebb671287b79cf7d267a03451535593b1a3).
- Byte-verbatim spot checks vs source (pilot, layer 3, TP2), command +
  output:
  - rank0 up rows [0:1024) verbatim: True
  - rank0 gate rows [0:1024) at intra-expert offset: True
  - rank1 up rows [1024:2048) verbatim: True (TP row partition exact)
  - global verbatim and replicated on both ranks: True
  - up block scales verbatim: True; down payload col-shard verbatim:
    True; down block scales col-shard verbatim: True
- Placement (tools/glm53full_place_packs.sh --from spark5, committed):
  rank r -> spark r (hex a-f), disk-only, resumable, in flight at
  filing; first run had a literal-$HOME path bug (fixed in 26e2826;
  junk dir cleaned by the fixed script). NO daemon contact, NO launches
  — waves are a later coordinated event.

## M-D: module republish against the 5.3 pack — PASS

`make -C modules/glm52_resident_decode_stage publish EXPERT_CODEC=nvfp4
MODEL_REVISION=363e8f08... CONTRACT_SHA256=6a367400...2429
STAGE_PACK_PATH=<rank0 pack>` on spark5 (nvcc sm_121a). The GPU
validator ran the REAL 5.3-full NVFP4 pack against the CPU oracle:

- tier=dense PASS (layer_forward cosine 0.9999818)
- tier=routed PASS — routed_selection exact sets (8 experts/token),
  routed_expert_forward cosine 0.9999897, routed_determinism
  routes=16 bit_exact=1 (the nvfp4 experts DECODE AND EXECUTE)
- tier=dsa PASS (dsa_selection exact=1, sparse attention cosine
  0.9999940), glm52_validation PASS overall
- published: module=spark.glm52.resident_decode_stage.bf16.expert_nvfp4.
  h6144.l78.e256.k8.v2 target=cuda.sm121.glm52.resident_decode_stage.
  bf16.expert_nvfp4 artifact=118644b2... record=
  build/module_library/active/2abe1256....json (spark5 lane checkout)

This answers the coordinator's central verify demand: the format-block
that stopped the other families does NOT apply to glm52 — nvfp4
(EXPERT_CODEC=nvfp4, codec 6) is a first-class expert codec here with
real kernels, and the 5.3-full pack serves through it. THE glm52 MODULE
IS THE 5.3-FULL MODULE, now proven with weights, not just geometry.

## M-C1 pilot note (superseded by C1b)

- Pilot (layer 3, TP2, spark5): two 4.86GB packs; validator entry-level
  checks ZERO errors (the 77 inventory lines are the expected
  missing-layer output of a 1-layer pack). Byte-verbatim spot checks vs
  source, command + output:
  - rank0 up rows [0:1024) verbatim: True
  - rank0 gate rows [0:1024) at intra-expert offset: True
  - rank1 up rows [1024:2048) verbatim: True (TP row partition exact)
  - global verbatim and replicated: True
  - up block scales verbatim: True; down payload col-shard verbatim:
    True; down block scales col-shard verbatim: True
- Full build: spark5 PID 1774590/1774592 (spawn-captured),
  `--layer-range 0-77 --tp-degree 16 --all-ranks --expert-codec nvfp4
  --model-revision 363e8f08...` →
  /home/spark5/sparkdata/glm53full.nvfp4.tp16/packs/glm53full.nvfp4.tp16-rank{rank}.glm52sp.
  Reads ~465GB from warm (ceph ~2GB/s in lockstep), writes ~526GB.
  On completion: glm52_validate_pack.py on all 16 + receipts, then
  placement per the fleet table (disk-only; glm5_next fleet untouched).

## Staging source status (polled, untouched)

- glm-5.3-bf16-304b8051 (BF16 official): 28GB / ~1.2T — STALLED across
  polls (~40 min apart, zero growth), 6 shard files present.
- glm-5.3-935644c0 = the FP8 OFFICIAL source (config: fp8 e4m3 dynamic,
  weight_block_size [128,128], 141 shards): 635G / 127 shard files, NO
  model.safetensors.index.json yet — also static across polls.
  Both look stalled — sysadmin attention requested; polls are cheap
  du/ls, nothing touched.
- FP8 pack path (C3) = the existing fp8 branch against the 5.3 source
  (blockwise layout identical to 5.2's; contract geometry shared) —
  runs as soon as the source promotes with a DOWNLOAD-RECEIPT.
- BF16 pack path (C2) has a REAL BLOCKER beyond source completion: the
  module static_asserts expert codec != BF16
  (modules/glm52_resident_decode_stage/source/cuda/unity.cu:47) and the
  expert GEMM instantiations start at int6 — a BF16-expert serving arm
  needs an LmBf16Format expert instantiation (shared kernels =
  INTEGRATION REQUEST below). The packer side (bf16 expert passthrough
  entries) is trivial once the module can decode it.

## INTEGRATION REQUEST (coordinator actions)

1. Makefile PYTHON_TESTS += `tests/test_glm52_pack_nvfp4_passthrough.py`
   (one line after tests/test_glm52_pack_fp8_source.py; Makefile is
   outside my write set).
2. (Future, when the BF16 source lands) shared-kernel decision: BF16
   expert GEMM instantiation (unity.cu static_assert + layer.cuh) to
   open the full-resolution quality arm; packer side is ready to add.

## Next experiments

1. Placement completion check (resumable; rank-size + SHA256SUMS
   re-verify per node), then the staging_manifest fleet-table entry for
   glm53full.nvfp4.tp16 (coordinator-owned tool — integration request).
2. M-E serving wave is a COORDINATED LATER EVENT (fleet live; no
   daemons touched this lane). First serve triggers the quality gate:
   COMPSEC-17 then the 92x per resolution.
3. Poll staging for BF16/FP8 completion (currently STALLED — sysadmin);
   C3 (FP8) runs the fp8 branch; C2 (BF16) blocked on the kernel
   instantiation.
4. When the fleet frees: ds4_eval 92x per resolution (fixtures pattern
   exists) — COMPSEC-17 first, per the quality gate.
