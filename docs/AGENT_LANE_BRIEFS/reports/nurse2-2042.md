# Nurse2 sweep 3 — 2026-08-29 20:33–20:43 UTC (lane/nurse2)

Telemetry gate OPEN=CLOSE @20:33/20:43Z: `active_nodes=0
avg_gpu_pct=0.0 input_tok_s=0.0 reachable=16`, zero busy nodes.

## ESCALATE

None yet (one on the clock — see WATCH-LOUD).

## WATCH-LOUD (decision at sweep 4, ~20:45Z)

- **wave3 silent at the API stage**: ranks reached `ready: 16/16` and
  `== api on spark0:8433 ==` at ~20:32Z; since then the log on spark0
  has NOT advanced (re-checked 20:35Z and 20:40Z — last line still
  "api on"), spark0 GPU 0%, and ALL 16 nodes are 0% / idle in
  telemetry with 16/16 leases held (g5dsa-wave3, TTL -> 21:01:32Z).
  Ranks-up-to-zero-compute is ~8-12 min old. If sweep 4 (~20:45Z =
  >12 min past ready) still shows no passes/no tok_s/no GPU: that is
  GHOST WORK under the 10-min rule -> ESCALATE with this log tail:
  `ready: 0/16 x3, ready: 16/16, == api on spark0:8433 ==` (nothing
  after). Possible cause: wave driver waiting on the API that isn't
  serving, or PASSES loop starting without logging.
- **k3finish HARD-FLAG** (charter: quiet >120m): quiet 129m, dirty=2
  (runs/queue.jsonl + runs/reservations.json), last report
  k3-finish-2026-08-30b.md. Mid-work death or abandoned checkout —
  coordinator's call; its launch-ready note task is still parked in
  the queue.

## Healthy / verified

- Dispatcher ALIVE all passes; queue stable: t4 (p1) + q27b-build-1
  (p1) + bf16-replace-verify-true (p2) correctly barriered behind
  wave3; 3 notes skipped. Idle-node alarm N/A (16/16 leased).
- bf16 pass2: rank 1 -> spark1 placed (18 "placed" lines; rank 2 in
  flight) — on the 25min/rank pace, ETA ~23:45Z unchanged.
- Nodes: 16/16 reporting, error_streak=0; no alarms.

## Flags carried

- toksidecar: dirty=14, quiet ~100m — FLAG stands.
- qwen smoke owner-fix: q27b-build-1 (fresh-main rebuild) queued
  behind wave3 — the correct mechanism; watch it get spark5.

## Actions

Read-only + this report.
