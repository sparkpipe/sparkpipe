# Big-Model Driver Static Review — 2026-08-25

Repo: `/Users/mac/sparkpipe` @ unified **c834436** ("milestone: dsv4-flash serving
restored + measured 40.67 tok/s B1…"). STATIC REVIEW ONLY — no build, no hardware,
no ssh. Reviewer: senior CUDA/systems review pass over drivers that cannot be
hardware-tested yet.

Scope covered:
1. DeepSeek-V4 Pro: `modules/dsv4_resident_decode_stage/` (+ `Makefile.pro`,
   `model-families/dsv4/include/sparkpipe/spark_dsv4_pro_model_aliases.h`),
   `model_contracts/dsv4_pro*.json`, `tools/dsv4_pro_stagepack.py`.
2. Qwen 3.8 Max: `inference/llms/qwen_3_8/`, `modules/qwen38_resident_decode_stage/`,
   `model-families/qwen38/`.
3. Kimi K3: `inference/llms/kimi_k3/`, `modules/k3_resident_decode_stage/`.

Severities: **P0** correctness (wrong output / build broken), **P1** reliability
(guaranteed runtime failure, leak, unchecked resource), **P2** quality
(drift, fragility, minor overflow).

---

## Findings

| ID | file:line | Sev | Description | Suggested fix | Effort |
|----|-----------|-----|-------------|---------------|--------|
| R1 | model-families/dsv4/include/sparkpipe/spark_dsv4_pro_model_aliases.h (whole file, absent macro); modules/dsv4_resident_decode_stage/source/spark_dsv4_resident_decode_stage_module.c:205,212-213,1887,2435-2437,2452,3675,3829,4292,4398; tools/generate_dsv4_contracts.py:288 | **P0** | **Pro build does not compile: `SPARK_DSV4_MODEL_DSPARK_SPEC_STEP` has no Pro definition.** The macro is defined only in the Flash branch (`spark_dsv4_model.h:43-45`, default 7). Under `SPARK_DSV4_PRO_BUILD`, that header routes to the aliases header (model.h:7-8), which defines `SPARK_DSV4_MODEL_DSPARK_BLOCK_SIZE` (=5) but **not** `SPEC_STEP`; module.c uses `SPEC_STEP` as array bounds and loop limits in ~10 places. `generate_dsv4_contracts.py:288` emits the overridable `#ifndef …_DSPARK_SPEC_STEP` block for `variant == "flash"` only, and `Makefile.pro:45` passes no `-DSPARK_DSV4_MODEL_DSPARK_SPEC_STEP`. First use (module.c:205 struct field) is a hard compile error for every Pro variant bucket. | Emit `SPARK_DSV4_PRO_DSPARK_SPEC_STEP` in the generator's pro branch (from `dspark.serving_block_size ?? block_size`) and add `#define SPARK_DSV4_MODEL_DSPARK_SPEC_STEP SPARK_DSV4_PRO_DSPARK_SPEC_STEP` to the aliases header; mirror the `DSPARK_SPEC_STEP=` override knob from `Makefile:26-29` in `Makefile.pro`. | S (0.5d) |
| R2 | modules/dsv4_resident_decode_stage/source/spark_dsv4_dspark_pro_kernels.cuh:21,156; source/spark_dsv4_dspark_pro_chain.cuh:25,129 (included from spark_dsv4_resident_decode_stage_cuda.cu:3438-3439) | **P0** | **The "landed" Pro DSpark native pass is dead code compiled to nothing.** Both new files are guarded by `#if defined(SPARK_DSV4_MODEL_BUILD) && …`, but the build defines `SPARK_DSV4_MODULE_BUILD` (Makefile:25, Makefile.pro:45); `SPARK_DSV4_MODEL_BUILD` is defined nowhere in the tree, so both translation units expand empty. Worse, even with the guard fixed the chain would not compile: it references state members that do not exist (`state->stream`, `state->dspark_capture_bf16`, `dspark_main_kv_bf16`, `dspark_draft_streams`, `dspark_markov_embed_bf16`, `dspark_confidence_*`, `state->vocab_offset/shard_count`, `mtp_layers[i].attn_sink_f32`) and undeclared helpers (`SparkDsv4ModuleLaunchDenseFp8Linear`, `SparkDsv4ModuleContinueDsparkLayer`, `SPARK_DSPARK_DRAFT_HEAD_DIMENSION`, `SPARK_DSPARK_DRAFT_ATTENTION_HEAD_COUNT`), and calls `SparkDsv4ModuleLaunchEmbeddingGather` without its `hidden_dimension` arg. Net: TOP10 item 1 ("GA DSpark native pass") is **not landed** despite file names/comments claiming it. Note module.c's own generic draft chain (4285-4560+, validated below) is real but is the Flash-shaped path gated on `bucket == SPEC_STEP+1`. | Fix the guard to `SPARK_DSV4_MODULE_BUILD` only if the chain is rewritten against real state fields; otherwise delete both files or move them under a clearly-marked `proposals/` path so the tree stops implying the pass exists. Add a source-contract test asserting any `.cuh` included from `_cuda.cu` compiles non-empty under the module flags (pattern already exists in tests/test_dsv4_driver_source_contracts.py). | M (2-3d to make honest; L to finish the pass) |
| R3 | modules/dsv4_resident_decode_stage/source/spark_dsv4_resident_decode_stage_cuda.cu:3169-3170, 3227 (routed W13/W2); Makefile.pro:4-11; model-families/dsv4/include/sparkpipe/spark_dsv4_pro_model.h:51-57 | **P1** | **`PRO_EXPERT_CODEC=fp8_e4m3` is a selectable build that bricks every MoE layer at runtime.** The flag flips `SPARK_DSV4_MODEL_EXPERT_WEIGHT_CODEC` to FP8_E4M3 and `tools/dsv4_pro_expert_requant.py` produces matching packs, but the routed-expert entry points hard-require MXFP4 views (`w1->weight_format != SPARK_LM_WEIGHT_FORMAT_MXFP4_E2M1 → cudaErrorInvalidValue`) and call the MXFP4-only `SparkLmHostLaunchSm121FusedExpertW13`/`ExpertW2` (B1 kernels in spark_lm_kernels.cuh:3335+ decode 4-bit payloads/scales directly). Only the *shared/dense* W13 path is codec-generic (`WEIGHT_BITS` template, FP8 instantiated at :3125/:3321). Result: fp8-expert packs load, pass geometry validation (-7 compares equal codecs), then fail every frame at the first routed-expert launch. Fail-closed, but the selectability axis is a trap, not a knob. | Either gate the Makefile flag with a `#error` until the grouped FP8 kernel exists, or add the FP8 specialization of the B1 grouped expert kernels + tile-N route build and relax the two format checks to accept both codecs. | L (3-5d) |
| R4 | modules/dsv4_resident_decode_stage/source/spark_dsv4_resident_decode_stage_cuda.cu:207-258 (SparkDsv4QuantSimGroup/Kernel), 344-358 (KvPost sim), 1063-1085 (emission sim); Makefile.pro:14-20; tools/dsv4_pro_stagepack.py:268-289 | **P1** | **Real FP8-KV cache still missing; the kv_fp8 variant is a lie end-to-end.** `KV_CACHE_CODEC` is unused in device code — KV writes/reads remain BF16 with quantization error *simulated* in-register (QuantSim), exactly as TOP10 documented. Meanwhile `PRO_KV_CODEC=fp8_e4m3` + `--kv-codec fp8_e4m3` produce packs whose header passes `CompareGeometry` (-8) against an FP8-expecting build, while the pool is allocated and touched as bf16 throughout. No bytes are E4M3; capacity/fidelity claims implied by the variant id are false. | Same posture as R3: `#error`-gate `SPARK_DSV4_PRO_KV_CODEC_FP8_E4M3` until the write-site conversions (CacheScatter/KvEmission incl. index) and three read sites emit/consume E4M3+UE8M0 per tools/devcycle/dsv4_pro_kv_codec_plan.md, or drop the flag from Makefile.pro. | M for the gate; L for real FP8-KV |
| R5 | modules/k3_resident_decode_stage/source/spark_k3_resident_decode_stage_runner.cu:823-862 | **P1** | **K3 runner initialization performs ~15 unchecked allocations with no error path.** `cudaMalloc(&state->head_slots_device,…)`, `route_expert/route_packed_row/route_source_token/route_weight/group_*`, `head_candidate_*`, `output_*`, `positions/context_length/sequence_of_row/kda_state_index` and four `new[]` arrays all ignore return values; the function ends `return SPARK_STATUS_OK` unconditionally. A single failed cudaMalloc (pool exhaustion is realistic given these sizes scale with `max_input_row_count × tp_degree`) leaves null device pointers that later kernels silently no-op on, corrupting the head exchange; earlier successful allocations are leaked because there is no unwind. | Collect statuses (or wrap in a small RAII/`goto fail` ladder mirroring the dsv4 ledger pattern), free on failure, and return `SPARK_STATUS_CAPACITY_EXCEEDED`. | S (0.5d) |
| R6 | modules/qwen38_resident_decode_stage/include/sparkpipe/spark_qwen38_resident_decode_stage_firmware.h:15,26-33,35; source/spark_qwen38_serving_adapter.c:2; source/spark_qwen38_resident_decode_stage_cuda.cu:10 | **P1** | **qwen38-max carries wholesale stale copy-paste identity from qwen36-27b.** Header title "Qwen 3.6 27B resident decode stage"; boundary-payload paragraph "rows x 5120 bf16" (real hidden is 8192 → rows × 16384 B, `SPARK_QWEN38_MODEL_HIDDEN_DIMENSION`), including the derived ring math "512 × 10.24KB = 5.2MB" and the "54GB of weights / 128GB / 2K-token caps" capacity narrative — all 27B numbers presented as Max sizing guidance; serving adapter and CUDA TU titles likewise say "Qwen 3.6 27B". Anyone sizing PP stages or boundary bandwidth from the firmware header will be wrong by 1.6× on payload and far worse on weights. | Rewrite the comment block against Max geometry (92 layers, hidden 8192, GDN 69/full-attn 23), fix the byte math, and retitle the adapter/CUDA headers. | S (2h) |
| R7 | modules/dsv4_resident_decode_stage/source/spark_dsv4_resident_decode_stage_module.c:47-49; include/sparkpipe/spark_dsv4_resident_decode_stage_firmware.h:39-41 vs module.c:880, 2531-2569, 3587-3702 | **P2** | **Stale refusal contract: comments say "GA DSpark execution remains refused / zero speculative-token capacity", but the code ships a live DSpark lane.** module.c unconditionally sets `dspark_enabled = 1` (:880), stages drafts + 8-row verify frames when `SPARK_BATCH_BUCKET == SPEC_STEP+1` (:2531), runs Leviathan acceptance and advances lanes/pages accordingly (:3587-3702) — i.e., the Flash B8 lane executes speculation today (the c834436 milestone). The firmware header still promises refusal and excluded checkpoint layers, and packs *require* MTP tensors whenever `MTP_LAYER_COUNT != 0` (VerifyCoverage :1241-1261), contradicting "baseline packs exclude all three checkpoint DSpark layers". Doc/code drift here is dangerous: the refusal text is what reviewers quote when reasoning about spec-decode safety. Additionally, hot-path `fprintf(stderr,…)` trace spam fires per frame (`dspark_staging`:2530, `dspark_accept`:3607, `dspark_drive_entry`:4298, DSPARK_S0_TRACE sync-per-stage at :4383-4389) — stderr writes and extra stream syncs on the serving path. | Update both comment blocks to describe the actual gate (bucket==k+1 ∧ pack carries MTP), move traces behind an env-gated verbose flag, and drop the per-stage S0 syncs from anything but debug builds. | S |
| R8 | modules/dsv4_resident_decode_stage/source/spark_dsv4_stagepack_format.h:320-321 | **P2** | **FP4 payload byte math truncates odd element counts.** `PayloadBytes` returns `elements / 2u` for `WEIGHT_FP4_E2M1`; any tensor whose `rows*columns` is odd silently drops the last nibble and shifts every subsequent offset check. All current shapes are even products (expert stacks, tid2eid=vocab×6), so this is latent, but the function is the shared validator for arbitrary future entries and there is no assert. | `if (elements & 1u) return 0;` (loader treats 0 as invalid) or round up + reject in `ValidateEntry`. | XS |
| R9 | modules/qwen38_resident_decode_stage/source/spark_qwen38_paged_kv.c:152-155 | **P2** | **Content-index bucket sizing can overflow u32.** Loop condition `buckets < physical_page_capacity * 2u` — `physical_page_capacity` is unbounded above in `Initialize` (only `!= 0` checked), and `cap * 2u` wraps for cap > 2^31, collapsing buckets to 16 and degrading the prefix index to linear chaining. Not a memory-safety bug; a silent perf cliff. | Compute in u64 (`(uint64_t)cap * 2u`) and clamp `hash_bucket_count` to the core's maximum. | XS |
| R10 | modules/k3_resident_decode_stage/source/spark_k3_resident_decode_stage_runner.cu:886 (with :821-827) | **P2** | **Cross-type scratch aliasing in the K3 head exchange.** `SparkTpCollectiveAllReduceSumF32(..., (float *)state->staging_values)` receives into the MoE staging buffer sized `max_rows*K3_TOP_K*(2*INTERMEDIATE)` u16. Capacity happens to dwarf `rows*2*tp_degree` floats for any plausible config, but safety depends on unrelated geometry constants and on the MoE staging not being live across the collective (it isn't today — the exchange runs after expert reduce). Fragile by construction; one reorder breaks it. | Allocate a dedicated `head_slots_recv` float buffer of `max_input_row_count*2*tp_degree`, or static_assert the capacity relation where the aliasing occurs. | XS |

Reviewed and found sound (no finding): DSpark verify/acceptance math — row staging
is `[anchor, d1..dk]` (:2443-2453), acceptance `o_k == d_{k+1}` comparison and
`1+accepted` emission align correctly (:3601-3611); tap-store publish indexes
`layer*(k+1)+accepted` stay within the `(k+1)*targets*hidden` allocation
(:1915, :3674-3677); `lane_next_positions` advance avoids the documented
double-count regression (:3612-3621); async-completion path releases
lane/slot/cache claims on every failure branch, quarantines them when the fence
itself fails (:5487-5518, :5375-5405); TP credit byte math is overflow-guarded
(:686-693); stagepack geometry compare is field-by-field with unique negative
codes and version pinning (:418-463); prefill emit-shape dedup/bounds
(:2052-2069); token-id range guard on qwen38 embedding upload
(qwen38 module.c:1657-1662); qwen38 paged-KV borrow/return accounting including
the F1 partial-growth fix (:395-411) and F4 witness-in-committed-tokens clamp
(:508-518).

---

## docs/TOP10_DSV4-PRO.md — item status @ c834436

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 1 | Land the GA DSpark speculative execution pass (native Pro pass) | **STILL-OPEN** | Refusal comment persists (module.c:47-49, firmware.h:39-41); the new `dspark_pro_kernels/chain.cuh` are dead-guarded (`SPARK_DSV4_MODEL_BUILD` never defined) and reference nonexistent symbols (R2). The running draft path is the Flash-shaped generic chain gated on `bucket==SPEC_STEP+1`; Pro cannot even compile (R1). |
| 2 | Reconcile two KV-cache systems onto the common arena (KV_SEAM §3.3) | **STILL-OPEN** | `model-families/dsv4/src/spark_dsv4_cache_plan.c` + `spark_dsv4_cache_arena.c` coexist with the module-runtime `paged_cache`/arena pair (module.c:5860-5862 reserves via arena while paged_cache owns layout). |
| 3 | Adopt the admission/policy core (phases C/D, dsv4 first) | **FIXED** | 4cfa095 "core: land admission/policy core"; `SparkAdmissionEvaluate` called from `spark_dsv4_serving_adapter.c:1255,1296,1313` and `spark_dsv4_stage_runner.c:386`; module Admit now emits `REJECTED_UNSUPPORTED_SHAPE` distinctly (module.c:5966-5969). |
| 4 | Fold the Pro packer into the Flash packer (DRY H2) | **STILL-OPEN** | `tools/dsv4_pro_stagepack.py` is still the importlib geometry-override wrapper (:23-31, :319-325). |
| 5 | Generate identity (module-id / description sha) from the contract | **STILL-OPEN** | Aliases header still hand-maintains driver revision, module id, target, and both description SHA256s (aliases :57-63); Makefile.pro:22 still rebuilds the id fragment by string concat. |
| 6 | Real FP8-E4M3 KV cache (write/read conversion) | **STILL-OPEN** | QuantSim simulation unchanged (R4); `KV_CACHE_CODEC` absent from device code. |
| 7 | FP8-E4M3 expert kernel variant + FP8-aware sharder | **STILL-OPEN (partially advanced)** | Dense/shared W13 is now `WEIGHT_BITS`-templated with FP8 instantiated (spark_lm_kernels.cuh:3099-3333), but routed-expert kernels remain MXFP4-hardwired and the launcher rejects FP8 views (R3); no FP8-aware sharder. |
| 8 | Weight read-ahead generalization (next-layer expert prefetch) | **STILL-OPEN** | Read-ahead remains attn-side (WQ_B / hc-fn payloads; module.c:3844-3864, ReduceHiddenReadAhead); no gate-driven expert prefetch found. |
| 9 | 2-token decode chains + control-plane pacing | **STILL-OPEN** (unverified statically) | Chain machinery accepts multi-step (`chain_step_count`, ValidateResidentChain :3453) but nothing in-tree configures production 2-token chains or the raised pacing ceilings; needs config-side confirmation off-repo. |
| 10 | Adopt the neutral speculation tree (Pro-shaped 5/6) | **STILL-OPEN** | Acceptance is still the hand-rolled greedy Leviathan loop (module.c:3597-3605); `spark_dsv4_speculation_tree.h` does not exist in model-families/dsv4. |

## Known-issue verification requested at this HEAD

| Known issue | Verdict |
|---|---|
| dsv4-pro GA DSpark execution refusal (module.c ~47) | **Still-present as text, but superseded by code**: the generic DSpark lane executes on Flash B8; Pro is blocked upstream of any refusal by R1/R2. See R7 for the drift hazard. |
| Missing FP8 expert / FP8-KV kernel variants behind codec flags | **Still-present**, and now *selectable*: R3 (experts) and R4 (KV). Recommend gating before anyone builds the variant. |
| `SPARK_DSV4_MODEL_DSPARK_SPEC_STEP` alias fix | **NOT present.** Generator emits the overridable define for flash only (generate_dsv4_contracts.py:288-293); aliases header lacks the mapping; Makefile.pro lacks the override knob. Build-breaking: R1. |
| qwen38-max stale copy-paste comments ("Qwen 3.6 27B", hidden 5120) | **Still-present**: R6 (firmware.h:15,35; adapter :2; cuda :10). |
| MTP path validation coverage | **Substantially fixed at the contracts layer.** dsv4: per-layer bit coverage incl. MTP stages + extras pointer check (module.c:1241-1261); packer emits full MTP record set with slice-correct counts (dsv4_pro_stagepack.py:175-231, verified against `ExpectedTensorCountForOwnership`). qwen38: strict `expected_mtp` bitset refusal (qwen38 module.c:523-526) and runtime MTP fails closed `UNSUPPORTED` (module.c:4-5). Runtime draft-path bounds reviewed sound (see "Reviewed and found sound"). Residual risk sits in the dead Pro chain files (R2), which bypass all of this if ever wired up naively. |

## Top-speed context (unchanged from TOP10)

Flash decode is measured (40.67 tok/s B1 per c834436); Pro remains
`NOT_MEASURED / production_ready false` (dsv4_pro_authoritative.json:77-81) and
is currently **unbuildable** pending R1. Nothing in this review changes the
TOP10 speed thesis: item 1 (native DSpark) remains the dominant lever once the
variant builds again.
