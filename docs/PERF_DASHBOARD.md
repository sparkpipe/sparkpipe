# Performance dashboard (HWM records only)

Updated on every new high-water-mark measurement or at ~15min intervals.
Values are the LATEST RECORDED per model - no benchmark reruns. Sources are
receipts/PERF docs in-tree; accuracy rows show the golden-hash state.

Legend: [HWM] = best recorded. spec = speculation ON. Accuracy bar:
a model's row is accuracy-VALIDATED only when its output reproduces the
pinned golden (no-spec O128 hashes, or the COMPSEC-17 set once accuracy is
reached - COMPSEC-17 scores get their own column then).

## Output speed (decode tok/s, HWM)

| Model | B1 no-spec | B1 spec | B8 | B16 | other B* |
| --- | --- | --- | --- | --- | --- |
| DSV4 Flash (TP4) | **40.46** [exact 3/3] | **8.48** (k=7, hash WRONG - not valid) | - | - | - |
| Qwen 3.8 27B (TP1) | **8.03** mixed/FP8 29.9GB pack (repro 8.028 of the 8.00 HWM; true-BF16 54.6GB pack = 4.45 - CORRECTED from stale 3.10) [validated] | DFlash2 block8: **5.93** @ L=0.55 first-pos 45% - FIRST VALID LOSSLESS spec measurement (spec stream == no-spec golden, 128/128 bit-exact, hash 84c1035f); LOSES to no-spec (0.74x); acceptance gap vs authors' L=3.39 -> tap-capture interrogation next. MTP D=2 7.16; DSpark k=7 5.068 @ L=0.735 (10.5%) | - | - | - |
| K3 | **none measured** (never run on fleet hardware) | - | - | - | - |
| GLM52 | **6.91** | - (draft weights untrained) | 2-3 | 2-3 | - |
| Qwen3.8-Max | 1.29 per-request | - | 0.61 | - | ~39 agg @B256 (TP4xPP4 replicated) |
| DSV4 Pro | ~12-13 (bandwidth-grounded est, not e2e) | - | - | - | - |

## Prefill speed (tok/s, HWM)

| Model | B8 | B1024 | notes |
| --- | --- | --- | --- |
| K3 | - | - | RETRACTED (not hardware-measured) |
| others | - | - | not recorded / not wired (Max) |

## Accuracy / correctness state

| Model | no-spec golden | spec golden | TP correctness |
| --- | --- | --- | --- |
| DSV4 Flash | O128 3/3 exact | CSA keep-old fix landed: 23 exact tokens (2c40465d); full-O128 not yet (golden pin moved to 211462f2 batch) | TP4 exact |
| Qwen 3.8 27B | bit-exact GPU validation + genuine prompt; O128 pins: BF16 7b58db3a..., mixed/FP8 80e8fb9d... (a9385d0b is the DSV4 golden, NOT qwen36) | DSpark draft parity BIT-EXACT vs fixed reference [220,16,92,198,12,328,82] (draft[1] = genuine 19.625 tie, first-max -> 16); spec speed not yet measured | TP1 |
| K3 | not measured | - | TP16 serial replay sum-vs-golden PASS (worst 0.0508) - correctness only |
| GLM52 | pre-fleet | - | - |
| Qwen3.8-Max | level-1 state | - | - |
| DSV4 Pro | - | - | transport-open validated |

## Active climbs (references being matched)

- 27B: DSpark port LANDED (49aa3b3 + gate coverage); greedy 5.078 @ 10.5%; sampling lever empirically CLOSED (probabilistic worse vs greedy target). DFLASH2 ADOPTION GO (spec landed c5053e7): Qwen3.8-27B drafter exists (z-lab, 3.85GB BF16, 5 layers, block 8, taps [5,19,33,47,61]); acceptance 4.80 vs DSpark 3.62 (+32.6%), vs no-spec 3.11-3.43x @ conc1; conv+selector bolt-ons, Markov slots = codebooks VERBATIM; gates: LM-head-BF16 check (step zero) + upstream sm120 OOB warning; W1-W8 ~8-11 days.
- Flash: keep-old CSA landed (23 exact); staging FULLY EXONERATED (frame-149 = anchor 290@150 + 7 distinct drafts sequential - mixed-staging falsified; pad = first-decode only, all-identical per-row); BUG = layer-0 wq_a FP8 pair batch-coupled activation scale (ch 256+ at 128-tile boundary); FIX DRAFT EXISTS in fork clone (multi-row -> per-row-exact routing) - build + r1_wq_a==delta_ge_raw verification pending; then k-sweep k=5/7/8/10.
- COMPSEC-17: added as a tracked stat once spec accuracy is reached.

Last update: 2026-08-19 19:15 UTC (REAL GSM8K CONFIRMS THE GAP IS THE FORWARD. The prompt-agent's real-dataset pack (gsm8k-real-1..25, verbatim openai/gsm8k test rows 0-24, tokenizer-verified) measures mean accepted 1.543 (484 rounds) - SAME as the synthetic pack. The prompt-set hypothesis is dead: the ~3.5x gap vs z-lab's published 5.46 lives in the drafter forward or the accept semantics. Fresh oracle agent (484e8081) building a host reference of the z-lab drafter forward to bisect the divergence. k=4 speed retest on the lossless driver: 257s vs 272s at k=8 (0.97x no-spec) - no win; the fused multi-row verify remains the speed lever. Kernel lane PARKED (stale-queue thrash; its library-linear hardware check 8825579 is recorded - the library path is bit-exact at 5..8 rows). Box: driver A (verify-step + gate default, lossless), depth 8, spec-armed. NO HWM MOVES. PREVIOUS: RECORD CORRECTION - NO COLLAPSE EVER HAPPENED. The 16:00 dashboard's 'ladder 4.27 (gsm25) / 4.70 (math25)' was the mean CREDITED tokens per round (accepted+3), mislabeled as acceptance. The lane's own historical grep of the 14:34-era dspark D=7 window shows mean ACCEPTED 1.350 - the true acceptance was always ~1.3-1.5. Hardware A/B/C on 23/25 GSM8K single-request ladders, fresh ids: driver A (gate+verify-step) 1.516, driver B (gate only) 1.518, driver C (tiled path via env) 1.515 - NO config change moves acceptance. The lane's hardware check (8825579) proves the library path is bit-exact for every verify+drafter shape (8-row == 8x1-row, 0 differing) - the library path is CORRECT. The gap vs published (4.10-5.46, real GSM8K/MATH-500/MT-Bench on z-lab's serving) is ~3x and has been constant: the open levers are (1) drafter-forward exactness vs the z-lab reference (lane oracle task), (2) real benchmark datasets (our pack is GSM8K-STYLE synthetic, not the real GSM8K), (3) possibly the newer Muse-Glimmer-30B block-16 drafter. Box restored to driver A (verify-step ON, gate default) - the lossless config. NO HWM MOVES. PREVIOUS: ACCEPTANCE COLLAPSE NAMED TO THE BUILD DELTAS - A/B RUNNING. Clean 23/25 GSM8K single-request ladder on driver v2 (tiled gate + verify-step): mean accepted 1.516 (657 rounds) vs the 16:00 measurement of 4.27 on the same prompt set at the same 128-token budget. The lane's 8-row attention 'corruption' is DISPROVEN - its harness only ran two single-row reference frames, so kv_bad=12288=6x2048 unwritten rows; the kernel is exonerated. The 4.27->1.5 drop must come from the two target-side deltas: the tiled gate (21d822c) or the verify-step (908b18c). A/B on the box now: driver B (gate ON, verify-step OFF) measuring; driver C (env SPARK_QWEN36_SMALL_BATCH_GEMM=1 = tiled path restored, verify-step OFF = the 16:00 shape) next. Also found: spec only engages for single-sequence submissions (adapter 42a2fab), so B25 packed batches silently decode without speculation - the ladder must use split per-request batches. NO HWM MOVES. PREVIOUS: WORKING DFLASH2: 11/11 LOSSLESS AT DEPTH 8. Driver v2 = tiled gate (21d822c) + verify-step routing (908b18c) + Destroy fix (71a7bc3) + gate polarity (e10aaa9), all validator-green and deployed. Depth-8 spec streams are BIT-EXACT vs the no-spec reference on all 5 MT-Bench prompts (incl. roleplay AND the coding prompt that diverged at 108 before the verify-step fix) plus 3 HumanEval + 3 MBPP. THE CODING DIVERGENCE IS CLOSED. Open items: (1) acceptance 1.12 at depth 8 (published MT-Bench 4.10) - the lane reproduced the cause on-device: SparkQwen36AttnPrepareKernel writes WRONG KV for rows >= 3 of the multi-row verify attention (rows 0-1 bit-exact, first_kv=4096 = row 2), so the accept loop rejects good drafts at rows 2..7; kernel fix in the lane now, 8-row gate (eb26cb4) will land with it. (2) speed: B5 spec 2.35 vs no-spec 2.57 tok/s = 0.92x on the same session (no-spec B1 HWM 8.03 stands; the box serializes per-sequence at concurrency) - spec still loses everywhere measured; the fused multi-row verify (one K/V pass) is the speed fix after the KV correction. NO HWM MOVES. PREVIOUS: DEPTH-8 HARDWARE VERDICT + VERIFY-STEP LANDED. Driver 71a7bc3 (tiled gate) at depth 8 on the 5 MT-Bench prompts: 4/5 LOSSLESS (writing, roleplay, reasoning, math) - the coding prompt diverges at token 108. Round audit: the first bad token is the REPLAY EMISSION (round at base 326, min_accepted=0) - the replay's re-walk reads KV written by the 8-row verify attention; the lane's GDN-3 multi-row-attention localization confirmed as the last item (its write-side retraction covered only 2 rows). Depth-8 MT-Bench acceptance = 1.13 (published 4.10) - the chunk-built verify heads suppress acceptance AND the verify-step fix (908b18c, lane ded861d: verify GDN now walks the step path like the replay) is being redeployed to fix both the acceptance and the correction truth. Depth-2 coding prompts were LOSSLESS (all 6). The lane is extending the multi-row attention comparison to 8 rows (KV write truth). NO HWM MOVES. PREVIOUS: DEPLOYED + VALIDATED. Head 71a7bc3: (1) 21d822c gates the 5..8-row tiled linear path behind SPARK_QWEN36_SMALL_BATCH_GEMM=1 - library path carries the verify (7-row) and replay frames; (2) e10aaa9 flips the layer_amplification gate to assert the chunk-vs-step retraction; (3) 71a7bc3 fixes Destroy free()ing a cudaMalloc'd slot buffer (validator segfault - caught ONLY because the flipped gate let CheckModule reach teardown). Full GPU validation PASS incl. module_determinism + module_mtp_draft; module published validation=executed; driver deployed to spark3. Env restored to armed shape (no SMALL_BATCH_GEMM line; the adapter sets GDN_SNAPSHOT_SLOTS=8 itself when spec is on). no-spec reference runs (MT-Bench + HumanEval + MBPP, fresh ids) in progress, then the spec repros - the decisive lossless check for the tiled boundary. NO HWM MOVES. PREVIOUS: THE BOUNDARY IS CLOSED - deploy in progress. The D=4-lossless / D=6-diverged lattice aligned EXACTLY with the SparkQwen36LaunchLinear row-count dispatch: only verify (7 rows) and replay (min_accepted+2) reach rows 5..8, and only those frames hit the small-batch TILED kernel - whose varies-across-boots signature matches a layout-dependent read. The tiled path is now gated behind SPARK_QWEN36_SMALL_BATCH_GEMM=1 (21d822c); 5..8 rows take the library path the lossless controls prove correct. Same wave: the layer_amplification validator gate flipped polarity (e10aaa9) - it now asserts the recorded chunk-vs-step retraction (bounded 2.98e-08/3.15e-10) instead of demanding the dead theory amplify, kept FATAL against a future regression. Deploy on spark3: archive built, publish+GPU-validation running; roleplay + coding repros next to confirm lossless. NO HWM MOVES - nothing new measured this tick (spec still below no-spec 8.03 at every measured concurrency; the fused multi-row verify is still the speed fix). PREVIOUS TICK: BATCH-MODE NUMBERS - THE LADDER HITS THE PUBLISHED RANGE. The 50-prompt batch sweep (25 GSM8K + 25 MATH-500, ~3200 tokens each, B25 concurrency) measures: ladder 4.27 (gsm25) / 4.70 (math25) - IN the published DFlash2 range (4.10-5.46) - and spec 14.68/16.53 tok/s vs no-spec 15.83/17.34 at the same batch size = 0.93-0.95x (the batch interleaving amortizes the round overhead; B1 remains 0.85x). The k-cap (052d0e5: SPARK_QWEN36_SERVING_SPECULATIVE_DRAFT_COUNT caps the dspark verify depth) measured k=4 -> 15.23 = 0.96x - a small win, not the break. The audit also named the LAST correctness residual: replay_row_mismatch verify=9045 replay=561 at the coding divergence - the verify's chunk-built outputs feed the accept+correction, so the verify must walk the step path too (lane has the patch task). The bar >= 6 acceptance needs the ladder from 4.3-4.7 to 6: the prompt-class sample + the verify-step fix. No HWM change since 8.03 (spec still under no-spec at every concurrency - the honest record). DSV4-FLASH PAUSED)
